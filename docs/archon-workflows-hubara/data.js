/* Auto-generado por gen.mjs — no editar a mano. */
window.PIPELINE_ORDER = ["hu-hubara-pipeline","hu-hubara-plugin-pipeline","review-pr-hubara","idea-a-hu-hubara"];
window.PIPELINES = {"hu-hubara-pipeline":{"pipeline_id":"hu-hubara-pipeline","title":"hu-hubara-pipeline — MAIN end-to-end HU orchestrator (NIVEL A, plugin-level DAG)","purpose":"Super-pipeline AUTOMATIZADO end-to-end para una HU de AgencyHubara (plugin system: DEHA backend + FSD frontend + Temporal workers). Toma un input (GitHub issue URL, ruta .md local, texto plano, o HU_ID existente para resume), lo refina técnicamente, planifica plugin-level, implementa (single-plugin inline via sub-pipeline / multi-plugin fan-out MANUAL con approval), corre validación final consolidada de ambos stacks con scope-detection, pasa 3 gates pre-PR (premortem self-review, evaluator rubric, multi-agent code-review de 5 specialists paralelos), crea 1 PR consolidado contra main (reusa si existe), archiva los artefactos + mergea spec-deltas, y dispara el review automático en background. Es el NIVEL A (plugin-level); delega el feature-level a hu-hubara-plugin-pipeline (NIVEL B). 77 nodos. NINGÚN gate aplica fixes — premortem/evaluator/review delegan al implementer vía loop; estados ambiguos cancelan visiblemente (anti-merge-silencioso). VERIFICACIÓN INDEPENDIENTE: el modelo del primer pass es estructuralmente CORRECTO (los 77 nodos, depends_on, when, trigger_rule y edges coinciden con el YAML); los únicos defectos estaban en sus verification_notes (conteos auto-contradictorios), corregidos acá.","trigger":"archon workflow run hu-hubara-pipeline \"<input>\" donde <input> = $ARGUMENTS / $USER_MESSAGE es UNO de: URL GitHub issue (^https://github.com/<owner>/<repo>/issues/<N>$, debe estar OPEN), ruta a un .md local existente con la HU, texto plano, o un HU_ID existente (^HU-[0-9]{8}-[0-9]{4,6}-.+ para smart-resume). CRITICAL: toma SOLO 1 token lógico — pasar el formato del SUB-pipeline (\"<HU_ID> <plugin>\") contamina el HU_ID con un espacio; gen-hu-id lo rechaza con valid:'false' (gotcha #10, run 38d8223e). Override env vars: MAX_PLUGINS_PER_HU=N (cap plugins en validate-plan, default 8), FORCE_ALL_GATES=1 (corre todos los gates de final-validation ignorando scope detection vs origin/main), ARCH_EVAL_OVERRIDE=1 (mencionado SOLO como hint en cancel-on-eval-block; ningún nodo lo lee — el \"override\" real es desactivar el nodo manualmente). worktree.enabled=true; provider claude; model sonnet; interactive false (pero rama-B-wait-fan-out-done es un approval node que espera input igual).","node_count":77,"inputs":["$ARGUMENTS / $USER_MESSAGE — input crudo (issue URL | ruta .md | texto plano | HU_ID resume)","$ARTIFACTS_DIR — dir de artefactos del run (substituido literal + env var real en bash nodes)","$WORKFLOW_ID — id del run (substitución literal)","Env: MAX_PLUGINS_PER_HU (default 8; leído por validate-plan)","Env: FORCE_ALL_GATES (default 0; leído por final-validation para forzar todos los gates)","Env: ARCH_EVAL_OVERRIDE (mencionado como hint en cancel-on-eval-block; ningún nodo lo lee)","hubara_agency/.hubara/spinal-files.yaml (convención, copiada a artifacts por stage-shared-files)","hubara_agency/.hubara/project-context.md (convención, copiada a artifacts)",".archon/github-project-config.yaml (opcional — habilita GitHub Project sync fail-soft)","Smart-resume reads: hubara_agency/.hubara/refinements/<HU_ID>-tech.md (detect-resume-state, load-refinement-if-resume), plans/<HU_ID>/plugin-manifest.yaml (detect-resume-state, load-plan-if-resume)","Per-plugin results escritos por sub-pipelines: hubara_agency/.hubara/results/<HU_ID>/plugin-<id>-result.yaml + feature-results/<plugin>/task-result.yaml"],"global_logic":"MODE DETECTION: el plugin-planner emite mode en plugin-manifest.yaml ∈ {single_plugin, multi_plugin, no_work, blocked}. classify-mode (bash + python3 heredoc) re-lee el manifest commiteado y emite JSON {mode, plugins[], batches[{id,plugins}], requires_merger (de totals.requires_merger), single_plugin_id (plugins[0] si single)}. RAMA A (single_plugin): rama-A-single-plugin-inline invoca el sub-pipeline hu-hubara-plugin-pipeline \"<HU_ID> <plugin>\" INLINE (env -u CLAUDECODE, espera rc, log a file), luego git fetch + merge --ff-only origin/$BRANCH + valida plugin-<id>-result.yaml status ∈ {passed, passed_with_warnings}. RAMA B (multi_plugin): rama-B-print-fan-out-commands imprime 1 comando por plugin del PRIMER batch (solo 1 batch soportado, comentario L1347-1351) + escribe .current-batch-plugins; rama-B-wait-fan-out-done es un approval node (espera \"ready\"/\"abort\"); rama-B-merge-batch ff-mergea + valida cada plugin-result (MISSING/FAILED→FAIL_BATCH_INCOMPLETE, passed_with_warnings continuable); rama-B-invoke-merger-if-shared corre command hubara-merge-intents si requires_merger=='true'; rama-B-commit-merger commitea spinal files consolidados (pull --rebase + retry). KEY ENV/VARS: HU_ID (de gen-hu-id, validado por regex ^HU-\\d{8}-\\d{4,6}-[a-z0-9][a-z0-9-]*$); BRANCH=hu/<HU_ID>; ARTIFACTS_DIR; WORKFLOW_ID. BRANCH STRATEGY: setup-branch usa git checkout --detach origin/$BRANCH (HEAD detached para sobrevivir worktrees stale + multi-worktree concurrency, gotcha #9 + run 710e0eb6); branch fresh = git push origin origin/main:refs/heads/$BRANCH; TODOS los pushes posteriores usan git push origin HEAD:$BRANCH; concurrencia entre pushes vía pull --rebase + retry. SMART-RESUME: detect-resume-state chequea refinement/plan commiteados → skipea FASE 1/2 vía gate-can-plan + when conditions. INVARIANTES RUN-WIDE: (1) cada cancel- node de fallo tiene un dump-on-cancel-* gemelo que corre ANTES (cancel depends_on el dump, dump depends_on el check/gate) para capturar diagnostic-bundle.yaml — EXCEPCIÓN: cancel-on-premortem-missing y cancel-on-review-missing NO tienen dump gemelo (cancelan directo desde el check node); (2) bash gate nodes emiten stdout SINGLE-LINE canónico (diagnostics a stderr) porque downstream usa strict == (gotchas #7/#8); (3) assignments de output refs en gate bash nodes (gate-can-plan, gate-plan-verdict) van SIN dquotes en el RHS por el shellQuote single-quote wrapping (AP=$node.output, no AP=\"$node.output\", gotcha #7, run cadd3d61); (4) NINGÚN gate aplica fixes — premortem/evaluator/review delegan al implementer vía loop con $LOOP_USER_INPUT, deferred complex → cancel visible; (5) project-set-* nodes son fail-soft (trigger_rule all_done, escriben skipped si no hay CFG; awk -F: + gsub para keys con em-dash); (6) los estados-agujero (PM_MISSING, CR_MISSING, PR_RESULT_MISSING/PR_UNKNOWN, RV_RESULT_MISSING/REVIEW_UNKNOWN, EVAL_MISSING/EVAL_UNKNOWN) ahora cancelan fuerte; SOLO el valor \"happy\" (PM_CLEAN/PR_RESOLVED, EVAL_PASS|WARN, REVIEW_CLEAN/REVIEW_RESOLVED) continúa.","phases":[{"name":"FASE 0 — Bootstrap (prereqs + input + branch + smart-resume)","nodes":["check-prereqs","dump-on-cancel-bad-prereqs","cancel-bad-prereqs","stage-shared-files","resolve-input","dump-on-cancel-bad-input","cancel-bad-input","gen-hu-id","dump-on-cancel-bad-hu-id","cancel-bad-hu-id","setup-branch","detect-resume-state","project-set-refining"],"description":"Verifica prerequisitos (gh auth, 8 herramientas, convenciones, 4 skills + guide, 10 commands, 2 workflows, lock files, npm ci, origin, protected files limpios vs origin/main, scope project), stagea shared files, resuelve el input a tipo+title+body, genera/valida el HU_ID con regex, crea/resumea la branch hu/<HU_ID> en detached HEAD, detecta estado de resume y marca el Project en Refining."},{"name":"FASE 1 — Refinar técnico","nodes":["load-refinement-if-resume","refinar-auto","validate-refinement","dump-on-cancel-bad-refinement","cancel-bad-refinement","commit-refinement","post-refinement-comment-to-issue","project-set-refined"],"description":"Skill hubara-tech-refiner-archon (loop max 2) produce hu-refinada.md con 14 secciones + §0 plugin classification; validate-refinement chequea estructura, commit-refinement persiste+pushea (stdout single-line), postea comentario al issue (fail-soft) y marca Project Refined. Salta refinar-auto si smart-resume detectó refinement existente (already_refined=='true')."},{"name":"FASE 2 — Plan plugin-level","nodes":["load-plan-if-resume","gate-can-plan","planificar-auto","validate-plan","gate-plan-verdict","dump-on-cancel-bad-plan","cancel-bad-plan","commit-plan","project-set-planned"],"description":"gate-can-plan decide CAN_PLAN/SKIP según resume+refinement (encapsula A&&(B||C) que el parser no soporta); skill hubara-plugin-planner-archon (loop max 2) produce plugin-manifest.yaml (mode + plugins[] + plugin_batches[]); validate-plan corre python3 (mode coherente, cap plugins, batches cubren todos los ids); gate-plan-verdict colapsa a PASS/FAIL (el parser no soporta regex); commit-plan persiste+pushea y marca Project Planned."},{"name":"FASE 3 — Implementación (rama A single / rama B multi-plugin)","nodes":["project-set-implementing","classify-mode","prewarm-uv-venv","rama-A-single-plugin-inline","rama-B-print-fan-out-commands","rama-B-wait-fan-out-done","rama-B-merge-batch","rama-B-invoke-merger-if-shared","rama-B-commit-merger","dump-on-cancel-multi-plugin-failure","cancel-on-multi-plugin-failure"],"description":"classify-mode bifurca: single_plugin → rama-A invoca el sub-pipeline inline; multi_plugin → rama-B imprime fan-out manual, espera approval, ff-mergea, valida cada plugin-result, corre merger de wiring_intents si shared + commitea. prewarm-uv-venv hace uv sync (+ bootstrap .env desde .env.example). cancel-on-multi-plugin-failure aborta si el batch quedó incompleto."},{"name":"FASE 4 — Validación final consolidada","nodes":["check-pipeline-error","dump-on-cancel-implement-error","cancel-on-implement-error","final-validation","dump-on-cancel-final-validation-fail","cancel-on-final-validation-fail"],"description":"check-pipeline-error es el FAN-IN de ramas A/B (busca pipeline-error.yaml de cualquier sub-pipeline). final-validation corre los gates duros con scope-detection vs merge-base origin/main (render-compose drift, pytest -m architecture, lint-imports, tests/plugins, tests/functional bajo watchdogs file-redirect; npm plugins:sync/test/test:arch/tsc/build; playwright+uvicorn si tocó UI), emite gates-summary.md y PASS/FAIL_*."},{"name":"FASE 4.5 — Premortem self-review gate","nodes":["premortem-self-review","check-premortem-clean","cancel-on-premortem-missing","loop-implementer-resolves-premortem","check-premortem-resolved","dump-on-cancel-premortem-blocked","cancel-on-premortem-blocked"],"description":"command hubara-premortem imagina failure_modes[] en premortem.yaml; check-premortem-clean → PM_CLEAN/PM_HAS_ISSUES/PM_MISSING. PM_MISSING→cancel directo. PM_HAS_ISSUES→loop-implementer (max 2); check-premortem-resolved verifica (busca task-result en ARTIFACTS_DIR + feature-results/, precedencia broken>blocked>resolved). Solo PR_RESOLVED continúa; PR_BLOCKED/BROKEN/RESULT_MISSING/UNKNOWN cancelan."},{"name":"FASE 4.6 — Pre-PR evaluation gate","nodes":["evaluate-pre-pr","gate-evaluator-verdict","dump-on-cancel-eval-block","cancel-on-eval-block"],"description":"command hubara-evaluate puntúa contra rúbrica calibrada → evaluation.yaml (verdict + weighted_average); gate-evaluator-verdict colapsa a EVAL_PASS/WARN/BLOCK/MISSING/UNKNOWN. PASS|WARN continúan a los 5 reviewers; BLOCK/MISSING/UNKNOWN cancelan. evaluate-pre-pr es FAN-IN de las 2 rutas del premortem (PM_CLEAN O PR_RESOLVED)."},{"name":"FASE 4.7 — Multi-agent code review gate","nodes":["review-deha","review-fsd","review-plugin-system","review-test-coverage","review-security","synthesize-review","check-review-clean","cancel-on-review-missing","loop-implementer-resolves-review","check-review-resolved","dump-on-cancel-review-blocked","cancel-on-review-blocked"],"description":"5 commands reviewer-* corren EN PARALELO (DEHA/FSD/plugin-system/test-coverage/security); synthesize-review (trigger one_success) consolida a code-review-findings.yaml + cross-ref con premortem; check-review-clean → REVIEW_CLEAN/HAS_BLOCKERS/HAS_MINOR/CR_MISSING. CR_MISSING→cancel directo. HAS_BLOCKERS||HAS_MINOR→loop-implementer (max 2). Solo REVIEW_CLEAN o REVIEW_RESOLVED continúan al PR."},{"name":"FASE 5 — PR + Project Done","nodes":["build-pr-body","create-pr","project-set-done"],"description":"build-pr-body (bun script) ensambla pr-body.md desde refinement/plan/results/evidencia (strip ANSI); es FAN-IN de las 2 rutas del review. create-pr commitea visual-evidence (non-fatal), hace gh pr create --base main (reusa PR existente si ya hay), escribe .pr-url, marca Project Done (fail-soft)."},{"name":"FASE 5.5 — Archive + spec deltas","nodes":["archive-hu","commit-archive"],"description":"command hubara-archive-hu snapshot de artefactos a hubara_agency/.hubara/archive/<fecha>-<HU_ID>/ + merge de spec-deltas a specs/; commit-archive commitea+pushea (no aborta el pipeline si falla — side effect benigno, trigger_rule all_done)."},{"name":"FASE 6 — Trigger review + summary","nodes":["trigger-review","print-final-summary"],"description":"trigger-review lanza el workflow review-pr-hubara en background (nohup+disown, condicional a que el file del workflow exista); print-final-summary es el FAN-IN final (trigger-review + project-set-done + commit-archive) e imprime el resumen con HU_ID/mode/branch/PR/review status."}],"nodes":[{"id":"check-prereqs","type":"bash","summary":"Verifica todos los prerequisitos del pipeline; emite OK o FAIL_*","detail":"Chequea gh auth, 8 herramientas (node/npm/bun/jq/git/curl/uv/python3), 2 convenciones commiteadas (spinal-files.yaml, project-context.md), 4 skills hubara-*-archon (tech-refiner/plugin-planner/feature-planner/implementer) + guide skill, 10 commands hubara-* (merge-intents/premortem/evaluate/5 reviewers/synthesize-review/archive-hu), 2 workflows, lock files (uv.lock root o hubara_agency, package-lock), npm ci si falta node_modules, remote origin, y detecta protected files modificados/untracked vs origin/main. Si project config existe valida scope. Emite a stdout 'OK' o el primer 'FAIL_<code>'; SIEMPRE exit 0 (routing por el when downstream). timeout 180000.","summary_extra":"","depends_on":[],"trigger_rule":"all_success","when":"","produces":"output: OK | FAIL_GH_AUTH | FAIL_NO_<TOOL> | FAIL_MISSING_CONVENTION_* | FAIL_MISSING_SKILL_* | FAIL_MISSING_GUIDE_SKILL | FAIL_MISSING_COMMAND_* | FAIL_MISSING_WORKFLOW_* | FAIL_NO_UV_LOCK | FAIL_NO_NPM_LOCK | FAIL_NPM_CI | FAIL_NO_ORIGIN_OR_NETWORK | FAIL_DIRTY_PROTECTED_FILES | FAIL_GH_NO_PROJECT_SCOPE","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Único nodo con depends_on vacío (START). Exit 0 incluso en fallo (fail-closed via when). DIRTY_PROTECTED detection es load-bearing: Archon copia .archon/ al worktree y el meta-gate final falla si main local tiene cambios sin commit en protected paths."},{"id":"cancel-bad-prereqs","type":"manual","summary":"Cancela el run con tabla de diagnóstico de prereqs","detail":"Nodo cancel: aborta imprimiendo el output de check-prereqs + tabla de decodificación (FAIL_GH_AUTH→gh auth login, etc.) + puntero al diagnostic-bundle.yaml. Solo cuando check-prereqs != OK.","depends_on":["dump-on-cancel-bad-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output != 'OK'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on el DUMP (no check-prereqs directo) — invierte el orden para capturar diagnostics antes de abortar."},{"id":"dump-on-cancel-bad-prereqs","type":"bash","summary":"Dump forense de diagnostics antes de cancelar por prereqs","detail":"Invoca hubara_agency/.hubara/dump-pipeline-diagnostics.sh con CANCEL_NODE=cancel-bad-prereqs, CANCEL_REASON=output de check-prereqs, PHASE=check-prereqs, HU_ID=unknown. Genera diagnostic-bundle.yaml. Corre solo cuando check-prereqs != OK.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output != 'OK'","produces":"diagnostic-bundle.yaml (side effect)","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Patrón dump-before-cancel: cancel-bad-prereqs depends_on ESTE nodo."},{"id":"stage-shared-files","type":"bash","summary":"Copia spinal-files.yaml + project-context.md a artifacts; detecta Project config","detail":"cp de spinal-files.yaml y project-context.md a $ARTIFACTS_DIR. Si .archon/github-project-config.yaml existe lo copia y emite PROJECT_ENABLED, si no PROJECT_DISABLED. set -e. Solo si check-prereqs==OK.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output == 'OK'","produces":"output: PROJECT_ENABLED | PROJECT_DISABLED; copia archivos a artifacts","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"PROJECT_ENABLED/DISABLED NO se consume por when downstream; los project-set-* re-chequean la existencia del CFG en artifacts."},{"id":"resolve-input","type":"bash","summary":"Clasifica el input crudo a tipo (issue/resume/file/text) + extrae title/body","detail":"Lee $ARGUMENTS. Si vacío → {type:error,error:empty_input}. Regex: issue URL → gh issue view (valida state OPEN, else issue_not_open), ^HU-[0-9]{8}-[0-9]{4,6}-.+ → hu_id_resume (hu_id_override), archivo existente → local_file (title del primer # heading), else plain_text. Emite JSON con jq: {type, title, body, issue_url, hu_id_override}.","depends_on":["stage-shared-files"],"trigger_rule":"all_success","when":"","produces":"output.type in {issue_url, hu_id_resume, local_file, plain_text, error}; output.{title, body, issue_url, hu_id_override}; error con output.error in {empty_input, cannot_fetch_issue, issue_not_open}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"output.type enrutina cancel-bad-input. El regex de resume es más laxo que el guard de gen-hu-id (que exige slug lowercase), por eso gen-hu-id re-valida."},{"id":"dump-on-cancel-bad-input","type":"bash","summary":"Dump forense antes de cancelar por input inválido","detail":"Invoca dump-pipeline-diagnostics.sh con CANCEL_NODE=cancel-bad-input, PHASE=resolve-input, HU_ID=unknown. Corre cuando resolve-input.output.type == error.","depends_on":["resolve-input"],"trigger_rule":"all_success","when":"$resolve-input.output.type == 'error'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Lee output.field (.type) — Archon parsea el JSON stdout de resolve-input para resolver el sub-campo."},{"id":"cancel-bad-input","type":"manual","summary":"Cancela si el input no se pudo resolver","detail":"Nodo cancel (single-line): aborta con 'No pude resolver el input' + output de resolve-input + puntero al diagnostic-bundle. Solo cuando output.type == error.","depends_on":["dump-on-cancel-bad-input"],"trigger_rule":"all_success","when":"$resolve-input.output.type == 'error'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on el dump."},{"id":"gen-hu-id","type":"script","summary":"Genera o usa HU_ID + deriva branch/paths; valida formato con regex","detail":"Script bun (runtime: bun): si type==hu_id_resume usa el override, si no construye HU-<YYYYMMDD>-<HHMMSS>-<slug> (slug NFKD-normalizado del title, slice 40). Aplica guard regex ^HU-\\d{8}-\\d{4,6}-[a-z0-9][a-z0-9-]*$; si falla emite {valid:'false', error:'invalid_hu_id', hu_id, hint} (hint distingue 'tiene espacios — pasaste args del sub-pipeline' vs 'no matchea formato'). Si pasa emite {valid:'true', hu_id, branch:'hu/'+huId, title, body, issue_url, type, refinement_path, original_path, plan_dir, results_dir}.","depends_on":["resolve-input"],"trigger_rule":"all_success","when":"","produces":"output.valid in {true,false} (STRING); si true: output.{hu_id, branch, title, body, issue_url, type, refinement_path, original_path, plan_dir, results_dir}; si false: output.{error, hu_id, hint}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Guard load-bearing (gotcha #10, run 38d8223e): sin el regex un input '<HU_ID> <plugin>' producía branch con espacio → refspec git inválido → 30 min de run muerto. output es la fuente de HU_ID/BRANCH para TODO el pipeline (referenciado por ~30 nodos). valid es string 'true'/'false', no boolean."},{"id":"dump-on-cancel-bad-hu-id","type":"bash","summary":"Dump forense antes de cancelar por HU_ID inválido","detail":"Invoca dump-pipeline-diagnostics.sh con CANCEL_NODE=cancel-bad-hu-id, PHASE=gen-hu-id, HU_ID=invalid. Corre cuando gen-hu-id.output.valid == 'false'.","depends_on":["gen-hu-id"],"trigger_rule":"all_success","when":"$gen-hu-id.output.valid == 'false'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"valid es string 'true'/'false' (no boolean) — el script lo emite como string deliberadamente para el == ."},{"id":"cancel-bad-hu-id","type":"manual","summary":"Cancela si el HU_ID es inválido","detail":"Nodo cancel (single-line): aborta con el hu_id recibido + el hint de gen-hu-id + puntero al diagnostic-bundle. Corte en segundos (gotcha #10 fix). Solo cuando valid=='false'.","depends_on":["dump-on-cancel-bad-hu-id"],"trigger_rule":"all_success","when":"$gen-hu-id.output.valid == 'false'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). Corta ANTES de tocar git (setup-branch tiene when valid=='true')."},{"id":"setup-branch","type":"bash","summary":"Crea o resumea la branch hu/<HU_ID> en detached HEAD + persiste hu-original.md","detail":"git fetch origin --prune. Si la branch existe en origin: warn anti-concurrencia si último commit <300s, luego git checkout --detach origin/$BRANCH → emite RESUMED. Si no existe: git push origin origin/main:refs/heads/$BRANCH, fetch, checkout --detach → emite FRESH. Persiste hu-original.md (del ORIGINAL_PATH si resume, si no construido desde title+body). set -e. Solo si valid=='true'.","depends_on":["gen-hu-id"],"trigger_rule":"all_success","when":"$gen-hu-id.output.valid == 'true'","produces":"output: RESUMED | FRESH; crea branch en origin; hu-original.md en artifacts","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"DETACHED HEAD load-bearing (gotcha #9 + run 710e0eb6): evita 'branch already checked out elsewhere'. TODOS los pushes posteriores usan HEAD:$BRANCH. La guard when valid=='true' evita crear hu/null."},{"id":"detect-resume-state","type":"bash","summary":"Detecta si refinement/plan ya existen commiteados (smart-resume)","detail":"Chequea existencia de refinements/<HU_ID>-tech.md y plans/<HU_ID>/plugin-manifest.yaml. Emite JSON {already_refined, already_planned} (strings 'true'/'false' vía jq --arg).","depends_on":["setup-branch"],"trigger_rule":"all_success","when":"","produces":"output.already_refined in {true,false}; output.already_planned in {true,false}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Habilita smart-resume: already_refined controla el when de refinar-auto; already_planned alimenta gate-can-plan. Strings, no booleans."},{"id":"project-set-refining","type":"bash","summary":"Marca el card del Project en Refining (fail-soft)","detail":"Si no hay CFG o no hay issue_url emite skipped. Lee project_number/owner/id/status_field_id del config, busca el option id de 'Refining' con awk -F: + gsub (keys con em-dash), encuentra el item por content.url y hace gh project item-edit. Emite 'set Refining ok' o warn. trigger_rule all_done (no aborta el pipeline si falla).","depends_on":["setup-branch"],"trigger_rule":"all_done","when":"","produces":"output: skipped | warn ... | set Refining ok","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Fail-soft por trigger_rule all_done. awk -F: + gsub deliberado: keys como 'Done — PR ready' con em-dash rompen el default whitespace FS (bug 2026-05-26)."},{"id":"load-refinement-if-resume","type":"bash","summary":"Copia el refinement existente a artifacts si resume","detail":"Si refinements/<HU_ID>-tech.md existe lo copia a $ARTIFACTS_DIR/hu-refinada.md y emite RESUMED_REFINEMENT, si no NO_RESUME.","depends_on":["detect-resume-state"],"trigger_rule":"all_success","when":"","produces":"output: RESUMED_REFINEMENT | NO_RESUME; copia hu-refinada.md","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Pre-puebla el artefacto para que validate-refinement lo vea aunque refinar-auto sea skipeado (resume)."},{"id":"refinar-auto","type":"skills","summary":"Refina técnicamente la HU (loop, skill tech-refiner)","detail":"Loop max_iterations 2 (1 try + 1 retry), until REFINER_OK, skill hubara-tech-refiner-archon. Lee hu-original.md + project-context.md + spinal-files.yaml + secciones del architecture-guide; produce hu-refinada.md con 14 secciones + §0 plugin classification. gate_message permite feedback via $LOOP_USER_INPUT. Skipea si already_refined=='true'. idle_timeout 600000, trigger_rule all_done.","depends_on":["load-refinement-if-resume","project-set-refining"],"trigger_rule":"all_done","when":"$detect-resume-state.output.already_refined != 'true'","produces":"promise REFINER_OK; escribe $ARTIFACTS_DIR/hu-refinada.md","invokes":"hubara-tech-refiner-archon","loop":"max_iterations:2, until:REFINER_OK","is_gate":false,"is_cancel":false,"notes":"trigger_rule all_done permite correr aunque project-set-refining 'falle' (fail-soft). El when usa != 'true' por lo que cualquier valor distinto de 'true' corre el refiner."},{"id":"validate-refinement","type":"bash","summary":"Valida estructura del refinement (secciones + §0 + protected files)","detail":"Chequea que hu-refinada.md exista, >1000 chars, §0 plugin classification + mode. Si mode=no_refinement_needed|blocked → PASS_NO_WORK. Verifica 14 secciones canónicas (§15 opcional). Detecta protected files como files-a-modificar en §3 (heurística awk §3..§4) → FAIL_REFINEMENT_TOUCHES_PROTECTED. Emite PASS o FAIL_*. trigger_rule all_done.","depends_on":["refinar-auto","load-refinement-if-resume"],"trigger_rule":"all_done","when":"","produces":"output: PASS | PASS_NO_WORK | FAIL_NOT_EXISTS | FAIL_TOO_SHORT_* | FAIL_NO_PLUGIN_CLASSIFICATION | FAIL_NO_MODE | FAIL_MISSING_SECTIONS:* | FAIL_REFINEMENT_TOUCHES_PROTECTED","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"trigger_rule all_done — corre aunque refinar-auto sea skipeado (resume) o agote iteraciones. Alimenta gate-can-plan (VR) y commit-refinement."},{"id":"dump-on-cancel-bad-refinement","type":"bash","summary":"Dump forense antes de cancelar por refinement inválido","detail":"Re-deriva HU_ID/ISSUE_URL del output de gen-hu-id (con fallback a unknown), invoca dump-pipeline-diagnostics.sh con PHASE=refiner. Corre cuando validate-refinement no es PASS ni PASS_NO_WORK.","depends_on":["validate-refinement"],"trigger_rule":"all_success","when":"$validate-refinement.output != 'PASS' && $validate-refinement.output != 'PASS_NO_WORK'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Condición compuesta con && — el parser de Archon la soporta (sin paréntesis)."},{"id":"cancel-bad-refinement","type":"manual","summary":"Cancela si la validación del refinement falla","detail":"Nodo cancel (multi-line): aborta con el FAIL code + branch + opciones de recovery (editar manual + commit + re-lanzar para smart-resume; o ADR si toca protected). Solo cuando validate-refinement no es PASS/PASS_NO_WORK.","depends_on":["dump-on-cancel-bad-refinement"],"trigger_rule":"all_success","when":"$validate-refinement.output != 'PASS' && $validate-refinement.output != 'PASS_NO_WORK'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on el dump."},{"id":"commit-refinement","type":"bash","summary":"Commitea + pushea el refinement; stdout SINGLE-LINE para downstream ==","detail":"cp hu-refinada.md y hu-original.md a refinements/<HU_ID>-{tech,original}.md, git add (a stderr), si hay staged commit + git push origin HEAD:$BRANCH (TODO el output git a stderr), emite 'committed' o 'no_changes'. set -e. Solo cuando validate-refinement PASS o PASS_NO_WORK.","depends_on":["validate-refinement"],"trigger_rule":"all_success","when":"$validate-refinement.output == 'PASS' || $validate-refinement.output == 'PASS_NO_WORK'","produces":"output: committed | no_changes; pushea a hu/<HU_ID>","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"STDOUT LIMPIO load-bearing (gotcha #8, run cadd3d61): git commit/push escupen multi-línea → si quedan en stdout, el == de post-refinement-comment falla → skip silencioso. Por eso todo va a stderr. HEAD:$BRANCH por el detached HEAD."},{"id":"post-refinement-comment-to-issue","type":"bash","summary":"Postea comentario al issue con resumen del refinement (fail-soft)","detail":"Si no hay issue_url o no existe el refinement emite skipped. Calcula stats (líneas original vs refinement, secciones, mode, spec deltas §16), construye comment markdown con links a la branch, gh issue comment --body-file. Emite comment_posted | comment_skipped | skipped. trigger_rule all_done.","depends_on":["commit-refinement"],"trigger_rule":"all_done","when":"$commit-refinement.output == 'committed' || $commit-refinement.output == 'no_changes'","produces":"output: comment_posted | comment_skipped | skipped no_issue_url | skipped no_refinement_file; comentario en el issue","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Fail-soft (trigger_rule all_done + gh failure no rompe). TERMINAL (→END): ningún nodo depende de él. El body del issue NO se modifica (spec inmutable); solo UN comentario por iteración."},{"id":"project-set-refined","type":"bash","summary":"Marca el Project en Refined (fail-soft)","detail":"Mismo patrón que project-set-refining pero con option 'Refined'. trigger_rule all_done. Sin when — siempre intenta (el body skipea si no hay CFG/issue_url).","depends_on":["commit-refinement"],"trigger_rule":"all_done","when":"","produces":"output: skipped | set Refined ok","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Fail-soft. Alimenta planificar-auto como dep (para serializar el Project status)."},{"id":"load-plan-if-resume","type":"bash","summary":"Copia el plugin-manifest existente a artifacts si resume","detail":"Si plans/<HU_ID>/plugin-manifest.yaml existe lo copia a $ARTIFACTS_DIR/plugin-manifest.yaml y emite RESUMED_PLAN, si no NO_RESUME.","depends_on":["commit-refinement"],"trigger_rule":"all_success","when":"","produces":"output: RESUMED_PLAN | NO_RESUME; copia plugin-manifest.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Pre-puebla el artefacto para que validate-plan lo vea aunque planificar-auto sea skipeado (resume)."},{"id":"gate-can-plan","type":"bash","summary":"Decide CAN_PLAN vs SKIP según resume + verdict del refinement","detail":"Lee AP=already_planned y VR=validate-refinement.output (SIN dquotes en RHS por shellQuote). Si AP==true → SKIP_PLAN_ALREADY_PLANNED; elif VR==PASS|PASS_NO_WORK → CAN_PLAN; else SKIP_PLAN_BAD_REFINEMENT. Encapsula A&&(B||C) que el parser no expresa con paréntesis. trigger_rule all_done. Trace a stderr.","depends_on":["detect-resume-state","validate-refinement","load-plan-if-resume"],"trigger_rule":"all_done","when":"","produces":"output: CAN_PLAN | SKIP_PLAN_ALREADY_PLANNED | SKIP_PLAN_BAD_REFINEMENT","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"shellQuote gotcha (gotcha #7, run cadd3d61): AP=$detect-resume-state.output.already_planned SIN dquotes — Archon wrappea el valor en single quotes y bash quote-removal las limpia; con dquotes quedaría el literal 'false' con comillas → CAN_PLAN nunca dispararía. Bug introducido commit 135f646."},{"id":"planificar-auto","type":"skills","summary":"Planifica plugin-level (loop, skill plugin-planner)","detail":"Loop max_iterations 2, until PLANNER_OK, skill hubara-plugin-planner-archon. Lee hu-refinada.md (§0) + plugin-manifest previo + spinal-files; produce plugin-manifest.yaml con mode + plugins[] (id, layers, action, depends_on, estimated_tasks) + plugin_batches[] + shared_files_intents[] si requires_merger. gate_message permite feedback. Solo si gate-can-plan==CAN_PLAN. idle_timeout 600000, trigger_rule all_done.","depends_on":["load-plan-if-resume","project-set-refined","gate-can-plan"],"trigger_rule":"all_done","when":"$gate-can-plan.output == 'CAN_PLAN'","produces":"promise PLANNER_OK; escribe $ARTIFACTS_DIR/plugin-manifest.yaml","invokes":"hubara-plugin-planner-archon","loop":"max_iterations:2, until:PLANNER_OK","is_gate":false,"is_cancel":false,"notes":"El when sobre gate-can-plan (no sobre el compound original) es el fix del shellQuote bug — si gate-can-plan emite mal, el planner se skipea silenciosamente → validate-plan FAIL_NOT_EXISTS (mensaje engañoso)."},{"id":"validate-plan","type":"bash","summary":"Valida el plugin-manifest con python3 (mode, cap, batches)","detail":"Chequea existencia + >200 chars. python3 carga el YAML: mode ∈ {single_plugin,multi_plugin,no_work,blocked}; no_work→PASS_NO_WORK, blocked→PASS_BLOCKED; plugins[] no vacío y ≤ MAX_PLUGINS_PER_HU (default 8); coherencia mode↔len(plugins); plugin_batches cubren exactamente los plugin ids declarados. Emite PASS_<MODE> o FAIL_*. trigger_rule all_done.","depends_on":["planificar-auto","load-plan-if-resume"],"trigger_rule":"all_done","when":"","produces":"output: PASS_SINGLE_PLUGIN | PASS_MULTI_PLUGIN | PASS_NO_WORK | PASS_BLOCKED | FAIL_NOT_EXISTS | FAIL_TOO_SHORT | FAIL_INVALID_YAML | FAIL_NOT_DICT | FAIL_BAD_MODE | FAIL_NO_PLUGINS | FAIL_TOO_MANY_PLUGINS | FAIL_SINGLE_BUT_MULTIPLE | FAIL_MULTI_BUT_SINGLE | FAIL_NO_BATCHES | FAIL_BATCH_PLUGINS_MISMATCH","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"trigger_rule all_done — corre aunque planificar-auto sea skipeado (resume). MAX_PLUGINS_PER_HU overrideable por env. Su output es la verdad detallada; gate-plan-verdict lo colapsa para los when downstream."},{"id":"gate-plan-verdict","type":"bash","summary":"Colapsa validate-plan a PASS|FAIL (el parser no soporta regex)","detail":"OUT=$validate-plan.output (SIN dquotes). case PASS* → PASS, else FAIL. Trace a stderr. Reemplaza el regex =~ /^PASS/ que el parser no soporta. trigger_rule default all_success.","depends_on":["validate-plan"],"trigger_rule":"all_success","when":"","produces":"output: PASS | FAIL","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"shellQuote gotcha igual que gate-can-plan — SIN dquotes en el RHS. El detalle del FAIL queda en validate-plan.output. NO tiene trigger_rule line → default all_success."},{"id":"dump-on-cancel-bad-plan","type":"bash","summary":"Dump forense antes de cancelar por plan inválido","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=planner, CANCEL_REASON=validate-plan.output. Corre cuando gate-plan-verdict==FAIL.","depends_on":["validate-plan","gate-plan-verdict"],"trigger_rule":"all_success","when":"$gate-plan-verdict.output == 'FAIL'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Depende de validate-plan Y gate-plan-verdict — necesita el detalle de validate-plan para el CANCEL_REASON (2 deps)."},{"id":"cancel-bad-plan","type":"manual","summary":"Cancela si la validación del plan falla","detail":"Nodo cancel (multi-line): aborta con el FAIL code + gate trace (gate-can-plan, validate-refinement, validate-plan, gate-plan-verdict) + decodificación detallada de cada FAIL code + recovery. Solo cuando gate-plan-verdict==FAIL.","depends_on":["dump-on-cancel-bad-plan","gate-plan-verdict"],"trigger_rule":"all_success","when":"$gate-plan-verdict.output == 'FAIL'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on DOS nodos (dump + gate-plan-verdict). El gate trace en el mensaje es deliberado para diagnosticar el skip silencioso del planner."},{"id":"commit-plan","type":"bash","summary":"Commitea + pushea el plugin-manifest; stdout SINGLE-LINE","detail":"cp plugin-manifest.yaml a plans/<HU_ID>/, cuenta N plugins con python3, git add (a stderr), si staged commit + push origin HEAD:$BRANCH (git output a stderr), emite committed_<N> o no_changes. set -e. Solo cuando gate-plan-verdict==PASS.","depends_on":["validate-plan","gate-plan-verdict"],"trigger_rule":"all_success","when":"$gate-plan-verdict.output == 'PASS'","produces":"output: committed_<N> | no_changes; pushea a hu/<HU_ID>","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"STDOUT LIMPIO (mismo patrón que commit-refinement). Es el ancla de FASE 3: classify-mode, load-plan? (no), prewarm-uv-venv, project-set-planned todos dependen de commit-plan. depends_on DOS nodos (validate-plan + gate-plan-verdict)."},{"id":"project-set-planned","type":"bash","summary":"Marca el Project en Planned (fail-soft)","detail":"Mismo patrón project-set con option 'Planned'. trigger_rule all_done.","depends_on":["commit-plan"],"trigger_rule":"all_done","when":"","produces":"output: skipped | set Planned ok","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"project-set-implementing depende de ESTE (no de commit-plan) para serializar Planned→Implementing y evitar race en el GitHub API (2026-05-26)."},{"id":"project-set-implementing","type":"bash","summary":"Marca el Project en Implementing (fail-soft)","detail":"Mismo patrón project-set con option 'Implementing'. depende de project-set-planned (no commit-plan) para evitar race. trigger_rule all_done.","depends_on":["project-set-planned"],"trigger_rule":"all_done","when":"","produces":"output: skipped | set Implementing ok","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Dep serializada deliberada (2026-05-26): ambos project-set compartían commit-plan → llegaban al GitHub API en orden no determinístico. prewarm-uv-venv depende de este."},{"id":"classify-mode","type":"bash","summary":"Re-lee el manifest y emite JSON con mode/plugins/batches/requires_merger","detail":"python3 (heredoc EOF) carga plans/<HU_ID>/plugin-manifest.yaml y emite JSON {mode, plugins:[ids], batches:[{id:batch_id,plugins}], requires_merger (bool de totals.requires_merger), single_plugin_id (plugins[0] si single)}. Es el bifurcador rama A vs rama B.","depends_on":["commit-plan"],"trigger_rule":"all_success","when":"","produces":"output.mode in {single_plugin, multi_plugin, no_work, blocked, unknown}; output.{plugins, batches, requires_merger, single_plugin_id}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"GATE de bifurcación clave. requires_merger es JSON boolean → Archon lo convierte a string 'true'/'false' para los when (quotes obligatorias). Referenciado por rama-A, rama-B-*, build-pr-body, print-final-summary."},{"id":"prewarm-uv-venv","type":"bash","summary":"Pre-calienta el venv uv (uv sync) + bootstrap .env antes del fan-out","detail":"Si existe hubara_agency: si no hay .env copia .env.example→.env (Settings pydantic validan env vars al import time, worktree sin .env), luego cd hubara_agency && uv sync. Emite ok. timeout 600000, trigger_rule all_done.","depends_on":["commit-plan","project-set-implementing"],"trigger_rule":"all_done","when":"","produces":"output: ok; .env bootstrapped + venv sincronizado","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Bootstrap .env load-bearing (run 940be3b9): sin .env, pytest -m architecture que importa workers crashea con 'MedusaSettings: base_url Field required'. trigger_rule all_done — uv sync warnings no bloquean. depends_on DOS nodos (commit-plan + project-set-implementing)."},{"id":"rama-A-single-plugin-inline","type":"bash","summary":"RAMA A: invoca el sub-pipeline inline para el único plugin + valida result","detail":"Si mode!=single_plugin → skipped. Invoca env -u CLAUDECODE archon workflow run hu-hubara-plugin-pipeline '<HU_ID> <PLUGIN_ID>' (espera, log a file). Si rc!=0 → FAIL_SUBPIPELINE. git fetch + merge --ff-only origin/$BRANCH. Valida plugin-<id>-result.yaml: status ∈ {passed, passed_with_warnings} continuable, else FAIL_PLUGIN_NOT_PASSED. Emite PASS o FAIL_*. idle_timeout/timeout 3600000.","depends_on":["classify-mode","prewarm-uv-venv"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'single_plugin'","produces":"output: PASS single-plugin completed: ... | skipped | FAIL_SUBPIPELINE | FAIL_NO_PLUGIN_RESULT | FAIL_PLUGIN_NOT_PASSED","invokes":"hu-hubara-plugin-pipeline (sub-workflow, inline)","loop":"","is_gate":true,"is_cancel":false,"notes":"Fan-out a NIVEL B inline. env -u CLAUDECODE evita nested archon-in-claude-code hang. Su PASS/FAIL lo recoge check-pipeline-error (trigger all_done). depends_on classify-mode + prewarm-uv-venv."},{"id":"rama-B-print-fan-out-commands","type":"bash","summary":"RAMA B: imprime los comandos de fan-out para el primer batch","detail":"Si mode!=multi_plugin → skipped. Toma el primer batch (batches[0].plugins via jq join), imprime un banner con un 'archon workflow run hu-hubara-plugin-pipeline \"<HU_ID> <p>\"' por plugin, escribe la lista a $ARTIFACTS_DIR/.current-batch-plugins. Emite OK.","depends_on":["classify-mode","prewarm-uv-venv"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'multi_plugin'","produces":"output: OK | skipped; escribe .current-batch-plugins","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Solo soporta 1 batch (comentario L1347-1351): HUs con >1 batch topológico requieren re-correr el orquestador por batch. El fan-out es MANUAL (operador abre N terminales). depends_on classify-mode + prewarm-uv-venv."},{"id":"rama-B-wait-fan-out-done","type":"manual","summary":"RAMA B: approval node — espera 'ready' cuando los sub-pipelines terminen","detail":"Nodo approval (approval: con message): espera que el operador responda 'ready' cuando todos los sub-pipelines del batch hayan terminado (o 'abort'). El mensaje lista los plugins esperados (de $rama-B-print-fan-out-commands.output) y lo que el pipeline hará después (fetch+ff-merge, validar results, merger, avanzar). Solo si mode==multi_plugin.","depends_on":["rama-B-print-fan-out-commands"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'multi_plugin'","produces":"approval: ready | abort","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"ÚNICO nodo HUMAN-GATE de tipo approval del pipeline. interactive global es false pero approval nodes esperan input igual. (Los cancel nodes también son 'manual' pero abortan, no esperan.)"},{"id":"rama-B-merge-batch","type":"bash","summary":"RAMA B: ff-mergea origin/$BRANCH + valida que todos los plugins del batch passed","detail":"Si mode!=multi_plugin → skipped. git fetch + merge --ff-only origin/$BRANCH (si diverge → FAIL_FF_MERGE). Por cada plugin en .current-batch-plugins valida plugin-<p>-result.yaml: missing → MISSING; passed OK; passed_with_warnings → WARNED; otro → FAILED. Si MISSING||FAILED → FAIL_BATCH_INCOMPLETE, else BATCH_OK. Detalles a stderr.","depends_on":["rama-B-wait-fan-out-done"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'multi_plugin'","produces":"output: BATCH_OK | skipped | FAIL_FF_MERGE | FAIL_BATCH_INCOMPLETE missing=N failed=M","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"passed_with_warnings es continuable (SKILL.md L1164). Su output enrutina rama-B-invoke-merger, rama-B-commit-merger, dump-on-cancel-multi-plugin-failure y check-pipeline-error (fan-in)."},{"id":"rama-B-invoke-merger-if-shared","type":"command","summary":"RAMA B: corre el merger de wiring_intents si requires_merger","detail":"Invoca command hubara-merge-intents para consolidar los shared_files_intents de los plugins del batch en los spinal files. Solo cuando mode==multi_plugin && requires_merger=='true' && rama-B-merge-batch==BATCH_OK.","depends_on":["rama-B-merge-batch"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'multi_plugin' && $classify-mode.output.requires_merger == 'true' && $rama-B-merge-batch.output == 'BATCH_OK'","produces":"modifica spinal files (hubara_agency/src, frontend_dashboard/src)","invokes":"hubara-merge-intents","loop":"","is_gate":false,"is_cancel":false,"notes":"requires_merger es JSON boolean convertido a string por Archon — quotes obligatorias en el when. Condición TRIPLE con &&."},{"id":"rama-B-commit-merger","type":"bash","summary":"RAMA B: commitea + pushea los spinal files que el merger modificó","detail":"Si mode!=multi_plugin o requires_merger!=true → skipped. git add hubara_agency/src frontend_dashboard/src, si staged commit + push origin HEAD:$BRANCH con pull --rebase + retry. Emite merger_committed o no_changes. trigger_rule all_done.","depends_on":["rama-B-invoke-merger-if-shared"],"trigger_rule":"all_done","when":"$classify-mode.output.mode == 'multi_plugin' && $rama-B-merge-batch.output == 'BATCH_OK'","produces":"output: merger_committed | no_changes (merger skipped...) | skipped; pushea a hu/<HU_ID>","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"trigger_rule all_done — corre aunque el merger command 'falle'. HEAD:$BRANCH por detached HEAD. pull --rebase + retry maneja concurrencia. NOTA: el when NO incluye requires_merger=='true' (el body lo re-chequea con jq y emite skipped) — solo mode==multi_plugin && BATCH_OK."},{"id":"dump-on-cancel-multi-plugin-failure","type":"bash","summary":"Dump forense antes de cancelar por batch multi-plugin incompleto","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=multi-plugin-batch, CANCEL_REASON=rama-B-merge-batch.output. Corre cuando mode==multi_plugin && rama-B-merge-batch != BATCH_OK.","depends_on":["rama-B-merge-batch"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'multi_plugin' && $rama-B-merge-batch.output != 'BATCH_OK'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Condición compuesta && con != — soportada por el parser."},{"id":"cancel-on-multi-plugin-failure","type":"manual","summary":"Cancela si el batch multi-plugin quedó incompleto","detail":"Nodo cancel (multi-line): aborta con el output de rama-B-merge-batch + puntero a results dir + recovery (lanzar sub-pipelines faltantes, re-lanzar con smart-resume). Solo cuando mode==multi_plugin && rama-B-merge-batch != BATCH_OK.","depends_on":["dump-on-cancel-multi-plugin-failure"],"trigger_rule":"all_success","when":"$classify-mode.output.mode == 'multi_plugin' && $rama-B-merge-batch.output != 'BATCH_OK'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on el dump."},{"id":"check-pipeline-error","type":"bash","summary":"Busca pipeline-error.yaml de cualquier sub-pipeline (fan-in de ramas A/B)","detail":"find $ARTIFACTS_DIR -name pipeline-error.yaml. Si hay alguno los cat a stderr y emite HAS_ERROR, si no OK. depends_on de rama-A + rama-B-merge-batch + rama-B-commit-merger con trigger_rule all_done — punto de fan-in donde convergen ambas ramas de implementación.","depends_on":["rama-A-single-plugin-inline","rama-B-merge-batch","rama-B-commit-merger"],"trigger_rule":"all_done","when":"","produces":"output: OK | HAS_ERROR","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"FAN-IN crítico de rama A y rama B (trigger_rule all_done — corre cuando todas las deps terminaron sin importar status; ramas no-tomadas emiten skipped). Su output gobierna final-validation vs cancel-on-implement-error. depends_on 3 nodos."},{"id":"dump-on-cancel-implement-error","type":"bash","summary":"Dump forense antes de cancelar por error de implementación","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=implementer, CANCEL_REASON='HAS_ERROR (see pipeline-error.yaml)'. Corre cuando check-pipeline-error==HAS_ERROR.","depends_on":["check-pipeline-error"],"trigger_rule":"all_success","when":"$check-pipeline-error.output == 'HAS_ERROR'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":""},{"id":"cancel-on-implement-error","type":"manual","summary":"Cancela si algún sub-pipeline dejó pipeline-error.yaml","detail":"Nodo cancel (multi-line): aborta indicando FASE 3 incompleta + branch pushada con progreso + puntero a pipeline-error.yaml + recovery (fix código, commit+push, re-lanzar smart-resume). Solo cuando HAS_ERROR.","depends_on":["dump-on-cancel-implement-error"],"trigger_rule":"all_success","when":"$check-pipeline-error.output == 'HAS_ERROR'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on el dump."},{"id":"final-validation","type":"bash","summary":"Gates duros consolidados de ambos stacks con scope-detection + watchdogs","detail":"set -o pipefail. Progress log incremental (final-validation-progress.log). Scope-detection vs merge-base con origin/main: TOUCHED_BACKEND/FRONTEND/UI/MANIFEST (FORCE_ALL_GATES=1 fuerza todos). Backend (si touched): render-compose drift, pytest -m architecture (watchdog 120s), lint-imports, pytest tests/plugins (120s), pytest tests/functional -m functional (180s) — todos con I/O a file para evitar el orphan-pipe hang + pkill defensivo. Frontend (si touched): plugins:sync (genera registry), npm test, test:arch, tsc -b, build. Playwright (si touched UI): arranca uvicorn en puerto random, espera ready 60s, npx playwright test. Emite gates-summary.md + PASS o FAIL_*. timeout 1200000.","depends_on":["check-pipeline-error"],"trigger_rule":"all_success","when":"$check-pipeline-error.output == 'OK'","produces":"output: PASS | FAIL_RENDER_COMPOSE_DRIFT | FAIL_ARCH | FAIL_LINT_IMPORTS | FAIL_PREMORTEM | FAIL_FUNCTIONAL | FAIL_PLUGINS_SYNC | FAIL_NPM_TEST | FAIL_NPM_ARCH | FAIL_TSC | FAIL_BUILD | FAIL_UVICORN_NOT_READY | FAIL_PLAYWRIGHT; escribe gates-summary.md, functional-evidence.log, playwright-final.log","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"El gate más pesado. Watchdog file-redirect load-bearing (runs 0538c537/710e0eb6): orphans de pytest/uv/temporal-test-server heredan el stdout pipe → Archon espera EOF hasta global timeout aunque bash ya salió. Full pytest -q removido (run ee8436ff). Su output PASS gobierna premortem, create-pr (when re-chequea PASS)."},{"id":"dump-on-cancel-final-validation-fail","type":"bash","summary":"Dump forense antes de cancelar por final-validation fail","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=final-validation, CANCEL_REASON=final-validation.output. Corre cuando final-validation != PASS.","depends_on":["final-validation"],"trigger_rule":"all_success","when":"$final-validation.output != 'PASS'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Captura el final-validation-progress.log que dice qué gate se atascó si hubo timeout."},{"id":"cancel-on-final-validation-fail","type":"manual","summary":"Cancela si la validación final pre-PR falla","detail":"Nodo cancel (multi-line): aborta con el FAIL code + branch + lista de comandos para diagnose manual (pytest, lint-imports, npm test, tsc, build) + recovery. Solo cuando final-validation != PASS.","depends_on":["dump-on-cancel-final-validation-fail"],"trigger_rule":"all_success","when":"$final-validation.output != 'PASS'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). depends_on el dump."},{"id":"premortem-self-review","type":"command","summary":"GATE 1: premortem imagina failure_modes en producción","detail":"Invoca command hubara-premortem (T6/T7 divergent forward-looking). Imagina 30-50 modos de fallo en 10 categorías (edge cases, race conditions, network, i18n, observability, performance, UI states) y emite failure_modes[] en premortem.yaml. NO aplica fixes. Solo cuando final-validation==PASS.","depends_on":["final-validation"],"trigger_rule":"all_success","when":"$final-validation.output == 'PASS'","produces":"escribe $ARTIFACTS_DIR/premortem.yaml con failure_modes[]","invokes":"hubara-premortem","loop":"","is_gate":false,"is_cancel":false,"notes":"Primer gate de los 3 pre-PR. command (no skills+loop) — patrón Archon-canonical para one-shot. Fundamenta failure modes en Requirements de capability specs."},{"id":"check-premortem-clean","type":"bash","summary":"Cuenta failure_modes del premortem → PM_CLEAN/PM_HAS_ISSUES/PM_MISSING","detail":"Si premortem.yaml no existe → PM_MISSING. Cuenta '^  - id: PM-' (failure_modes), severity critical/high (a stderr). Si FAILURE_COUNT==0 → PM_CLEAN, else PM_HAS_ISSUES.","depends_on":["premortem-self-review"],"trigger_rule":"all_success","when":"","produces":"output: PM_CLEAN | PM_HAS_ISSUES | PM_MISSING","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"PM_MISSING era un agujero → ahora cancel-on-premortem-missing lo captura. Gobierna 3 ramas: loop (PM_HAS_ISSUES), evaluate-pre-pr (PM_CLEAN), cancel-missing (PM_MISSING)."},{"id":"cancel-on-premortem-missing","type":"manual","summary":"Cancela si premortem no produjo premortem.yaml","detail":"Nodo cancel (single-line): aborta indicando que hubara-premortem falló/no escribió output y la HU quedó sin auditar. Solo cuando PM_MISSING.","depends_on":["check-premortem-clean"],"trigger_rule":"all_success","when":"$check-premortem-clean.output == 'PM_MISSING'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). Hole-fix proactivo (clase gotcha #8). NO tiene dump gemelo — cancel DIRECTO desde check-premortem-clean (uno de los 2 cancels sin dump)."},{"id":"loop-implementer-resolves-premortem","type":"skills","summary":"GATE 1 loop: el implementer resuelve los failure_modes del premortem","detail":"Loop max_iterations 2, until PREMORTEM_LOOP_DONE, skill hubara-implementer-archon en modo PROCESAR PREMORTEM. Lee premortem.yaml + task-result.yaml, por cada failure_mode decide según fix_complexity (trivial→aplica, medium→si no cambia signature, complex→defer), re-corre §7 verification, actualiza task-result.yaml con premortem_processing. Emite promise interna PREMORTEM_RESOLVED/BLOCKED/BROKEN. Solo si PM_HAS_ISSUES. idle_timeout 1800000.","depends_on":["check-premortem-clean"],"trigger_rule":"all_success","when":"$check-premortem-clean.output == 'PM_HAS_ISSUES'","produces":"promise PREMORTEM_LOOP_DONE; actualiza task-result.yaml con premortem_processing (fixes_applied/deferred)","invokes":"hubara-implementer-archon","loop":"max_iterations:2, until:PREMORTEM_LOOP_DONE","is_gate":false,"is_cancel":false,"notes":"El implementer aplica con su contexto completo — NINGÚN gate aplica fixes (diseño anti-deuda-silenciosa). Hard rules: no cambiar signatures, no borrar tests, no type:ignore, no tocar protected, cada fix con test."},{"id":"check-premortem-resolved","type":"bash","summary":"Verifica el resultado del loop premortem (busca task-result en 2 paths)","detail":"Re-deriva HU_ID. Candidatos = $ARTIFACTS_DIR/task-result.yaml + find en feature-results/<plugin>/task-result.yaml. Por cada uno grepea PREMORTEM_BROKEN/BLOCKED/RESOLVED (o premortem_status: / ^premortem_processing:). Precedencia broken>blocked>resolved. Emite PR_RESULT_MISSING/BROKEN/BLOCKED/RESOLVED/UNKNOWN. Stdout single-line.","depends_on":["loop-implementer-resolves-premortem"],"trigger_rule":"all_success","when":"","produces":"output: PR_RESOLVED | PR_BLOCKED | PR_BROKEN | PR_RESULT_MISSING | PR_UNKNOWN","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Path-fix load-bearing (gotcha #11, run 894495e1): el implementer multi-plugin actualiza task-result en feature-results/, NO en ARTIFACTS_DIR → buscar en AMBOS evita PR_RESULT_MISSING falso → cancel engañoso. Solo PR_RESOLVED continúa."},{"id":"dump-on-cancel-premortem-blocked","type":"bash","summary":"Dump forense antes de cancelar por premortem no-resoluble","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=premortem. Corre cuando check-premortem-resolved ∈ {PR_BLOCKED, PR_BROKEN, PR_RESULT_MISSING, PR_UNKNOWN}.","depends_on":["check-premortem-resolved"],"trigger_rule":"all_success","when":"$check-premortem-resolved.output == 'PR_BLOCKED' || $check-premortem-resolved.output == 'PR_BROKEN' || $check-premortem-resolved.output == 'PR_RESULT_MISSING' || $check-premortem-resolved.output == 'PR_UNKNOWN'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Hole-fix (run b9b95fc5): PR_RESULT_MISSING/PR_UNKNOWN incluidos — antes caían en agujero (evaluate skipeaba con when PR_RESOLVED, cancel solo con BLOCKED/BROKEN) → todo downstream skip silencioso. Condición OR de 4 términos."},{"id":"cancel-on-premortem-blocked","type":"manual","summary":"Cancela si el premortem chain no produjo estado resoluble","detail":"Nodo cancel (multi-line): aborta con decodificación de cada estado (BLOCKED/BROKEN=fixes complex deferred o tests rotos; RESULT_MISSING=loop no escribió, HU posiblemente incompleta; UNKNOWN=status no reconocible) + recovery (re-correr sub-pipeline si HU incompleta, ADR si complex). Solo cuando estado ∈ {PR_BLOCKED,PR_BROKEN,PR_RESULT_MISSING,PR_UNKNOWN}.","depends_on":["dump-on-cancel-premortem-blocked"],"trigger_rule":"all_success","when":"$check-premortem-resolved.output == 'PR_BLOCKED' || $check-premortem-resolved.output == 'PR_BROKEN' || $check-premortem-resolved.output == 'PR_RESULT_MISSING' || $check-premortem-resolved.output == 'PR_UNKNOWN'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). El ÚNICO estado que continúa es PR_RESOLVED; cualquier otro cancela visiblemente (política anti-merge-silencioso). depends_on el dump. Condición OR de 4 términos."},{"id":"evaluate-pre-pr","type":"command","summary":"GATE 2: evaluador escéptico puntúa contra rúbrica calibrada","detail":"Invoca command hubara-evaluate (T7 convergent rubric). Puntúa 5 criterios (architectural, test coverage real, visual, code quality, scope) contra la rúbrica YAML y emite evaluation.yaml con verdict pass/warn/block_merge + weighted_average + recommended_actions[]. Corre tras el premortem chain (PM_CLEAN path O PR_RESOLVED path).","depends_on":["check-premortem-clean","check-premortem-resolved"],"trigger_rule":"all_success","when":"$check-premortem-clean.output == 'PM_CLEAN' || $check-premortem-resolved.output == 'PR_RESOLVED'","produces":"escribe $ARTIFACTS_DIR/evaluation.yaml con verdict + weighted_average + recommended_actions[]","invokes":"hubara-evaluate","loop":"","is_gate":false,"is_cancel":false,"notes":"Segundo gate. FAN-IN de las 2 rutas del premortem (PM_CLEAN sin issues O PR_RESOLVED tras loop). depends_on DOS nodos; el OR del when resuelve cuál ruta se tomó. trigger_rule default all_success."},{"id":"gate-evaluator-verdict","type":"bash","summary":"Colapsa el verdict del evaluator a EVAL_PASS/WARN/BLOCK/MISSING/UNKNOWN","detail":"Si evaluation.yaml no existe → EVAL_MISSING. Grepea ^verdict: + ^weighted_average:. case pass→EVAL_PASS, warn→EVAL_WARN, block_merge→EVAL_BLOCK, *→EVAL_UNKNOWN. Stdout single-line, trace a stderr.","depends_on":["evaluate-pre-pr"],"trigger_rule":"all_success","when":"","produces":"output: EVAL_PASS | EVAL_WARN | EVAL_BLOCK | EVAL_MISSING | EVAL_UNKNOWN","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"STDOUT single-line (gotcha #8). PASS|WARN continúan a los 5 reviewers; BLOCK/MISSING/UNKNOWN → cancel. Gobierna las 5 ramas review-* + synthesize-review via when."},{"id":"dump-on-cancel-eval-block","type":"bash","summary":"Dump forense antes de cancelar por evaluator block","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=evaluator. Corre cuando gate-evaluator-verdict ∈ {EVAL_BLOCK, EVAL_MISSING, EVAL_UNKNOWN}.","depends_on":["gate-evaluator-verdict"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_BLOCK' || $gate-evaluator-verdict.output == 'EVAL_MISSING' || $gate-evaluator-verdict.output == 'EVAL_UNKNOWN'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"EVAL_MISSING/UNKNOWN incluidos para no caer en agujero (mismo patrón que premortem). Condición OR de 3 términos."},{"id":"cancel-on-eval-block","type":"manual","summary":"Cancela si el evaluator bloqueó el merge","detail":"Nodo cancel (multi-line): aborta con verdict block_merge (criterio bajo hard_threshold o weighted_average <5.5) + punteros a evaluation.yaml findings/recommended_actions + opciones (fix manual, override ARCH_EVAL_OVERRIDE=1, devolver al implementer, abort). Solo cuando estado ∈ {EVAL_BLOCK,EVAL_MISSING,EVAL_UNKNOWN}.","depends_on":["dump-on-cancel-eval-block"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_BLOCK' || $gate-evaluator-verdict.output == 'EVAL_MISSING' || $gate-evaluator-verdict.output == 'EVAL_UNKNOWN'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). Menciona override ARCH_EVAL_OVERRIDE=1 pero el nodo NO lo lee (override = desactivar nodo manualmente). depends_on el dump. Condición OR de 3 términos."},{"id":"review-deha","type":"command","summary":"GATE 3 specialist: review DEHA (R-rules) — paralelo","detail":"Invoca command hubara-reviewer-deha. Audita su área vertical (DEHA R-rules) y escribe review-findings-deha.yaml. Corre EN PARALELO con los otros 4 reviewers (todos dependen solo de gate-evaluator-verdict). Solo si EVAL_PASS|EVAL_WARN.","depends_on":["gate-evaluator-verdict"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'","produces":"escribe review-findings-deha.yaml","invokes":"hubara-reviewer-deha","loop":"","is_gate":false,"is_cancel":false,"notes":"1 de 5 specialists paralelos (paralelismo DAG-level de Archon). Cross-ref con premortem.yaml para no duplicar."},{"id":"review-fsd","type":"command","summary":"GATE 3 specialist: review FSD (anti-patterns) — paralelo","detail":"Invoca command hubara-reviewer-fsd. Audita FSD anti-patterns y escribe review-findings-fsd.yaml. Paralelo. Solo si EVAL_PASS|EVAL_WARN.","depends_on":["gate-evaluator-verdict"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'","produces":"escribe review-findings-fsd.yaml","invokes":"hubara-reviewer-fsd","loop":"","is_gate":false,"is_cancel":false,"notes":"2 de 5 specialists paralelos."},{"id":"review-plugin-system","type":"command","summary":"GATE 3 specialist: review plugin-system (schema) — paralelo","detail":"Invoca command hubara-reviewer-plugin-system. Audita el plugin schema/manifest y escribe review-findings-plugin-system.yaml. Paralelo. Solo si EVAL_PASS|EVAL_WARN.","depends_on":["gate-evaluator-verdict"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'","produces":"escribe review-findings-plugin-system.yaml","invokes":"hubara-reviewer-plugin-system","loop":"","is_gate":false,"is_cancel":false,"notes":"3 de 5 specialists paralelos."},{"id":"review-test-coverage","type":"command","summary":"GATE 3 specialist: review test-coverage (behavior tests) — paralelo","detail":"Invoca command hubara-reviewer-test-coverage. Audita cobertura de tests de comportamiento real y escribe review-findings-test-coverage.yaml. Paralelo. Solo si EVAL_PASS|EVAL_WARN.","depends_on":["gate-evaluator-verdict"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'","produces":"escribe review-findings-test-coverage.yaml","invokes":"hubara-reviewer-test-coverage","loop":"","is_gate":false,"is_cancel":false,"notes":"4 de 5 specialists paralelos."},{"id":"review-security","type":"command","summary":"GATE 3 specialist: review security — paralelo","detail":"Invoca command hubara-reviewer-security. Audita vulnerabilidades de seguridad y escribe review-findings-security.yaml. Paralelo. Solo si EVAL_PASS|EVAL_WARN.","depends_on":["gate-evaluator-verdict"],"trigger_rule":"all_success","when":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'","produces":"escribe review-findings-security.yaml","invokes":"hubara-reviewer-security","loop":"","is_gate":false,"is_cancel":false,"notes":"5 de 5 specialists paralelos. security critical NUNCA se mergea sin fix (política en cancel-on-review-blocked)."},{"id":"synthesize-review","type":"command","summary":"GATE 3: consolida los 5 reviews en code-review-findings.yaml","detail":"Invoca command hubara-synthesize-review. Consolida los 5 review-findings-<area>.yaml en code-review-findings.yaml + cross-ref con premortem.yaml (also_in_premortem). trigger_rule one_success — corre aunque algún specialist haya fallado. Solo si EVAL_PASS|EVAL_WARN.","depends_on":["review-deha","review-fsd","review-plugin-system","review-test-coverage","review-security"],"trigger_rule":"one_success","when":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'","produces":"escribe $ARTIFACTS_DIR/code-review-findings.yaml (findings con also_in_premortem cross-ref)","invokes":"hubara-synthesize-review","loop":"","is_gate":false,"is_cancel":false,"notes":"FAN-IN de los 5 specialists. trigger_rule one_success deliberado — robusto a fallo de un specialist. ÚNICO nodo con one_success en TODO el pipeline. depends_on 5 nodos."},{"id":"check-review-clean","type":"bash","summary":"Cuenta findings del code-review → REVIEW_CLEAN/HAS_BLOCKERS/HAS_MINOR/CR_MISSING","detail":"Si code-review-findings.yaml no existe → CR_MISSING. Cuenta '^  - id: CR-' (total), severity critical/high. Si TOTAL==0 → REVIEW_CLEAN; elif critical||high>0 → REVIEW_HAS_BLOCKERS; else REVIEW_HAS_MINOR.","depends_on":["synthesize-review"],"trigger_rule":"all_success","when":"","produces":"output: REVIEW_CLEAN | REVIEW_HAS_BLOCKERS | REVIEW_HAS_MINOR | CR_MISSING","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"CR_MISSING era agujero → ahora cancel-on-review-missing. Gobierna 3 ramas: loop (HAS_BLOCKERS||HAS_MINOR), build-pr-body (REVIEW_CLEAN), cancel-missing. HAS_MINOR también va al loop (no directo a PR)."},{"id":"cancel-on-review-missing","type":"manual","summary":"Cancela si synthesize-review no produjo code-review-findings.yaml","detail":"Nodo cancel (single-line): aborta indicando que synthesize-review falló/no escribió output y la HU quedó sin revisar. Solo cuando CR_MISSING.","depends_on":["check-review-clean"],"trigger_rule":"all_success","when":"$check-review-clean.output == 'CR_MISSING'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). Hole-fix proactivo. NO tiene dump gemelo — cancel DIRECTO desde check-review-clean (el otro de los 2 cancels sin dump)."},{"id":"loop-implementer-resolves-review","type":"skills","summary":"GATE 3 loop: el implementer resuelve los code-review findings","detail":"Loop max_iterations 2, until CODE_REVIEW_LOOP_DONE, skill hubara-implementer-archon en modo PROCESAR CODE REVIEW. Lee code-review-findings.yaml + premortem.yaml (skip findings con also_in_premortem), aplica según severity×fix_complexity (critical/high trivial/medium→aplica, complex→defer, low→defer), re-corre §7, actualiza task-result.yaml.code_review_processing. Emite promise CODE_REVIEW_RESOLVED/BLOCKED/BROKEN. Solo si REVIEW_HAS_BLOCKERS||REVIEW_HAS_MINOR. idle_timeout 1800000.","depends_on":["check-review-clean"],"trigger_rule":"all_success","when":"$check-review-clean.output == 'REVIEW_HAS_BLOCKERS' || $check-review-clean.output == 'REVIEW_HAS_MINOR'","produces":"promise CODE_REVIEW_LOOP_DONE; actualiza task-result.yaml con code_review_processing","invokes":"hubara-implementer-archon","loop":"max_iterations:2, until:CODE_REVIEW_LOOP_DONE","is_gate":false,"is_cancel":false,"notes":"Mismas hard rules que el loop premortem. Skip de findings ya cubiertos por premortem (also_in_premortem) para no re-trabajar."},{"id":"check-review-resolved","type":"bash","summary":"Verifica el resultado del loop review (busca task-result en 2 paths)","detail":"Mismo path-fix que check-premortem-resolved: candidatos = ARTIFACTS_DIR/task-result.yaml + feature-results/. Grepea CODE_REVIEW_BROKEN/BLOCKED/RESOLVED (o code_review_status: / ^code_review_processing:). Precedencia broken>blocked>resolved. Emite RV_RESULT_MISSING/REVIEW_BROKEN/BLOCKED/RESOLVED/UNKNOWN. Stdout single-line.","depends_on":["loop-implementer-resolves-review"],"trigger_rule":"all_success","when":"","produces":"output: REVIEW_RESOLVED | REVIEW_BLOCKED | REVIEW_BROKEN | RV_RESULT_MISSING | REVIEW_UNKNOWN","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Path-fix load-bearing (gotcha #11). Solo REVIEW_RESOLVED continúa al PR. El estado missing acá se llama RV_RESULT_MISSING (no REVIEW_RESULT_MISSING — asimetría deliberada vs premortem que usa PR_RESULT_MISSING)."},{"id":"dump-on-cancel-review-blocked","type":"bash","summary":"Dump forense antes de cancelar por review no-resoluble","detail":"Invoca dump-pipeline-diagnostics.sh con PHASE=code-review. Corre cuando check-review-resolved ∈ {REVIEW_BLOCKED, REVIEW_BROKEN, RV_RESULT_MISSING, REVIEW_UNKNOWN}.","depends_on":["check-review-resolved"],"trigger_rule":"all_success","when":"$check-review-resolved.output == 'REVIEW_BLOCKED' || $check-review-resolved.output == 'REVIEW_BROKEN' || $check-review-resolved.output == 'RV_RESULT_MISSING' || $check-review-resolved.output == 'REVIEW_UNKNOWN'","produces":"diagnostic-bundle.yaml","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"RV_RESULT_MISSING/REVIEW_UNKNOWN incluidos para no caer en agujero (silent skip de build-pr-body + create-pr). Condición OR de 4 términos."},{"id":"cancel-on-review-blocked","type":"manual","summary":"Cancela si el code-review chain no produjo estado resoluble","detail":"Nodo cancel (multi-line): aborta con punteros a code-review-findings.yaml + fixes_deferred + recovery por tipo de finding (security critical=ADR+hotfix, DEHA/FSD=ADR+nueva HU, test-coverage=agregar test) + nota sobre RESULT_MISSING/UNKNOWN. Solo cuando estado ∈ {REVIEW_BLOCKED,REVIEW_BROKEN,RV_RESULT_MISSING,REVIEW_UNKNOWN}.","depends_on":["dump-on-cancel-review-blocked"],"trigger_rule":"all_success","when":"$check-review-resolved.output == 'REVIEW_BLOCKED' || $check-review-resolved.output == 'REVIEW_BROKEN' || $check-review-resolved.output == 'RV_RESULT_MISSING' || $check-review-resolved.output == 'REVIEW_UNKNOWN'","produces":"run cancelado","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Terminal (→END). Solo REVIEW_RESOLVED continúa (política anti-merge-silencioso). depends_on el dump. Condición OR de 4 términos."},{"id":"build-pr-body","type":"script","summary":"Ensambla pr-body.md desde refinement/plan/results/evidencia","detail":"Script bun (runtime: bun): lee refinement original (summary 20 líneas), parsea plugin-manifest (lista de plugins), per-plugin results (status + feature_tasks), functional-evidence.log + playwright logs (strip ANSI), gates-summary.md (o fallback hardcodeado), metadata (HU id, branch, mode de classify-mode.output, Closes <issue>). Construye pr-body.md línea por línea. Solo si REVIEW_CLEAN o REVIEW_RESOLVED.","depends_on":["check-review-clean","check-review-resolved"],"trigger_rule":"all_success","when":"$check-review-clean.output == 'REVIEW_CLEAN' || $check-review-resolved.output == 'REVIEW_RESOLVED'","produces":"escribe $ARTIFACTS_DIR/pr-body.md","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"FAN-IN de las 2 rutas del review (REVIEW_CLEAN sin findings O REVIEW_RESOLVED tras loop). stripAnsi load-bearing (run 5d99ee6b — operador vio basura ANSI ^[[2m en el PR). depends_on DOS nodos."},{"id":"create-pr","type":"bash","summary":"Commitea visual-evidence + crea el PR contra main (reusa si existe)","detail":"Si hay screenshots en visual-evidence/ los commitea+pushea (retry 3x con pull --rebase) y embebe como imágenes raw.githubusercontent por SHA (non-fatal). gh pr create --title '<HU_ID>: <title>' --body-file pr-body.md --base main --head $BRANCH. Si rc!=0 busca PR existente (reusa) o emite FAIL_PR_CREATE. Parsea la URL → FAIL_PR_URL_NOT_PARSEABLE si no. Escribe .pr-url. Solo si final-validation==PASS.","depends_on":["build-pr-body"],"trigger_rule":"all_success","when":"$final-validation.output == 'PASS'","produces":"output: <PR_URL> | FAIL_PR_CREATE | FAIL_PR_URL_NOT_PARSEABLE; escribe .pr-url","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"when re-chequea $final-validation.output=='PASS' (NO el review state) — redundante pero defensivo. Ancla de FASE 5.5/6: project-set-done, archive-hu, trigger-review dependen de create-pr. No cancela en FAIL_PR_CREATE (exit 0); archive-hu/print-final-summary corren con all_done."},{"id":"project-set-done","type":"bash","summary":"Marca el Project en Done — PR ready (fail-soft)","detail":"Mismo patrón project-set, busca option 'Done — PR ready' (con em-dash, awk -F:) o fallback 'Done'. trigger_rule all_done.","depends_on":["create-pr"],"trigger_rule":"all_done","when":"","produces":"output: skipped | warn no Done option | set Done ok","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Fail-soft. awk -F: + gsub para el em-dash en la key (2026-05-26). print-final-summary depende de este."},{"id":"archive-hu","type":"command","summary":"FASE 5.5: snapshot de artefactos + merge de spec-deltas","detail":"Invoca command hubara-archive-hu (Fase 12 OpenSpec). Snapshot de TODOS los artefactos a hubara_agency/.hubara/archive/<fecha>-<HU_ID>/ + merge de spec-deltas/<capability> a specs/. Emite archive-result.yaml. Solo si create-pr no fue FAIL_PR_URL_NOT_PARSEABLE.","depends_on":["create-pr"],"trigger_rule":"all_success","when":"$create-pr.output != 'FAIL_PR_URL_NOT_PARSEABLE'","produces":"escribe archive-result.yaml; snapshot en archive/ + specs/ mergeadas","invokes":"hubara-archive-hu","loop":"","is_gate":false,"is_cancel":false,"notes":"El when excluye SOLO FAIL_PR_URL_NOT_PARSEABLE — corre incluso si create-pr emitió FAIL_PR_CREATE (que igual exit 0). El command NO commitea (lo hace commit-archive)."},{"id":"commit-archive","type":"bash","summary":"Commitea + pushea el archive + specs mergeadas","detail":"cd al PROJECT_DIR (fallback CLAUDE_PROJECT_DIR → git toplevel → pwd). Si no hay archive-result.yaml → ARCHIVE_SKIPPED. Si status=skipped → ARCHIVE_SKIPPED=<reason>. Stagea ARCHIVE_DIR + specs/, si nada staged → NOTHING_TO_COMMIT, si no commit con PR link (via file) + push si hay upstream. Emite ARCHIVE_COMMITTED=<hash> o ARCHIVE_COMMIT_FAILED (no aborta). trigger_rule all_done.","depends_on":["archive-hu"],"trigger_rule":"all_done","when":"","produces":"output: ARCHIVE_COMMITTED=<hash> | ARCHIVE_SKIPPED | ARCHIVE_SKIPPED=<reason> | NOTHING_TO_COMMIT | ARCHIVE_RESULT_MALFORMED | ARCHIVE_COMMIT_FAILED | ARCHIVE_CD_FAILED","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"No aborta el pipeline si falla (archive es side effect benigno, exit 0). print-final-summary depende de este. Fixes F-OS-3/5/17 anotados en comentarios."},{"id":"trigger-review","type":"bash","summary":"FASE 6: dispara el workflow review-pr-hubara en background","detail":"Lee .pr-url. Si no hay → skipped. Si .archon/workflows/review-pr-hubara.yaml no existe → skip (PR18 lo agrega). Si no, nohup archon workflow run review-pr-hubara <PR_URL> en background (disown, env -u CLAUDECODE). Emite review_triggered_log=<file> | skipped | review_trigger_failed_run_manually. trigger_rule all_done.","depends_on":["create-pr"],"trigger_rule":"all_done","when":"","produces":"output: review_triggered_log=<file> | skipped (no PR URL) | review-pr-hubara workflow no existe... | review_trigger_failed_run_manually","invokes":"review-pr-hubara (background, condicional a existencia del file)","loop":"","is_gate":false,"is_cancel":false,"notes":"Background fire-and-forget (nohup + disown). El workflow review-pr-hubara puede no existir aún (gated por existencia del file). print-final-summary depende de este."},{"id":"print-final-summary","type":"bash","summary":"Imprime el resumen final del pipeline (HU/mode/branch/PR/review)","detail":"Lee HU_ID/branch/mode/.pr-url/trigger-review.output e imprime un banner final con HU id, mode, branch, PR URL, review status, próximos pasos. trigger_rule all_done, timeout 5000.","depends_on":["trigger-review","project-set-done","commit-archive"],"trigger_rule":"all_done","when":"","produces":"output: banner de resumen final","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Nodo TERMINAL de la rama feliz (→END). FAN-IN final de trigger-review + project-set-done + commit-archive (trigger_rule all_done — corre aunque alguno haya fallado). timeout 5000ms. depends_on 3 nodos."}],"edges":[{"from":"START","to":"check-prereqs","kind":"sequence","condition":""},{"from":"check-prereqs","to":"dump-on-cancel-bad-prereqs","kind":"gate","condition":"$check-prereqs.output != 'OK'"},{"from":"dump-on-cancel-bad-prereqs","to":"cancel-bad-prereqs","kind":"cancel","condition":"$check-prereqs.output != 'OK'"},{"from":"check-prereqs","to":"stage-shared-files","kind":"gate","condition":"$check-prereqs.output == 'OK'"},{"from":"stage-shared-files","to":"resolve-input","kind":"sequence","condition":""},{"from":"resolve-input","to":"dump-on-cancel-bad-input","kind":"gate","condition":"$resolve-input.output.type == 'error'"},{"from":"dump-on-cancel-bad-input","to":"cancel-bad-input","kind":"cancel","condition":"$resolve-input.output.type == 'error'"},{"from":"resolve-input","to":"gen-hu-id","kind":"sequence","condition":""},{"from":"gen-hu-id","to":"dump-on-cancel-bad-hu-id","kind":"gate","condition":"$gen-hu-id.output.valid == 'false'"},{"from":"dump-on-cancel-bad-hu-id","to":"cancel-bad-hu-id","kind":"cancel","condition":"$gen-hu-id.output.valid == 'false'"},{"from":"gen-hu-id","to":"setup-branch","kind":"gate","condition":"$gen-hu-id.output.valid == 'true'"},{"from":"setup-branch","to":"detect-resume-state","kind":"sequence","condition":""},{"from":"setup-branch","to":"project-set-refining","kind":"sequence","condition":""},{"from":"detect-resume-state","to":"load-refinement-if-resume","kind":"sequence","condition":""},{"from":"load-refinement-if-resume","to":"refinar-auto","kind":"gate","condition":"$detect-resume-state.output.already_refined != 'true'"},{"from":"project-set-refining","to":"refinar-auto","kind":"gate","condition":"$detect-resume-state.output.already_refined != 'true'"},{"from":"refinar-auto","to":"validate-refinement","kind":"sequence","condition":""},{"from":"load-refinement-if-resume","to":"validate-refinement","kind":"sequence","condition":""},{"from":"validate-refinement","to":"dump-on-cancel-bad-refinement","kind":"gate","condition":"$validate-refinement.output != 'PASS' && $validate-refinement.output != 'PASS_NO_WORK'"},{"from":"dump-on-cancel-bad-refinement","to":"cancel-bad-refinement","kind":"cancel","condition":"$validate-refinement.output != 'PASS' && $validate-refinement.output != 'PASS_NO_WORK'"},{"from":"validate-refinement","to":"commit-refinement","kind":"gate","condition":"$validate-refinement.output == 'PASS' || $validate-refinement.output == 'PASS_NO_WORK'"},{"from":"commit-refinement","to":"post-refinement-comment-to-issue","kind":"gate","condition":"$commit-refinement.output == 'committed' || $commit-refinement.output == 'no_changes'"},{"from":"commit-refinement","to":"project-set-refined","kind":"sequence","condition":""},{"from":"commit-refinement","to":"load-plan-if-resume","kind":"sequence","condition":""},{"from":"detect-resume-state","to":"gate-can-plan","kind":"sequence","condition":""},{"from":"validate-refinement","to":"gate-can-plan","kind":"sequence","condition":""},{"from":"load-plan-if-resume","to":"gate-can-plan","kind":"sequence","condition":""},{"from":"load-plan-if-resume","to":"planificar-auto","kind":"gate","condition":"$gate-can-plan.output == 'CAN_PLAN'"},{"from":"project-set-refined","to":"planificar-auto","kind":"gate","condition":"$gate-can-plan.output == 'CAN_PLAN'"},{"from":"gate-can-plan","to":"planificar-auto","kind":"gate","condition":"$gate-can-plan.output == 'CAN_PLAN'"},{"from":"planificar-auto","to":"validate-plan","kind":"sequence","condition":""},{"from":"load-plan-if-resume","to":"validate-plan","kind":"sequence","condition":""},{"from":"validate-plan","to":"gate-plan-verdict","kind":"sequence","condition":""},{"from":"validate-plan","to":"dump-on-cancel-bad-plan","kind":"gate","condition":"$gate-plan-verdict.output == 'FAIL'"},{"from":"gate-plan-verdict","to":"dump-on-cancel-bad-plan","kind":"gate","condition":"$gate-plan-verdict.output == 'FAIL'"},{"from":"dump-on-cancel-bad-plan","to":"cancel-bad-plan","kind":"cancel","condition":"$gate-plan-verdict.output == 'FAIL'"},{"from":"gate-plan-verdict","to":"cancel-bad-plan","kind":"cancel","condition":"$gate-plan-verdict.output == 'FAIL'"},{"from":"validate-plan","to":"commit-plan","kind":"gate","condition":"$gate-plan-verdict.output == 'PASS'"},{"from":"gate-plan-verdict","to":"commit-plan","kind":"gate","condition":"$gate-plan-verdict.output == 'PASS'"},{"from":"commit-plan","to":"project-set-planned","kind":"sequence","condition":""},{"from":"project-set-planned","to":"project-set-implementing","kind":"sequence","condition":""},{"from":"commit-plan","to":"classify-mode","kind":"sequence","condition":""},{"from":"commit-plan","to":"prewarm-uv-venv","kind":"sequence","condition":""},{"from":"project-set-implementing","to":"prewarm-uv-venv","kind":"sequence","condition":""},{"from":"classify-mode","to":"rama-A-single-plugin-inline","kind":"fan-out","condition":"$classify-mode.output.mode == 'single_plugin'"},{"from":"prewarm-uv-venv","to":"rama-A-single-plugin-inline","kind":"fan-out","condition":"$classify-mode.output.mode == 'single_plugin'"},{"from":"classify-mode","to":"rama-B-print-fan-out-commands","kind":"fan-out","condition":"$classify-mode.output.mode == 'multi_plugin'"},{"from":"prewarm-uv-venv","to":"rama-B-print-fan-out-commands","kind":"fan-out","condition":"$classify-mode.output.mode == 'multi_plugin'"},{"from":"rama-B-print-fan-out-commands","to":"rama-B-wait-fan-out-done","kind":"gate","condition":"$classify-mode.output.mode == 'multi_plugin'"},{"from":"rama-B-wait-fan-out-done","to":"rama-B-merge-batch","kind":"gate","condition":"$classify-mode.output.mode == 'multi_plugin'"},{"from":"rama-B-merge-batch","to":"rama-B-invoke-merger-if-shared","kind":"gate","condition":"$classify-mode.output.mode == 'multi_plugin' && $classify-mode.output.requires_merger == 'true' && $rama-B-merge-batch.output == 'BATCH_OK'"},{"from":"rama-B-invoke-merger-if-shared","to":"rama-B-commit-merger","kind":"gate","condition":"$classify-mode.output.mode == 'multi_plugin' && $rama-B-merge-batch.output == 'BATCH_OK'"},{"from":"rama-B-merge-batch","to":"dump-on-cancel-multi-plugin-failure","kind":"gate","condition":"$classify-mode.output.mode == 'multi_plugin' && $rama-B-merge-batch.output != 'BATCH_OK'"},{"from":"dump-on-cancel-multi-plugin-failure","to":"cancel-on-multi-plugin-failure","kind":"cancel","condition":"$classify-mode.output.mode == 'multi_plugin' && $rama-B-merge-batch.output != 'BATCH_OK'"},{"from":"rama-A-single-plugin-inline","to":"check-pipeline-error","kind":"fan-in","condition":""},{"from":"rama-B-merge-batch","to":"check-pipeline-error","kind":"fan-in","condition":""},{"from":"rama-B-commit-merger","to":"check-pipeline-error","kind":"fan-in","condition":""},{"from":"check-pipeline-error","to":"dump-on-cancel-implement-error","kind":"gate","condition":"$check-pipeline-error.output == 'HAS_ERROR'"},{"from":"dump-on-cancel-implement-error","to":"cancel-on-implement-error","kind":"cancel","condition":"$check-pipeline-error.output == 'HAS_ERROR'"},{"from":"check-pipeline-error","to":"final-validation","kind":"gate","condition":"$check-pipeline-error.output == 'OK'"},{"from":"final-validation","to":"dump-on-cancel-final-validation-fail","kind":"gate","condition":"$final-validation.output != 'PASS'"},{"from":"dump-on-cancel-final-validation-fail","to":"cancel-on-final-validation-fail","kind":"cancel","condition":"$final-validation.output != 'PASS'"},{"from":"final-validation","to":"premortem-self-review","kind":"gate","condition":"$final-validation.output == 'PASS'"},{"from":"premortem-self-review","to":"check-premortem-clean","kind":"sequence","condition":""},{"from":"check-premortem-clean","to":"cancel-on-premortem-missing","kind":"cancel","condition":"$check-premortem-clean.output == 'PM_MISSING'"},{"from":"check-premortem-clean","to":"loop-implementer-resolves-premortem","kind":"gate","condition":"$check-premortem-clean.output == 'PM_HAS_ISSUES'"},{"from":"loop-implementer-resolves-premortem","to":"check-premortem-resolved","kind":"sequence","condition":""},{"from":"check-premortem-resolved","to":"dump-on-cancel-premortem-blocked","kind":"gate","condition":"$check-premortem-resolved.output == 'PR_BLOCKED' || $check-premortem-resolved.output == 'PR_BROKEN' || $check-premortem-resolved.output == 'PR_RESULT_MISSING' || $check-premortem-resolved.output == 'PR_UNKNOWN'"},{"from":"dump-on-cancel-premortem-blocked","to":"cancel-on-premortem-blocked","kind":"cancel","condition":"$check-premortem-resolved.output == 'PR_BLOCKED' || $check-premortem-resolved.output == 'PR_BROKEN' || $check-premortem-resolved.output == 'PR_RESULT_MISSING' || $check-premortem-resolved.output == 'PR_UNKNOWN'"},{"from":"check-premortem-clean","to":"evaluate-pre-pr","kind":"fan-in","condition":"$check-premortem-clean.output == 'PM_CLEAN' || $check-premortem-resolved.output == 'PR_RESOLVED'"},{"from":"check-premortem-resolved","to":"evaluate-pre-pr","kind":"fan-in","condition":"$check-premortem-clean.output == 'PM_CLEAN' || $check-premortem-resolved.output == 'PR_RESOLVED'"},{"from":"evaluate-pre-pr","to":"gate-evaluator-verdict","kind":"sequence","condition":""},{"from":"gate-evaluator-verdict","to":"dump-on-cancel-eval-block","kind":"gate","condition":"$gate-evaluator-verdict.output == 'EVAL_BLOCK' || $gate-evaluator-verdict.output == 'EVAL_MISSING' || $gate-evaluator-verdict.output == 'EVAL_UNKNOWN'"},{"from":"dump-on-cancel-eval-block","to":"cancel-on-eval-block","kind":"cancel","condition":"$gate-evaluator-verdict.output == 'EVAL_BLOCK' || $gate-evaluator-verdict.output == 'EVAL_MISSING' || $gate-evaluator-verdict.output == 'EVAL_UNKNOWN'"},{"from":"gate-evaluator-verdict","to":"review-deha","kind":"fan-out","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"gate-evaluator-verdict","to":"review-fsd","kind":"fan-out","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"gate-evaluator-verdict","to":"review-plugin-system","kind":"fan-out","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"gate-evaluator-verdict","to":"review-test-coverage","kind":"fan-out","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"gate-evaluator-verdict","to":"review-security","kind":"fan-out","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"review-deha","to":"synthesize-review","kind":"fan-in","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"review-fsd","to":"synthesize-review","kind":"fan-in","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"review-plugin-system","to":"synthesize-review","kind":"fan-in","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"review-test-coverage","to":"synthesize-review","kind":"fan-in","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"review-security","to":"synthesize-review","kind":"fan-in","condition":"$gate-evaluator-verdict.output == 'EVAL_PASS' || $gate-evaluator-verdict.output == 'EVAL_WARN'"},{"from":"synthesize-review","to":"check-review-clean","kind":"sequence","condition":""},{"from":"check-review-clean","to":"cancel-on-review-missing","kind":"cancel","condition":"$check-review-clean.output == 'CR_MISSING'"},{"from":"check-review-clean","to":"loop-implementer-resolves-review","kind":"gate","condition":"$check-review-clean.output == 'REVIEW_HAS_BLOCKERS' || $check-review-clean.output == 'REVIEW_HAS_MINOR'"},{"from":"loop-implementer-resolves-review","to":"check-review-resolved","kind":"sequence","condition":""},{"from":"check-review-resolved","to":"dump-on-cancel-review-blocked","kind":"gate","condition":"$check-review-resolved.output == 'REVIEW_BLOCKED' || $check-review-resolved.output == 'REVIEW_BROKEN' || $check-review-resolved.output == 'RV_RESULT_MISSING' || $check-review-resolved.output == 'REVIEW_UNKNOWN'"},{"from":"dump-on-cancel-review-blocked","to":"cancel-on-review-blocked","kind":"cancel","condition":"$check-review-resolved.output == 'REVIEW_BLOCKED' || $check-review-resolved.output == 'REVIEW_BROKEN' || $check-review-resolved.output == 'RV_RESULT_MISSING' || $check-review-resolved.output == 'REVIEW_UNKNOWN'"},{"from":"check-review-clean","to":"build-pr-body","kind":"fan-in","condition":"$check-review-clean.output == 'REVIEW_CLEAN' || $check-review-resolved.output == 'REVIEW_RESOLVED'"},{"from":"check-review-resolved","to":"build-pr-body","kind":"fan-in","condition":"$check-review-clean.output == 'REVIEW_CLEAN' || $check-review-resolved.output == 'REVIEW_RESOLVED'"},{"from":"build-pr-body","to":"create-pr","kind":"gate","condition":"$final-validation.output == 'PASS'"},{"from":"create-pr","to":"project-set-done","kind":"sequence","condition":""},{"from":"create-pr","to":"archive-hu","kind":"gate","condition":"$create-pr.output != 'FAIL_PR_URL_NOT_PARSEABLE'"},{"from":"archive-hu","to":"commit-archive","kind":"sequence","condition":""},{"from":"create-pr","to":"trigger-review","kind":"sequence","condition":""},{"from":"trigger-review","to":"print-final-summary","kind":"fan-in","condition":""},{"from":"project-set-done","to":"print-final-summary","kind":"fan-in","condition":""},{"from":"commit-archive","to":"print-final-summary","kind":"fan-in","condition":""},{"from":"cancel-bad-prereqs","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-input","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-hu-id","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-refinement","to":"END","kind":"sequence","condition":""},{"from":"post-refinement-comment-to-issue","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-plan","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-multi-plugin-failure","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-implement-error","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-final-validation-fail","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-premortem-missing","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-premortem-blocked","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-eval-block","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-review-missing","to":"END","kind":"sequence","condition":""},{"from":"cancel-on-review-blocked","to":"END","kind":"sequence","condition":""},{"from":"print-final-summary","to":"END","kind":"sequence","condition":""}],"verification_notes":["AUTHORITATIVE re-count from scratch: `grep -c '^  - id: '` = exactly 77 nodes. The first pass's node_count=77 is CORRECT. All 77 nodes read across the full 2704-line file (4 Read passes: 1-700, 700-1400, 1400-2099, 2099-2704). No node missed, none invented.","FIRST PASS NODES[] AND EDGES[]: structurally CORRECT. Independently rebuilt the entire node+edge model and it matches the first pass on every node's id, type, depends_on, when, trigger_rule, is_gate, is_cancel, and on the full edge set (1 START edge + 1 edge per dependency→node pair + 15 terminal→END edges). No substantive structural correction was needed.","DISCREPANCY 1 (first pass verification_notes self-contradiction — CORRECTED): the first pass prose said both '14 total' and '13 cancel nodes' for is_cancel. GROUND TRUTH: `grep -c '^    cancel:'` = 13 cancel nodes (is_cancel=true): cancel-bad-prereqs, cancel-bad-input, cancel-bad-hu-id, cancel-bad-refinement, cancel-bad-plan, cancel-on-multi-plugin-failure, cancel-on-implement-error, cancel-on-final-validation-fail, cancel-on-premortem-missing, cancel-on-premortem-blocked, cancel-on-eval-block, cancel-on-review-missing, cancel-on-review-blocked. The first pass's nodes[] correctly marked exactly these 13 as is_cancel=true.","DISCREPANCY 2 (first pass verification_notes self-contradiction — CORRECTED): the first pass prose said both '10 of them' and 'actually 11 dump nodes'. GROUND TRUTH: `grep -c '^  - id: dump-on-'` = 11 dump-on-* bash nodes: dump-on-cancel-bad-prereqs, -bad-input, -bad-hu-id, -bad-refinement, -bad-plan, -multi-plugin-failure, -implement-error, -final-validation-fail, -premortem-blocked, -eval-block, -review-blocked. The first pass's nodes[] correctly included all 11 as distinct bash nodes.","DISCREPANCY 3 (first pass verification_notes loop count typo — CORRECTED): the first pass loop bullet header said '3 total' then listed 4. GROUND TRUTH: `grep -c '^    loop:'` = 4 loop (skills) nodes, all max_iterations:2: refinar-auto (REFINER_OK), planificar-auto (PLANNER_OK), loop-implementer-resolves-premortem (PREMORTEM_LOOP_DONE), loop-implementer-resolves-review (CODE_REVIEW_LOOP_DONE). The first pass's nodes[] correctly modeled all 4.","AUTHORITATIVE TYPE TALLY (verified by grep): 47 bash nodes total (`grep -c '^    bash:'`) = 36 plain bash + 11 dump bash; 10 command nodes (`grep -c '^    command:'`: hubara-merge-intents, hubara-premortem, hubara-evaluate, 5 reviewers, hubara-synthesize-review, hubara-archive-hu); 4 loop/skills nodes; 2 script nodes (`grep -c '^    script:'`: gen-hu-id, build-pr-body, both runtime: bun); 1 approval node (`grep -c '^    approval:'`: rama-B-wait-fan-out-done). The 13 cancel nodes use the `cancel:` key (type=manual in this model). 47+10+4+2+1+13 = 77. Note: dump nodes are a subset of the 47 bash; cancel nodes are NOT bash (they use cancel:). Cross-check: 36 plain-bash + 11 dump-bash + 10 cmd + 4 loop + 2 script + 1 approval + 13 cancel = 77. CONFIRMED.","GATES (is_gate=true, output value routes the chain), 19 total — matches first pass exactly: check-prereqs, resolve-input, gen-hu-id, detect-resume-state, validate-refinement, gate-can-plan, validate-plan, gate-plan-verdict, classify-mode, rama-A-single-plugin-inline, rama-B-merge-batch, check-pipeline-error, final-validation, check-premortem-clean, check-premortem-resolved, gate-evaluator-verdict, check-review-clean, check-review-resolved, create-pr.","trigger_rule audit (verified by grep of '^    trigger_rule:'): one_success appears on EXACTLY ONE node — synthesize-review (confirmed). all_done appears on these nodes: project-set-refining, refinar-auto, validate-refinement, post-refinement-comment-to-issue, project-set-refined, gate-can-plan, planificar-auto, validate-plan, project-set-planned, project-set-implementing, prewarm-uv-venv, rama-B-commit-merger, check-pipeline-error, project-set-done, commit-archive, trigger-review, print-final-summary (= 17 nodes with all_done). All other nodes default to all_success (no trigger_rule line). The first pass's trigger_rule assignments were correct.","when-condition audit (verified by grep of '^    when:'): 51 nodes have a when, 26 have none (must be when:''). The 26 without when: check-prereqs, resolve-input, gen-hu-id, detect-resume-state, project-set-refining, load-refinement-if-resume, validate-refinement, project-set-refined, load-plan-if-resume, gate-can-plan, validate-plan, gate-plan-verdict, project-set-planned, project-set-implementing, classify-mode, prewarm-uv-venv, check-pipeline-error, check-premortem-clean, check-premortem-resolved, gate-evaluator-verdict, check-review-clean, check-review-resolved, project-set-done, commit-archive, trigger-review, print-final-summary. Matches first pass.","depends_on audit (verified by grep): 76 nodes have a depends_on line; check-prereqs is the ONLY node with empty depends_on (START). Multi-dep nodes verified verbatim: refinar-auto[load-refinement-if-resume,project-set-refining]; validate-refinement[refinar-auto,load-refinement-if-resume]; gate-can-plan[detect-resume-state,validate-refinement,load-plan-if-resume]; planificar-auto[load-plan-if-resume,project-set-refined,gate-can-plan]; validate-plan[planificar-auto,load-plan-if-resume]; dump-on-cancel-bad-plan[validate-plan,gate-plan-verdict]; cancel-bad-plan[dump-on-cancel-bad-plan,gate-plan-verdict]; commit-plan[validate-plan,gate-plan-verdict]; prewarm-uv-venv[commit-plan,project-set-implementing]; rama-A & rama-B-print[classify-mode,prewarm-uv-venv]; check-pipeline-error[rama-A-single-plugin-inline,rama-B-merge-batch,rama-B-commit-merger]; evaluate-pre-pr[check-premortem-clean,check-premortem-resolved]; synthesize-review[5 reviewers]; build-pr-body[check-review-clean,check-review-resolved]; print-final-summary[trigger-review,project-set-done,commit-archive]. All match first pass.","CANCEL-without-DUMP (verified): exactly 2 cancel nodes depend DIRECTLY on a check node (no dump twin): cancel-on-premortem-missing (depends_on check-premortem-clean, when PM_MISSING) and cancel-on-review-missing (depends_on check-review-clean, when CR_MISSING). The other 11 cancel nodes each depend on their dump-on-* twin. First pass noted this correctly.","TERMINAL→END edges, 15 total (verified by set-difference all_ids minus all-depended-upon ids): the 13 cancel nodes + post-refinement-comment-to-issue (fail-soft leaf, nothing depends on it) + print-final-summary (happy-path leaf). Matches first pass's END edge set exactly.","shellQuote gotcha (gate-can-plan, gate-plan-verdict): RHS assignments are intentionally WITHOUT double-quotes (AP=$node.output, lines 783-795 and 909-910). Do NOT 'fix' to AP=\"$node.output\". stdout-single-line invariant on commit-refinement/commit-plan/all gate-* and check-* nodes confirmed (diagnostics → stderr, gotcha #8).","ASYMMETRY confirmed (easy-to-confuse, both modeled correctly): premortem missing state = PR_RESULT_MISSING (check-premortem-resolved), review missing state = RV_RESULT_MISSING (check-review-resolved). Different prefixes by design.","RISK/AMBIGUITY (carried over, all real): (1) create-pr.when re-checks $final-validation.output=='PASS' NOT the review state (line 2534) — defensive redundancy. (2) build-pr-body depends on BOTH check-review-clean and check-review-resolved with an OR when; if neither emits the happy value it is skipped and create-pr (all_success on build-pr-body) is also silently skipped — but the cancel nodes (CR_MISSING / REVIEW_BLOCKED/BROKEN/RESULT_MISSING/UNKNOWN) cover those states first. (3) Only 1 multi-plugin batch supported (no rama-B-batch-2/3 nodes exist; comment L1347-1351). (4) rama-B-commit-merger.when (line 1312) is ONLY 'mode==multi_plugin && rama-B-merge-batch==BATCH_OK' — does NOT gate on requires_merger (the body re-checks via jq and emits skipped); the first pass's edge condition matched the YAML verbatim. CONCLUSION: first pass model was accurate; corrections are confined to the prose count tallies."]},"hu-hubara-plugin-pipeline":{"pipeline_id":"hu-hubara-plugin-pipeline","title":"Hubara Plugin Sub-Pipeline (per-plugin feature plan + sequential implement, detached-HEAD worktree)","purpose":"Sub-pipeline that runs inside ONE plugin's worktree. Given \"<HU_ID> <plugin_id>\", it does a detached-HEAD checkout of origin/hu/<HU_ID>, stages that plugin's slice of the orchestrator-committed plugin-manifest (plugin-work.yaml), runs the feature-planner skill (interactive, max 2 iter) to produce a feature-level DAG (feature-plan-manifest.yaml + tareas/F<NN>-*.md), validates it deterministically (shape, F-id format, task-file existence, cap of 12), commits+pushes the plan to HEAD:<BRANCH>, then loops the implementer skill SEQUENTIALLY (max 50 iter, fresh_context) where a large until_bash is the real controller: re-runs pytest + pytest -m architecture + lint-imports + render-compose drift + npm test/test:arch/tsc/build + Playwright (with a backgrounded FastAPI on a random free port) per task, commits+pushes each passed task to HEAD:<BRANCH>, handles det-retries (max 2) and transient retries (max 1), and on permanent failure writes pipeline-error.yaml and exits 0. Finally it computes the plugin's authoritative status with a strict completeness check (planned-vs-produced result files; passed_with_warnings counts as passing) and writes+commits+pushes plugin-<id>-result.yaml for the orchestrator to aggregate, then prints a summary box.","trigger":"archon workflow run hu-hubara-plugin-pipeline \"<HU_ID> <plugin_id>\" — EXACTLY 2 whitespace-separated tokens. $ARGUMENTS is split with awk '{print $1}' (HU_ID) and awk '{print $2}' (PLUGIN_ID). Example: \"HU-20260517-143025-add-image-tool chats\". Optional env override: MAX_FEATURES_PER_PLUGIN (default 12). worktree.enabled: true (runs in a dedicated worktree). interactive: false at file level, but the feature-planner loop carries a gate_message. Invoked (a) inline by hu-hubara-pipeline when mode=single_plugin, (b) manually fanned-out N times by the operator when mode=multi_plugin (one terminal per plugin), (c) standalone for plugin debug. It does NOT itself detect single vs multi — each invocation handles exactly one plugin_id.","node_count":17,"nodes":[{"id":"parse-input","type":"bash","summary":"Parse '<HU_ID> <plugin_id>' from $ARGUMENTS into a structured JSON object with all derived paths (always exit 0).","detail":"Splits $ARGUMENTS with awk into HU_ID ($1) and PLUGIN_ID ($2). Emits a jq error object {type:error,...} (exit 0) if either token is empty, if HU_ID fails grep -qE '^HU-[0-9]{8}-[0-9]{4,6}-', or if PLUGIN_ID fails grep -qE '^[a-z][a-z0-9_]*$'. On success emits a jq object {type:ok, hu_id, plugin_id, branch:('hu/'+h), plan_dir, plugin_manifest, feature_plan_dir, results_dir, plugin_result_file, feature_results_dir} — all computed paths under hubara_agency/.hubara/. ALWAYS exits 0 so routing is by output.type, not exit code.","depends_on":[],"trigger_rule":"all_success","when":"","produces":"output.type in {ok,error}; on ok: hu_id, plugin_id, branch, plan_dir, plugin_manifest, feature_plan_dir, results_dir, plugin_result_file, feature_results_dir; on error: error (+ optional got)","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Root node (no depends_on; START attaches here). The whole $parse-input.output JSON is the run-wide source of truth — nearly every downstream bash node re-derives HU_ID/PLUGIN_ID/BRANCH via `echo $parse-input.output | jq -r '.field'`. HU_ID regex (^HU-[0-9]{8}-[0-9]{4,6}-) is LOOSER than the main pipeline's (only a trailing hyphen, no slug-char class). exit 0 on all paths is deliberate."},{"id":"cancel-bad-input","type":"manual","summary":"Cancel node: aborts the run when parse-input reported an input error.","detail":"Single-line cancel: message echoes $parse-input.output and prints the usage string (archon workflow run hu-hubara-plugin-pipeline '<HU_ID> <plugin_id>'). Fires only when parse-input emitted type:error (bad/missing tokens or failed regex). Terminates the workflow.","depends_on":["parse-input"],"trigger_rule":"all_success","when":"$parse-input.output.type == 'error'","produces":"cancel (terminates run)","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Default trigger_rule all_success: only fires if parse-input SUCCEEDED (exit 0) AND its when matches. parse-input always exits 0, so this is reliable. Mutually exclusive branch vs check-prereqs (which has when type=='ok')."},{"id":"check-prereqs","type":"bash","summary":"Verify runtime prerequisites: gh auth, uv/npm/jq/python3 on PATH, required spinal/context files, the 3 hubara skills, and origin reachability.","detail":"`set -e` then a sequence of guarded checks, each `|| { echo FAIL_*; exit 0; }`: gh auth status (FAIL_GH_AUTH; stdout redirected >&2 2>&1), command -v uv/npm/jq/python3 (FAIL_NO_UV/NPM/JQ/PYTHON3), test -f hubara_agency/.hubara/spinal-files.yaml (FAIL_MISSING_SPINAL_FILES) and project-context.md (FAIL_MISSING_PROJECT_CONTEXT), test -f the SKILL.md of hubara-feature-planner-archon / hubara-implementer-archon / hubara-architecture-guide (FAIL_MISSING_FEATURE_PLANNER / FAIL_MISSING_IMPLEMENTER / FAIL_MISSING_GUIDE), git ls-remote origin HEAD (FAIL_NO_ORIGIN). Echoes 'OK' only if all pass. timeout 60000.","depends_on":["parse-input"],"trigger_rule":"all_success","when":"$parse-input.output.type == 'ok'","produces":"output == 'OK' | FAIL_GH_AUTH | FAIL_NO_UV | FAIL_NO_NPM | FAIL_NO_JQ | FAIL_NO_PYTHON3 | FAIL_MISSING_SPINAL_FILES | FAIL_MISSING_PROJECT_CONTEXT | FAIL_MISSING_FEATURE_PLANNER | FAIL_MISSING_IMPLEMENTER | FAIL_MISSING_GUIDE | FAIL_NO_ORIGIN","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Despite `set -e`, every check uses `|| { echo FAIL...; exit 0; }`, so it always exits 0 with a single-line status — routing is by output value. This sub-pipeline does NOT check gh project scope (no FAIL_GH_NO_PROJECT_SCOPE token here, unlike the main pipeline). gh auth status output redirected so stdout stays clean."},{"id":"cancel-bad-prereqs","type":"manual","summary":"Cancel node: aborts when check-prereqs did not return exactly 'OK'.","detail":"Single-line cancel: message echoes $check-prereqs.output and points to the same diagnostics as hu-hubara-pipeline.cancel-bad-prereqs. Fires whenever the prereq check produced any FAIL_* token instead of OK.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output != 'OK'","produces":"cancel (terminates run)","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Default trigger_rule all_success. check-prereqs always exits 0, so all_success holds and the when decides. Silent-hole risk low because check-prereqs cannot crash (always exit 0)."},{"id":"checkout-branch","type":"bash","summary":"Detached-HEAD checkout of origin/hu/<HU_ID>, then verify the orchestrator-persisted plugin-manifest and refinement files exist.","detail":"`set -e`. Reads BRANCH and HU_ID from $parse-input.output via jq. Runs `git fetch origin --prune`. If `git ls-remote --exit-code --heads origin $BRANCH` fails → echoes FAIL_BRANCH_NOT_FOUND and exit 1. Else `git checkout --detach origin/$BRANCH` (stderr tee'd via tail -3 >&2). Then checks PLAN=hubara_agency/.hubara/plans/$HU_ID/plugin-manifest.yaml exists (else FAIL_NO_PLUGIN_MANIFEST, exit 1) and REFINEMENT=hubara_agency/.hubara/refinements/$HU_ID-tech.md exists (else FAIL_NO_REFINEMENT, exit 1). Echoes BRANCH_READY on full success.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output == 'OK'","produces":"output == 'BRANCH_READY' (exit 0) | FAIL_BRANCH_NOT_FOUND (exit 1) | FAIL_NO_PLUGIN_MANIFEST (exit 1) | FAIL_NO_REFINEMENT (exit 1)","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Architectural fix (run 52fa70e0, 2026-05-27): detached HEAD (`git checkout --detach origin/$BRANCH`) instead of `git checkout $BRANCH`, so N concurrent sub-pipeline worktrees can target the same hu/<HU_ID> without git's 'already checked out' refusal. Consequently EVERY push in the file uses `git push origin HEAD:$BRANCH`. Unlike parse-input/check-prereqs, the FAIL_* paths here exit 1 (NON-ZERO) — so the cancel that follows MUST use trigger_rule all_done. The BRANCH_READY value is reused as a cross-node guard by validate-feature-plan and cancel-bad-feature-plan."},{"id":"cancel-bad-checkout","type":"manual","summary":"Cancel node: aborts when checkout-branch did not reach BRANCH_READY (with worktree-cleanup guidance).","detail":"Multi-line cancel: echoes $checkout-branch.output, explains the most frequent cause (branch already checked out in another worktree — suggests `git worktree list` and `git worktree remove --force <path>`), plus decode of FAIL_BRANCH_NOT_FOUND (run hu-hubara-pipeline first), FAIL_NO_PLUGIN_MANIFEST (orchestrator didn't finish FASE 2), FAIL_NO_REFINEMENT (didn't finish FASE 1).","depends_on":["checkout-branch"],"trigger_rule":"all_done","when":"$checkout-branch.output != 'BRANCH_READY'","produces":"cancel (terminates run)","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"CRITICAL (YAML comment L181-185): trigger_rule MUST be all_done (NOT default all_success) because checkout-branch FAIL paths exit 1. With default all_success this cancel would be SKIPPED on a non-zero exit and the real error would be masked by the misleading cancel-bad-feature-plan FAIL_NOT_EXISTS (cascading-skip). Lesson from run 52fa70e0."},{"id":"stage-plugin-context","type":"bash","summary":"Copy shared context files into $ARTIFACTS_DIR and extract this plugin's slice (plugin-work.yaml) from the plugin-manifest.","detail":"`set -e`. Reads HU_ID, PLUGIN_ID via jq. Copies spinal-files.yaml, project-context.md, plans/$HU_ID/plugin-manifest.yaml (→ plugin-manifest.yaml), and refinements/$HU_ID-tech.md (→ hu-refinada.md) into $ARTIFACTS_DIR. Then runs a python3 heredoc that loads plugin-manifest.yaml, finds plugins[] entry with id==PLUGIN_ID; if not found raises SystemExit('FAIL_PLUGIN_NOT_IN_MANIFEST...') listing declared ids. If found, builds a `work` dict (hu_id, plugin_id, work_summary, layers, template default 'A', affects_layers_detail, affects_shared_files, estimated_tasks default 1, risk default 'low', depends_on_plugins) and yaml.safe_dumps it to $ARTIFACTS_DIR/plugin-work.yaml, then prints OK.","depends_on":["checkout-branch"],"trigger_rule":"all_success","when":"","produces":"output: OK (stdout) | crashes (set -e + python SystemExit) on FAIL_PLUGIN_NOT_IN_MANIFEST; side-effect: $ARTIFACTS_DIR/{spinal-files.yaml, project-context.md, plugin-manifest.yaml, hu-refinada.md, plugin-work.yaml}","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"No `when` clause + default trigger_rule all_success: runs only if checkout-branch SUCCEEDED (exit 0 = BRANCH_READY), so the checkout-failure cascade naturally skips it. SILENT-HOLE RISK (confirmed): FAIL_PLUGIN_NOT_IN_MANIFEST raises SystemExit (non-zero) under `set -e`, but there is NO dedicated cancel node guarding stage-plugin-context. A crash here surfaces only as cascading skips / all_done re-evaluation downstream, not as a clear cancel message."},{"id":"detect-resume-feature-plan","type":"bash","summary":"Smart-resume probe: if a committed feature-plan-manifest already exists for this plugin, copy it (+ tareas) into $ARTIFACTS_DIR and signal resume.","detail":"Reads HU_ID, PLUGIN_ID via jq. Checks F=hubara_agency/.hubara/plans/$HU_ID/feature-plans/$PLUGIN_ID/feature-plan-manifest.yaml. If it exists: copies it to $ARTIFACTS_DIR/feature-plan-manifest.yaml, and if the sibling tareas/ dir exists, mkdir $ARTIFACTS_DIR/tareas and `cp $TAREAS_DIR/F*.md` into it (errors ignored with || true), then echoes RESUMED_FEATURE_PLAN. Otherwise echoes NO_RESUME.","depends_on":["stage-plugin-context"],"trigger_rule":"all_success","when":"","produces":"output == 'RESUMED_FEATURE_PLAN' | 'NO_RESUME'","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Gate that routes the planner: planificar-feature-auto runs only when output != RESUMED_FEATURE_PLAN. Default all_success → depends on stage-plugin-context succeeding. Its output is consumed by planificar-feature-auto (when) AND it is also a dep of validate-feature-plan (so validate runs via all_done on both the auto path and the smart-resume path)."},{"id":"planificar-feature-auto","type":"skills","summary":"Feature-planner loop: invoke hubara-feature-planner-archon (max 2 iter) to decompose the plugin's work into a feature-level DAG, with operator gate feedback.","detail":"skills node carrying a loop {max_iterations:2, until:FEATURE_PLANNER_OK, skills:[hubara-feature-planner-archon], gate_message, prompt}. Prompt: decompose plugin '$parse-input.output.plugin_id' into a feature-level DAG, reading $ARTIFACTS_DIR/{plugin-work.yaml, plugin-manifest.yaml, hu-refinada.md, spinal-files.yaml, project-context.md} and writing $ARTIFACTS_DIR/feature-plan-manifest.yaml + tareas/F<NN>-<slug>.md (one per task), loading only guide sections matching the plugin template (A/B/C/D from plugin-work.yaml). Feedback via $LOOP_USER_INPUT. Completion signal <promise>FEATURE_PLANNER_OK</promise>. gate_message asks operator to review manifest + task files and say 'ok'/'aprobado' or request adjustments. node-level idle_timeout 600000.","depends_on":["detect-resume-feature-plan"],"trigger_rule":"all_success","when":"$detect-resume-feature-plan.output != 'RESUMED_FEATURE_PLAN'","produces":"side-effect: $ARTIFACTS_DIR/feature-plan-manifest.yaml + $ARTIFACTS_DIR/tareas/F*.md; completion signal <promise>FEATURE_PLANNER_OK</promise>","invokes":"hubara-feature-planner-archon","loop":"max_iterations:2, until:FEATURE_PLANNER_OK","is_gate":false,"is_cancel":false,"notes":"Interactive loop (gate_message + max_iterations:2) — uses skills+loop rather than command: because operator feedback iteration is needed. SKIPPED entirely on smart-resume (when guard). Writes only to $ARTIFACTS_DIR (ephemeral); the durable commit happens later in commit-feature-plan. idle_timeout 600000ms is the interactive idle gate."},{"id":"validate-feature-plan","type":"bash","summary":"Deterministic validation of the feature-plan-manifest: existence, dict shape, blocked-mode, task-count cap, F<NN> ids, and a matching task file per task.","detail":"Guarded by `when` checkout==BRANCH_READY. Checks $ARTIFACTS_DIR/feature-plan-manifest.yaml exists (else FAIL_NOT_EXISTS, exit 0). Exports MAX_FEATURES_PER_PLUGIN (default 12). python3 heredoc: loads yaml; if not a dict → FAIL_NOT_DICT; if mode=='blocked' → PASS_BLOCKED; if tasks not a non-empty list → FAIL_NO_TASKS; if len(tasks) > MAX → FAIL_TOO_MANY_TASKS: <n> > cap=<MAX>...; per task: missing id → FAIL_TASK_NO_ID, id not F+digits → FAIL_BAD_TASK_ID: <id>, no file in $ARTIFACTS_DIR/tareas starting with '<id>-' → FAIL_MISSING_TASK_FILE_<id>; else PASS. All paths exit 0.","depends_on":["planificar-feature-auto","detect-resume-feature-plan"],"trigger_rule":"all_done","when":"$checkout-branch.output == 'BRANCH_READY'","produces":"output: PASS | PASS_BLOCKED | FAIL_NOT_EXISTS | FAIL_NOT_DICT | FAIL_NO_TASKS | FAIL_TOO_MANY_TASKS:... | FAIL_TASK_NO_ID | FAIL_BAD_TASK_ID:... | FAIL_MISSING_TASK_FILE_<id>","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"CONVERGENCE/FAN-IN node: two deps (planificar-feature-auto, detect-resume-feature-plan) with trigger_rule all_done, so it runs whether the planner ran (auto path) OR was skipped (smart-resume path, where detect-resume staged the manifest). The `when` guard (checkout==BRANCH_READY) is the documented fix (run 52fa70e0): without it, on a checkout failure this node would run via all_done and emit the misleading FAIL_NOT_EXISTS instead of letting cancel-bad-checkout surface the real error. AMBIGUITY: emits both 'PASS' and 'PASS_BLOCKED', but every downstream consumer keys on =='PASS' (commit/implement) or !='PASS' (cancel) — so a legitimately blocked plan (mode:blocked → PASS_BLOCKED) routes to cancel-bad-feature-plan (treated as a validation failure), with no distinct blocked-handler."},{"id":"cancel-bad-feature-plan","type":"manual","summary":"Cancel node: aborts when the feature plan failed validation AND checkout had succeeded, with FAIL-code decode + recovery options.","detail":"Multi-line cancel: echoes $validate-feature-plan.output and the plugin id, then decodes FAIL_TOO_MANY_TASKS (split HU or bump MAX_FEATURES_PER_PLUGIN), FAIL_MISSING_TASK_FILE_F<NN> (re-run planner), FAIL_BAD_TASK_ID, plus generic recovery (edit plugin-manifest work_summary and re-run, or hand-edit feature-plan + tareas and re-run via smart-resume). Fires only when checkout reached BRANCH_READY AND validation != PASS.","depends_on":["validate-feature-plan"],"trigger_rule":"all_success","when":"$checkout-branch.output == 'BRANCH_READY' && $validate-feature-plan.output != 'PASS'","produces":"cancel (terminates run)","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Compound when (YAML comment L369-374): the extra `checkout==BRANCH_READY` guard prevents a confusing fire when checkout failed → validate skipped → its output '' which is != 'PASS'. Archon has NO regex, so `!= 'PASS'` substitutes for the original PASS-only regex. CAVEAT: because the guard is `!= 'PASS'`, a legitimate PASS_BLOCKED also trips this cancel — no separate blocked path. Default trigger_rule all_success: validate-feature-plan always exits 0, so this holds."},{"id":"commit-feature-plan","type":"bash","summary":"Persist the validated feature plan into the repo (plans/.../feature-plans/<plugin>/) and commit+push HEAD:<BRANCH>.","detail":"`set -e`. Reads HU_ID, PLUGIN_ID, BRANCH via jq. DIR=hubara_agency/.hubara/plans/$HU_ID/feature-plans/$PLUGIN_ID; mkdir DIR/tareas; copies $ARTIFACTS_DIR/feature-plan-manifest.yaml and tareas/F*.md into it. N=files in DIR/tareas. `git add DIR` (>&2). If staged diff non-empty: `git commit -m '<HU_ID> [<PLUGIN_ID>]: feature plan (auto, <N> tareas)'` (>&2), then `git push origin HEAD:$BRANCH` (tail -3 >&2). If PIPESTATUS[0] != 0 → `git pull --rebase origin $BRANCH` then retry push; if it fails twice, warns 'commit local, no en origin'. Echoes committed_<N>. Else echoes no_changes.","depends_on":["validate-feature-plan"],"trigger_rule":"all_success","when":"$validate-feature-plan.output == 'PASS'","produces":"output: committed_<N> | no_changes; side-effect: commit + push HEAD:$BRANCH of the feature plan","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"STDOUT-POLLUTION DISCIPLINE: all git output (add/commit/push) redirected to >&2 so stdout is single-line (committed_<N> | no_changes), per repo gotcha #8. Push uses HEAD:$BRANCH because checkout-branch left detached HEAD. Concurrency with parallel sub-pipelines handled via pull --rebase + single retry. `when ==PASS` excludes PASS_BLOCKED (a blocked plan never commits — it's already cancelled by cancel-bad-feature-plan)."},{"id":"prewarm-uv-venv","type":"bash","summary":"Pre-warm the uv virtualenv (cd hubara_agency && uv sync) before the implementer loop, to avoid cold-sync stalls in the gates.","detail":"If a hubara_agency dir exists, cd into it and run `uv sync` (tail -10; on warnings echoes 'uv sync warnings — gate retry'). Always echoes 'ok' at the end. timeout 600000 (10 min) to tolerate a cold full sync.","depends_on":["commit-feature-plan"],"trigger_rule":"all_done","when":"","produces":"output: ok (always)","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"trigger_rule all_done so it runs even if commit-feature-plan emitted no_changes or had warnings (no `when`, doesn't gate on a value). Best-effort warm-up — never fails the run (always echoes ok). Mirrors hu-frontend-pipeline's prewarm. One of the two deps of implementar-secuencial."},{"id":"implementar-secuencial","type":"skills","summary":"Sequential implementer loop: hubara-implementer-archon writes code per task; a large until_bash runs deterministic gates, commits/pushes, handles retries, and decides loop termination via exit code.","detail":"skills node with loop {max_iterations:50, until:NEVER_AI_SIGNAL, fresh_context:true, skills:[hubara-implementer-archon], until_bash, prompt}; node-level idle_timeout 1800000. The AI prompt restricts the implementer to ONLY writing code for the next pending task (iterate parallel_batches[] then tasks[] by ascending F-id; first task whose F<id>-result.yaml is missing/not status:passed in RESULTS_DIR), reading task.md + selective guide sections, running §10 verification, writing $ARTIFACTS_DIR/task-result.yaml with status + wiring_intents — NO git ops; if $ARTIFACTS_DIR/test-failures.md exists (gate feedback) it must fix the SAME task. The until_bash is the real controller: cleans stale result.yaml without a matching commit; if task-result.yaml status is passed|passed_with_warnings it RE-RUNS deterministic gates by FILES_TOUCHED (backend: pytest -q, pytest -m architecture, lint-imports; render-compose drift if a *plugin.yaml manifest touched; frontend: npm test --run, npm run test:arch, npx tsc -b, npm run build; Playwright E2E with a backgrounded FastAPI on a random free port if UI touched), logging to test-failures.md. On gate failure → det-retry up to MAX_DET_RETRIES=2 (rm task-result.yaml+task.md, exit 1 to re-loop); on exhaustion → rewrites status→blocked, reverts src/, SHOULD_COMMIT_BROKEN_CODE=0. On gate success → copies result.yaml to RESULTS_DIR, git add src/k8s/compose/.hubara/plugins + result, resets node_modules/dist/.vite/coverage/lock/.venv, commits '<HU_ID> [<PLUGIN_ID>] <TASK_ID>: status=<STATUS> (auto)' and pushes HEAD:$BRANCH (pull --rebase + retry). For non-passed status it classifies transient (blocked_reason in {command_timeout,regression}) → 1 auto-retry, else writes pipeline-error.yaml and exit 0. Finally TOTAL=grep -cE '^  - id: F[0-9]+' feature-plan-manifest vs PASSED=find F*-result.yaml with '^status: passed'; exit 0 when PASSED>=TOTAL>0, else exit 1 to continue.","depends_on":["commit-feature-plan","prewarm-uv-venv"],"trigger_rule":"all_done","when":"$validate-feature-plan.output == 'PASS'","produces":"side-effects: per-task commits+pushes to HEAD:$BRANCH; $ARTIFACTS_DIR/{task-result.yaml(transient), test-failures.md, det-retries-<TASK>.count, retries-<TASK>.count, playwright-evidence-<TASK>.log, .uvicorn-<TASK>.log}; RESULTS_DIR/F<NN>-result.yaml (durable); on permanent failure $ARTIFACTS_DIR/pipeline-error.yaml. Loop exits 0 when all tasks passed OR a permanent failure occurred.","invokes":"hubara-implementer-archon","loop":"max_iterations:50, until:NEVER_AI_SIGNAL (until_bash exit code controls termination)","is_gate":false,"is_cancel":false,"notes":"CONVERGENCE/FAN-IN node: two deps (commit-feature-plan, prewarm-uv-venv); trigger_rule all_done lets it run even though prewarm is best-effort. until:NEVER_AI_SIGNAL is a sentinel the AI never emits — the loop is driven ENTIRELY by until_bash exit codes (0=stop, 1=continue), capped at max_iterations:50. fresh_context:true wipes AI context each iteration; cross-iteration state lives ONLY in files. `when ==PASS` excludes PASS_BLOCKED. CRITICAL silent-hole nuance: a PERMANENT task failure exits 0 (NOT a crash), writing pipeline-error.yaml — so the loop 'completes successfully' and the failed verdict is recomputed downstream by write-plugin-result, NOT by an in-pipeline cancel. The det gate re-runs even on passed_with_warnings (SKILL.md L1164) because the AI can lie/silence. Playwright path traps EXIT to kill uvicorn; 180s liveness probe. All pushes use HEAD:$BRANCH (detached HEAD)."},{"id":"check-pipeline-error","type":"bash","summary":"Detect whether the implementer loop left a permanent-failure marker (pipeline-error.yaml).","detail":"If $ARTIFACTS_DIR/pipeline-error.yaml exists, cats it to >&2 and echoes HAS_ERROR; otherwise echoes OK. Runs with trigger_rule all_done so it executes regardless of how implementar-secuencial finished.","depends_on":["implementar-secuencial"],"trigger_rule":"all_done","when":"","produces":"output == 'HAS_ERROR' | 'OK'","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"SILENT-HOLE / DESIGN FLAG (confirmed): emits HAS_ERROR but NOTHING downstream gates on it — write-plugin-result depends on it with trigger_rule all_done and NO `when`, so HAS_ERROR does NOT cancel the run. The permanent-failure signal is effectively informational; the actual 'failed' verdict is recomputed independently by write-plugin-result via missing-tasks / non-passed result.yaml scanning. A pipeline-error.yaml alone (with all result files somehow present-and-passed) would not by itself force failed status."},{"id":"write-plugin-result","type":"bash","summary":"Compute the plugin's overall status (with a strict completeness check) and write+commit+push plugin-<id>-result.yaml.","detail":"`set -e`. Reads HU_ID, PLUGIN_ID, BRANCH via jq. COMPLETENESS: parses feature-plan-manifest (grep -oE '^  - id: F[0-9]+') to get PLANNED ids; for each, if FEATURE_RDIR/<tid>-result.yaml is missing, adds to MISSING_TASKS. STATUS AGGREGATION: scans FEATURE_RDIR/F*-result.yaml; 'passed' → PASSED++, 'passed_with_warnings' → PASSED++ & WARNED++ & WARNED_TASKS, anything else → ALL_PASSED=false & FAILED_TASKS. Plugin status: if MISSING_TASKS non-empty → failed (NEVER passed); elif ALL_PASSED and TOTAL>0 → passed_with_warnings if WARNED>0 else passed; else failed. Writes PLUGIN_RFILE (results/<HU_ID>/plugin-<PLUGIN_ID>-result.yaml) with version, hu_id, plugin_id, pipeline, date, status, feature_tasks_planned/total/passed/with_warnings, and missing_tasks/failed_tasks/warned_tasks lists. git add results/, and if staged diff non-empty commit '<HU_ID> [<PLUGIN_ID>]: plugin result.yaml' + push HEAD:$BRANCH (pull --rebase + retry fallback). Echoes plugin_result_committed status=<...> tasks=<PASSED>/<TOTAL> warnings=<WARNED>.","depends_on":["check-pipeline-error"],"trigger_rule":"all_done","when":"","produces":"output: plugin_result_committed status=<PLUGIN_STATUS> tasks=<P>/<T> warnings=<W>; side-effect: results/<HU_ID>/plugin-<plugin_id>-result.yaml committed+pushed; PLUGIN_STATUS in {passed, passed_with_warnings, failed}","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"trigger_rule all_done + NO `when` → ALWAYS runs; this is the report-back step the orchestrator reads, and the CANONICAL source of the plugin's verdict (independent of check-pipeline-error's HAS_ERROR). Two CRITICAL fixes encoded: (1) completeness check (run b9b95fc5, 2026-05-29) — counts PLANNED vs result files so a loop that completed only F01 of F01/F02/F03 reports failed with explicit missing_tasks, not passed; (2) passed_with_warnings counts as passing (run 25512e9, 2026-05-27) so the orchestrator's rama-B-merge-batch is not wrongly cancelled."},{"id":"print-summary","type":"bash","summary":"Print a human-readable box summarizing HU id, plugin, final status, branch, and operator next-steps.","detail":"Reads HU_ID, PLUGIN_ID via jq. Reads RESULT_STATUS from grep '^status:' of plugin-<id>-result.yaml (default 'unknown' if file missing). Cats an ASCII box with HU id, plugin, status, branch hu/<HU_ID>, and conditional guidance: if Status=passed the orchestrator collects the result, advances to FASE 4, and invokes the merger if shared files were touched (operator returns to orchestrator terminal and says 'ready'); if Status=failed it points to the feature-results dir and lists options (re-launch sub-pipeline for smart-resume, hand-edit the plan, or abandon the plugin). timeout 5000.","depends_on":["write-plugin-result"],"trigger_rule":"all_success","when":"","produces":"output: human-readable summary box (RESULT_STATUS read from plugin-result.yaml)","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Terminal node (nothing depends on it; END attaches here). Default trigger_rule all_success → only prints if write-plugin-result succeeded (it has set -e but always reaches an echo, so normally exits 0). Purely informational; no push/mutate. Short timeout (5000ms)."}],"edges":[{"from":"START","to":"parse-input","kind":"sequence","condition":""},{"from":"parse-input","to":"cancel-bad-input","kind":"cancel","condition":"$parse-input.output.type == 'error'"},{"from":"parse-input","to":"check-prereqs","kind":"gate","condition":"$parse-input.output.type == 'ok'"},{"from":"check-prereqs","to":"cancel-bad-prereqs","kind":"cancel","condition":"$check-prereqs.output != 'OK'"},{"from":"check-prereqs","to":"checkout-branch","kind":"gate","condition":"$check-prereqs.output == 'OK'"},{"from":"checkout-branch","to":"cancel-bad-checkout","kind":"cancel","condition":"$checkout-branch.output != 'BRANCH_READY'"},{"from":"checkout-branch","to":"stage-plugin-context","kind":"sequence","condition":""},{"from":"stage-plugin-context","to":"detect-resume-feature-plan","kind":"sequence","condition":""},{"from":"detect-resume-feature-plan","to":"planificar-feature-auto","kind":"gate","condition":"$detect-resume-feature-plan.output != 'RESUMED_FEATURE_PLAN'"},{"from":"planificar-feature-auto","to":"validate-feature-plan","kind":"fan-in","condition":"$checkout-branch.output == 'BRANCH_READY'"},{"from":"detect-resume-feature-plan","to":"validate-feature-plan","kind":"fan-in","condition":"$checkout-branch.output == 'BRANCH_READY'"},{"from":"validate-feature-plan","to":"cancel-bad-feature-plan","kind":"cancel","condition":"$checkout-branch.output == 'BRANCH_READY' && $validate-feature-plan.output != 'PASS'"},{"from":"validate-feature-plan","to":"commit-feature-plan","kind":"gate","condition":"$validate-feature-plan.output == 'PASS'"},{"from":"commit-feature-plan","to":"prewarm-uv-venv","kind":"sequence","condition":""},{"from":"commit-feature-plan","to":"implementar-secuencial","kind":"fan-in","condition":"$validate-feature-plan.output == 'PASS'"},{"from":"prewarm-uv-venv","to":"implementar-secuencial","kind":"fan-in","condition":"$validate-feature-plan.output == 'PASS'"},{"from":"implementar-secuencial","to":"check-pipeline-error","kind":"sequence","condition":""},{"from":"check-pipeline-error","to":"write-plugin-result","kind":"sequence","condition":""},{"from":"write-plugin-result","to":"print-summary","kind":"sequence","condition":""},{"from":"print-summary","to":"END","kind":"sequence","condition":""}],"phases":[{"name":"FASE 0 — Bootstrap & input validation","nodes":["parse-input","cancel-bad-input","check-prereqs","cancel-bad-prereqs","checkout-branch","cancel-bad-checkout","stage-plugin-context","detect-resume-feature-plan"],"description":"Parse and validate the '<HU_ID> <plugin_id>' input, verify runtime prerequisites and required files/skills, detached-HEAD-checkout origin/hu/<HU_ID>, verify the orchestrator-persisted plugin-manifest + refinement, stage shared context and extract the per-plugin plugin-work.yaml, and probe for a resumable feature plan. Three cancel nodes (input, prereqs, checkout) guard this phase; cancel-bad-checkout uses all_done to survive checkout's non-zero exits. stage-plugin-context can crash (FAIL_PLUGIN_NOT_IN_MANIFEST) with NO dedicated cancel."},{"name":"FASE 1 — Feature-level plan (within the plugin)","nodes":["planificar-feature-auto","validate-feature-plan","cancel-bad-feature-plan","commit-feature-plan","prewarm-uv-venv"],"description":"Run the hubara-feature-planner-archon skill (interactive, max 2 iter, gate_message) to decompose the plugin's work into feature-plan-manifest.yaml + tareas/F<NN>-*.md, unless smart-resume already staged them. Deterministically validate the plan (shape, blocked-mode, task cap 12, F-ids, task files) — validate is a fan-in of planner+detect-resume with all_done. Cancel on failure (PASS_BLOCKED also trips the cancel), commit+push the plan to HEAD:<BRANCH>, and pre-warm the uv venv for the implement phase."},{"name":"FASE 2 — Sequential implement (per-task gated loop)","nodes":["implementar-secuencial"],"description":"Loop the hubara-implementer-archon skill (max 50 iter, fresh_context, fan-in of commit-feature-plan+prewarm) over each pending task. The implementer only writes code; a large until_bash re-runs all deterministic gates (pytest, arch, lint-imports, render-compose drift, npm/tsc/build, Playwright with a backgrounded FastAPI on a random free port), handles det-retries (max 2) and transient retries (max 1), commits+pushes each passed task to HEAD:<BRANCH>, and terminates via exit code when all tasks pass OR a permanent failure is recorded (pipeline-error.yaml, exit 0)."},{"name":"FASE 3 — Report back to orchestrator","nodes":["check-pipeline-error","write-plugin-result","print-summary"],"description":"Detect any permanent-failure marker (informational only — nothing gates on HAS_ERROR), then compute the plugin's authoritative status with a strict completeness check (planned vs produced result files; passed_with_warnings counts as passing), write+commit+push plugin-<id>-result.yaml to HEAD:<BRANCH>, and print a human-readable summary box with operator next-steps. write-plugin-result always runs (all_done, no when) so the orchestrator always gets a result."}],"global_logic":"MODE / ROLE: This is the SUB-pipeline, invoked once per plugin. It does NOT itself detect single vs multi_plugin — that classification lives upstream in hu-hubara-tech-refiner / the orchestrator (hu-hubara-pipeline). The orchestrator either invokes this inline once (single_plugin) or has the operator fan it out N times (multi_plugin), each invocation handling exactly ONE plugin_id. Input contract is strictly 2 whitespace tokens: HU_ID (awk $1) + plugin_id (awk $2); a 1-token input (the main-pipeline shape) would leave plugin_id empty → cancel-bad-input. File-level: provider claude, model sonnet, interactive:false, worktree.enabled:true.\n\nKEY ENV / VARS: $ARGUMENTS (raw input). $parse-input.output is the run-wide source of truth — a JSON object carrying hu_id, plugin_id, branch=hu/<HU_ID>, and all derived hubara_agency/.hubara/ paths; almost every bash node re-derives HU_ID/PLUGIN_ID/BRANCH from it via jq. $ARTIFACTS_DIR is the ephemeral per-run workspace (skills read/write canonical filenames here; also a real env var in bash nodes). $WORKFLOW_ID is not referenced in this file. Tunable: MAX_FEATURES_PER_PLUGIN (default 12) caps feature tasks per plugin. fresh_context:true on the implement loop means NO context carries across iterations — all cross-iteration state is on disk (task-result.yaml, det-retries-<TASK>.count, retries-<TASK>.count, test-failures.md, RESULTS_DIR/*-result.yaml, pipeline-error.yaml).\n\nBRANCH STRATEGY (the load-bearing invariant): checkout-branch does `git checkout --detach origin/$BRANCH` (DETACHED HEAD, never claims the branch) precisely so N concurrent sub-pipeline worktrees can all target the same hu/<HU_ID>. Consequently EVERY push in the file is `git push origin HEAD:$BRANCH` (never `git push origin $BRANCH`). Inter-pipeline push concurrency is resolved by `git pull --rebase origin $BRANCH` + a single retry, with a 'commit local, no en origin' warning if it fails twice. The branch must already exist on origin (created by the orchestrator); else FAIL_BRANCH_NOT_FOUND. Smart-resume: re-launching with an existing HU_ID+plugin skips FASE 1 when feature-plan-manifest is already committed (detect-resume) and skips already-passed tasks (the implement loop scans RESULTS_DIR).\n\nRUN-WIDE INVARIANTS / DISCIPLINE: (1) status-on-stdout-single-line — every gate/bash node sends git and diagnostic output to >&2 and reserves stdout for a single canonical token, because Archon's condition evaluator does strict equality (repo gotcha #8). (2) all_done on the cancel/report nodes that follow steps which can exit non-zero (cancel-bad-checkout, validate-feature-plan, prewarm-uv-venv, implementar-secuencial, check-pipeline-error, write-plugin-result — exactly 6 nodes carry trigger_rule:all_done; the other 11 use the default all_success) — the default all_success would skip them on a crash and hide the real error (repo gotcha #9, run 52fa70e0). (3) The deterministic gate in until_bash NEVER trusts the AI's reported status — it re-runs the real gates even on passed/passed_with_warnings, and only commits code that passes; on gate-failure exhaustion it reverts src/ and marks blocked. (4) No silent 'passed' on incomplete work — write-plugin-result reports failed if any planned task lacks a result file. (5) passed_with_warnings is a first-class passing status everywhere (gate, retry decision, aggregation) per implementer SKILL.md L1164. RISK: check-pipeline-error emits HAS_ERROR but no node gates on it; the failed verdict is recomputed by write-plugin-result, and a permanent-failure (pipeline-error.yaml) exits the implement loop with 0 rather than crashing — so failure surfaces via the plugin-result.yaml status, never via a cancel node inside this sub-pipeline.","inputs":["$ARGUMENTS = \"<HU_ID> <plugin_id>\" (exactly 2 whitespace tokens; e.g. \"HU-20260517-143025-add-image-tool chats\")","HU_ID must match ^HU-[0-9]{8}-[0-9]{4,6}- (validated in parse-input; looser than main pipeline)","plugin_id must match ^[a-z][a-z0-9_]*$ (validated in parse-input)","Pre-existing origin branch hu/<HU_ID> (created by hu-hubara-pipeline)","Pre-committed hubara_agency/.hubara/plans/<HU_ID>/plugin-manifest.yaml (orchestrator FASE 2)","Pre-committed hubara_agency/.hubara/refinements/<HU_ID>-tech.md (orchestrator FASE 1)","Pre-committed hubara_agency/.hubara/spinal-files.yaml and project-context.md","Skills present: hubara-feature-planner-archon, hubara-implementer-archon, hubara-architecture-guide","Tooling on PATH: gh (authenticated), uv, npm, jq, python3 (curl/npx used inside gates)","Optional env: MAX_FEATURES_PER_PLUGIN (default 12)"],"verification_notes":["INDEPENDENT RE-READ: full file read in one Read call (978 lines — file ends at line 978, not 979 as the first pass stated; minor metadata error, no node impact). Node ids enumerated directly from the YAML by grepping top-level `- id:` lines plus inner loop markers to exclude `until:`/`max_iterations:` false positives.","NODE COUNT CONFIRMED = 17 (independently counted). First pass node_count=17 is CORRECT. The 17 ids verbatim: parse-input, cancel-bad-input, check-prereqs, cancel-bad-prereqs, checkout-branch, cancel-bad-checkout, stage-plugin-context, detect-resume-feature-plan, planificar-feature-auto, validate-feature-plan, cancel-bad-feature-plan, commit-feature-plan, prewarm-uv-venv, implementar-secuencial, check-pipeline-error, write-plugin-result, print-summary. NO nodes missed, NO nodes invented by the first pass.","depends_on for ALL 17 nodes verified against the YAML line-by-line (L84,102,108,161,187,233,252,259,344,376,410,420,430,804,938,977): every first-pass depends_on is CORRECT. Two multi-dep nodes confirmed: validate-feature-plan=[planificar-feature-auto, detect-resume-feature-plan]; implementar-secuencial=[commit-feature-plan, prewarm-uv-venv].","ALL `when` conditions verified verbatim against the YAML, including the two compound conditions: cancel-bad-feature-plan `$checkout-branch.output == 'BRANCH_READY' && $validate-feature-plan.output != 'PASS'` (L375) and the cross-node guard on validate-feature-plan `$checkout-branch.output == 'BRANCH_READY'` (L306). All match first pass. Nodes with NO `when` (correctly): parse-input, stage-plugin-context, detect-resume-feature-plan, prewarm-uv-venv, check-pipeline-error, write-plugin-result, print-summary.","trigger_rule verified by grepping every `trigger_rule:` line: exactly SIX nodes carry all_done — cancel-bad-checkout (L186), validate-feature-plan (L345), prewarm-uv-venv (L421), implementar-secuencial (L432), check-pipeline-error (L805), write-plugin-result (L939). The other 11 nodes have NO trigger_rule line → default all_success. This matches the first pass EXACTLY.","is_gate / is_cancel verified: 4 cancel nodes (cancel-bad-input, cancel-bad-prereqs, cancel-bad-checkout, cancel-bad-feature-plan) all use the `cancel:` key (L82,106,165,348) and are manual/cancel type — matches first pass. Gate (output-routing) nodes: parse-input, check-prereqs, checkout-branch, detect-resume-feature-plan, validate-feature-plan, check-pipeline-error — matches first pass.","Loop specs verified: planificar-feature-auto loop {max_iterations:2 (L263), until:FEATURE_PLANNER_OK (L264), skills:[hubara-feature-planner-archon], gate_message present (L267) → interactive}. implementar-secuencial loop {max_iterations:50 (L435), until:NEVER_AI_SIGNAL (L436) — sentinel, termination via until_bash exit code; fresh_context:true (L437); has until_bash, no gate_message}. Both match the first pass.","ONLY SUBSTANTIVE CORRECTION (edge KIND): the first pass labeled the two convergence edges into multi-dep nodes as kind:gate. I re-classified them as kind:fan-in to surface the DAG's only two convergence points: (validate-feature-plan ← planificar-feature-auto, validate-feature-plan ← detect-resume-feature-plan) and (implementar-secuencial ← commit-feature-plan, implementar-secuencial ← prewarm-uv-venv). The edge CONDITIONS are unchanged and preserved (validate's incoming carry checkout=='BRANCH_READY'; implementar's incoming carry validate=='PASS'). This is a modeling-precision refinement, not a correctness fix in the first pass's topology.","Edge TOPOLOGY otherwise identical to the first pass: 19 dependency edges + START→parse-input + print-summary→END = 21 edges total. Every (dep→node) pair independently re-derived and matches. Single root = parse-input (empty depends_on); single terminal = print-summary (no dependents).","CONFIRMED FLAGS (not transcription errors, re-verified): (a) check-pipeline-error emits HAS_ERROR but write-plugin-result (all_done, no when) does NOT gate on it — failure verdict recomputed by write-plugin-result; (b) a permanent task failure in implementar-secuencial exits the loop with code 0 (L727), writing pipeline-error.yaml — failure surfaces via plugin-result.yaml status, never via an in-sub-pipeline cancel; (c) validate-feature-plan can emit PASS_BLOCKED (L323) but all consumers key on =='PASS'/!='PASS' so PASS_BLOCKED routes to cancel-bad-feature-plan (no distinct blocked-handler); (d) stage-plugin-context can crash (set -e + python SystemExit FAIL_PLUGIN_NOT_IN_MANIFEST, L213) with NO dedicated cancel node. All four first-pass design flags are accurate.","Branch/push invariant re-confirmed across all 3 push sites: commit-feature-plan (L398,402), implementar-secuencial until_bash (L675,681), write-plugin-result (L932,934) — all use `git push origin HEAD:$BRANCH` with pull --rebase + retry, consistent with the detached-HEAD checkout (L146).","VERDICT: the first pass was almost entirely correct — node count, all node ids, all depends_on, all when (incl. both compounds), all trigger_rules, all is_gate/is_cancel, both loop specs, and the full 21-edge topology were accurate. Corrections made: (1) edge kind gate→fan-in on the 4 convergence edges (precision); (2) noted the file is 978 lines (first pass said 979). No node was missed or invented; no when/trigger_rule/depends_on was wrong."]},"review-pr-hubara":{"pipeline_id":"review-pr-hubara","title":"review-pr-hubara — Automated post-PR multi-agent code review + auto-fix","purpose":"POST-PR automated, NON-BLOCKING code review of PRs produced by hu-hubara-pipeline (or invoked manually on any PR URL). Bootstraps by validating the URL, checking prereqs, fetching the PR via gh, checking out its head branch (full checkout, not detached), and computing the merge-base diff vs origin/base. A haiku classifier loop decides which of 5 specialist review agents to run (deha-compliance, fsd-compliance, plugin-system, test-coverage[always], security). The 5 agents run in parallel, each as a loop node that Reads only its relevant hubara-architecture-guide sections and emits findings-<agent>.yaml. A synthesize loop node consolidates all findings into review-report.md, auto-fix-plan.yaml (CRITICAL/HIGH only), and merge-decision.yaml. An auto-fix bash node applies each fix patch, runs its verifier test, and reverts (git checkout HEAD -- file) any fix that breaks tests. Surviving fixes are committed/pushed to the PR branch. Finally it posts a consolidated informational comment to the PR and best-effort sets the GitHub Project status to 'Reviewing'. The comment does NOT block merge — the operator decides whether to merge or iterate.","trigger":"archon workflow run review-pr-hubara \"<PR_URL>\" where PR_URL matches ^https://github\\.com/.+/pull/[0-9]+$. Also auto-triggered in background by hu-hubara-pipeline.trigger-review. Top-level config: provider: claude, model: sonnet, interactive: false, worktree.enabled: true. Single positional argument ($ARGUMENTS) = the PR URL.","node_count":20,"global_logic":"No HU_ID / single-vs-multi_plugin mode detection exists in THIS workflow (that lives in hu-hubara-pipeline). The run-wide selector here is the haiku classifier output: agents-to-run.json = {deha:bool, fsd:bool, plugin_system:bool, test_coverage:true(always), security:bool}, parsed by parse-classify (line 141) into a structured JSON object via `jq -n --argjson ...` so Archon exposes output.deha/fsd/plugin_system/test_coverage/security as structured fields. Each of the 5 agents is gated via `when: $parse-classify.output.<field> == 'true'` (compares the JSON boolean rendered as the STRING 'true'). Key env/substitution vars: $ARGUMENTS (PR URL), $ARTIFACTS_DIR (ephemeral per-run workspace; both literal text substitution AND a real env var for bash nodes). BRANCH/BASE are NOT workflow env vars — derived at runtime from pr.json (jq .headRefName / .baseRefName) and persisted to $ARTIFACTS_DIR/branch.txt + base.txt; pr-url.txt + pr-num.txt persisted by parse-input. Branch strategy: FULL checkout of the PR head branch (git fetch origin BRANCH; git checkout BRANCH; git pull --ff-only) — NOT detached HEAD (unlike the plugin sub-pipelines, project gotcha #9). Diff computed `git diff origin/BASE...HEAD` (three-dot, merge-base). Auto-fix commits land on the SAME PR branch via `git push origin BRANCH` with pull --rebase + retry fallback. Invariant: only TWO cancel nodes (cancel-bad-input, cancel-bad-prereqs), both pre-diff (FASE 0). After bootstrap NO gate cancels the run. SILENT HOLE: merge-decision.yaml is PRODUCED by synthesize and its prompt (lines 620-621) asserts 'la fase final del workflow lee este artifact y bloquea el merge si blocked=true', but NO downstream node ever reads merge-decision.yaml — the merge_blocking machinery (R-DIP cross-agent, F5, F7, F1) is computed but never enforced; the review is purely informational (lines 28-29 confirm). Convention for bash status nodes: canonical single-line status on stdout (OK / FAIL_* / no_plan / no_fixes_to_commit / skipped / etc.), diagnostics to stderr (>&2). Several bash nodes use `exit 0` even on failure (emitting a FAIL_* token) so all_done downstream nodes still fire; only the two explicit cancel nodes abort.","inputs":["$ARGUMENTS = PR URL (^https://github\\.com/.+/pull/[0-9]+$)","Runtime prereqs: gh authenticated, jq present, .claude/skills/hubara-architecture-guide/SKILL.md present","Git remote 'origin' with the PR head + base branches fetchable","hubara-architecture-guide sections (02-09) + references (deha-rules.md, fsd-rules.md, manifest-schema.md) Read by the 5 agents","Optional .archon/github-project-config.yaml for the Project 'Reviewing' status update","PR body containing 'Closes <issue-url>' for project-set-reviewing to locate the project item"],"nodes":[{"id":"parse-input","type":"bash","summary":"Validate the PR URL argument and persist URL + PR number.","detail":"Reads PR_URL=$ARGUMENTS. If it does NOT match ^https://github\\.com/.+/pull/[0-9]+$, echoes 'FAIL_BAD_URL: <url>' and exits 0 (non-fatal). On match, extracts the trailing PR number via grep -oE '[0-9]+$', writes $ARTIFACTS_DIR/pr-url.txt and pr-num.txt, echoes 'OK'. timeout 5000ms.","depends_on":[],"trigger_rule":"all_success","when":"","produces":"stdout: 'OK' | 'FAIL_BAD_URL: <url>'. Files: pr-url.txt, pr-num.txt.","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Entry node (no depends_on). Exits 0 even on bad URL — its output VALUE routes the fork: cancel-bad-input (!= 'OK') vs check-prereqs (== 'OK'). Functionally a gate identical in shape to check-prereqs (first pass omitted is_gate here — corrected to true)."},{"id":"cancel-bad-input","type":"manual","summary":"Abort run if the PR URL was invalid.","detail":"A `cancel:` node (line 57) carrying the message \"Input inválido. Usage: archon workflow run review-pr-hubara '<PR_URL>'\". Fires only when parse-input did not emit exactly 'OK'. Cancels the whole workflow.","depends_on":["parse-input"],"trigger_rule":"all_success","when":"$parse-input.output != 'OK'","produces":"Run cancellation with usage message.","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"No explicit trigger_rule → default all_success. parse-input always exits 0 (so it is 'success'), thus this node is eligible; the `when` is the real guard routing the FAIL_BAD_URL branch to termination."},{"id":"check-prereqs","type":"bash","summary":"Verify gh auth, jq, and the architecture-guide skill exist.","detail":"`gh auth status >&2 2>&1` → on failure echoes 'FAIL_GH_AUTH', exit 0. `command -v jq` → 'FAIL_NO_JQ'. `test -f .claude/skills/hubara-architecture-guide/SKILL.md` → 'FAIL_MISSING_GUIDE_SKILL'. If all pass echoes 'OK'. Gated on parse-input == 'OK'.","depends_on":["parse-input"],"trigger_rule":"all_success","when":"$parse-input.output == 'OK'","produces":"stdout: 'OK' | 'FAIL_GH_AUTH' | 'FAIL_NO_JQ' | 'FAIL_MISSING_GUIDE_SKILL'.","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"Line 63 `gh auth status >&2 2>&1` sends gh output to stderr keeping the canonical status line clean. Each check exits 0 with a FAIL_* token (not non-zero), deferring the abort to cancel-bad-prereqs. Its output VALUE routes the fork."},{"id":"cancel-bad-prereqs","type":"manual","summary":"Abort run if any prerequisite check failed.","detail":"A `cancel:` node (line 73) with message \"Pre-requisitos: $check-prereqs.output. (gh auth login / brew install jq / commit guide skill)\" interpolating the FAIL_* token. Fires when check-prereqs did not emit 'OK'.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output != 'OK'","produces":"Run cancellation with the failing prereq token echoed back.","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"Default trigger_rule all_success (check-prereqs always exits 0); the `when` does the routing."},{"id":"fetch-pr","type":"bash","summary":"Fetch PR metadata via gh and persist head/base branch names.","detail":"`set -e`. Reads PR_NUM from pr-num.txt, `gh pr view PR_NUM --json title,body,baseRefName,headRefName,files > pr.json`. Extracts BRANCH=jq .headRefName, BASE=jq .baseRefName, echoes 'branch=<B> base=<B>', writes branch.txt + base.txt. Gated on check-prereqs == 'OK'.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output == 'OK'","produces":"stdout: 'branch=<head> base=<base>'. Files: pr.json, branch.txt, base.txt.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"`set -e` means a gh/jq failure aborts the node non-zero (unlike the prereq nodes). NO cancel node guards fetch-pr — a failure here lets downstream all_success deps simply not fire (checkout-branch is default all_success), silently halting without an explicit cancel message. Silent-hole / observability gap."},{"id":"checkout-branch","type":"bash","summary":"Fetch and checkout the PR head branch (fast-forward pull).","detail":"`set -e`. Reads BRANCH from branch.txt; `git fetch origin BRANCH`; `git checkout BRANCH`; `git pull --ff-only origin BRANCH` (output to stderr, '|| true' so a non-ff pull doesn't abort). Echoes 'OK'.","depends_on":["fetch-pr"],"trigger_rule":"all_success","when":"","produces":"stdout: 'OK'. Working tree on the PR head branch.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"FULL branch checkout (claims the branch), NOT detached HEAD (contrast project gotcha #9 plugin sub-pipelines). Safe as a standalone review run in its own worktree. `set -e` + NO cancel guard: a checkout failure halts downstream silently (no FAIL token, no cancel)."},{"id":"fetch-diff","type":"bash","summary":"Compute the PR diff and changed-file list vs origin/base.","detail":"Reads BASE from base.txt. Writes `git diff origin/BASE...HEAD` → diff.patch and `git diff --name-only origin/BASE...HEAD` → files-changed.txt (three-dot = merge-base). Echoes 'files=<count>' (wc -l). These two artifacts feed the classifier and all 5 agents.","depends_on":["checkout-branch"],"trigger_rule":"all_success","when":"","produces":"stdout: 'files=<N>'. Files: diff.patch, files-changed.txt.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"No `set -e`; if origin/BASE is unresolvable git diff errors but the node still exits 0 with a possibly-empty diff. Three-dot merge-base diff (consistent with the recent frontend meta-gate commits)."},{"id":"classify","type":"skills","summary":"Haiku classifier loop decides which of the 5 agents to run.","detail":"provider: claude, model: haiku, idle_timeout 60000. Loop node (skills: [] empty — pure prompt, gate_message 'Classifier auto.') reads files-changed.txt and writes agents-to-run.json = {deha,fsd,plugin_system,test_coverage:true,security}. Rules: deha if any hubara_agency/src/; fsd if any frontend_dashboard/src/; plugin_system if plugin.yaml/plugin.schema.yaml/plugin_manifest.py/scripts/render-compose.py/k8s/aws-produccion/; test_coverage ALWAYS true; security if .env*/secrets/configmap.yaml or Python adding os.environ/getpass. Emits <promise>CLASSIFY_OK</promise>.","depends_on":["fetch-diff"],"trigger_rule":"all_success","when":"","produces":"File: agents-to-run.json. Completion signal: <promise>CLASSIFY_OK</promise>.","invokes":"inline loop prompt (skills: [] empty)","loop":"max_iterations:2, until:CLASSIFY_OK","is_gate":false,"is_cancel":false,"notes":"loop max_iterations:2 is a retry safety net — the agent sometimes finishes without emitting the promise on iteration 1 (run 2484bd91, agent-plugin-system); 2nd pass lets it emit, no-op if already done. If classifier writes nothing, parse-classify defaults to all-true."},{"id":"parse-classify","type":"bash","summary":"Parse agents-to-run.json into structured boolean output fields.","detail":"F=$ARTIFACTS_DIR/agents-to-run.json. If absent, writes default {all true incl test_coverage}. Reads DEHA=jq '.deha//false', FSD=jq '.fsd//false', PS=jq '.plugin_system//false', TC=jq '.test_coverage//true', SEC=jq '.security//false', then emits a JSON object via `jq -n --argjson ...` so Archon exposes output.deha/fsd/plugin_system/test_coverage/security as structured fields. trigger_rule: all_done.","depends_on":["classify"],"trigger_rule":"all_done","when":"","produces":"structured output: output.deha/fsd/plugin_system/test_coverage/security each {true,false} JSON booleans (compared as strings 'true' downstream).","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"The fan-out selector/gate. test_coverage defaults to true (jq '.test_coverage // true') so the test-coverage agent runs even if classifier omitted it. Downstream `when` clauses compare to the STRING 'true'. trigger_rule all_done is critical: classify can exhaust its 2 loops without CLASSIFY_OK (non-success) yet parse-classify must still fire to default the agents."},{"id":"agent-deha-compliance","type":"skills","summary":"DEHA R-rules + plugin-manifest specialist review agent.","detail":"idle_timeout 300000. Loop node (skills: []) gated on classifier.deha. Reads guide sections 02/03/04-backend + references/deha-rules.md plus diff.patch + files-changed.txt. Hunts R-DET/R-JSON/R-STATELESS/R-HEARTBEAT/R-DIP including the CRITICAL cross-agent import pattern (ADR-2026-05-20 #10) and start_workflow on a sibling agent's workflow class; plus footguns F1 (dict→dataclass contract drift, HIGH), F2 (workflow.patched branch parity, MEDIUM), F3 (Path.resolve, LOW), F4 (bootstrap runtime_workspace_path fallback, MEDIUM), F5 (nested dataclass + PEP 563 in activity return, HIGH merge_blocking), F7 (start_delay without eligibility gate, HIGH merge_blocking). Emits findings-deha.yaml. <promise>REPORT_OK</promise>.","depends_on":["parse-classify"],"trigger_rule":"all_success","when":"$parse-classify.output.deha == 'true'","produces":"File: findings-deha.yaml (findings[] or findings: []). Signal: <promise>REPORT_OK</promise>.","invokes":"inline loop prompt (reads hubara-architecture-guide via Read; skills: [])","loop":"max_iterations:2, until:REPORT_OK","is_gate":false,"is_cancel":false,"notes":"Default trigger_rule all_success. Runs in parallel with the other 4 (all depend only on parse-classify). Skipped if deha not selected. By far the richest prompt (~240 lines). Only R-DIP cross-agent and F5/F7 set merge_blocking."},{"id":"agent-fsd-compliance","type":"skills","summary":"Frontend Feature-Sliced Design compliance review agent.","detail":"idle_timeout 300000. Loop node gated on classifier.fsd. Reads guide sections 05/06-frontend + references/fsd-rules.md plus diff.patch + files-changed.txt. Hunts the 14 anti-patterns + 4 import rules: cross-plugin imports, deep imports bypassing barrel, direct fetch() in components/pages, useState for server data, apiClient.get<T>() without schema.parse(), JSX in .ts, cross-feature imports, Tailwind --color-text-* naming, hardcoded env vars outside shared/config/env.ts, layering violations. Emits findings-fsd.yaml (same schema as deha). <promise>REPORT_OK</promise>.","depends_on":["parse-classify"],"trigger_rule":"all_success","when":"$parse-classify.output.fsd == 'true'","produces":"File: findings-fsd.yaml. Signal: <promise>REPORT_OK</promise>.","invokes":"inline loop prompt (skills: [])","loop":"max_iterations:2, until:REPORT_OK","is_gate":false,"is_cancel":false,"notes":"Default trigger_rule all_success. Parallel sibling; skipped if fsd not selected. No merge_blocking footguns defined for this agent."},{"id":"agent-plugin-system","type":"skills","summary":"Plugin manifest schema + parity + render-compose drift agent.","detail":"idle_timeout 300000. Loop node gated on classifier.plugin_system. Reads guide sections 07/08 + references/manifest-schema.md plus diff.patch + files-changed.txt. Verifies plugin.yaml schema (id ^[a-z][a-z0-9_]*$, SemVer, workers with task_queue), worker↔K8s manifest correspondence (worker-<name>.yaml in k8s/aws-produccion/), no orphan K8s manifests, task_queue uniqueness cross-plugin, render-compose drift, plugin id == directory name, shared-file wiring_intents, architecture-protected files NOT modified. Footgun F7 frontend block contract: manifest declares frontend: → <plugin>/frontend/index.ts MUST exist (HIGH merge_blocking); not declared → dir must NOT exist (MEDIUM); touching plugin.yaml/frontend requires regenerated src/app/plugin-registry.generated.ts (LOW). Emits findings-plugin-system.yaml. <promise>REPORT_OK</promise>.","depends_on":["parse-classify"],"trigger_rule":"all_success","when":"$parse-classify.output.plugin_system == 'true'","produces":"File: findings-plugin-system.yaml. Signal: <promise>REPORT_OK</promise>.","invokes":"inline loop prompt (skills: [])","loop":"max_iterations:2, until:REPORT_OK","is_gate":false,"is_cancel":false,"notes":"Default trigger_rule all_success. The agent named in run 2484bd91 that motivated the max_iterations:2 retry across ALL loop nodes. Its 'frontend block contract' F7 is a DIFFERENT F7 than the deha agent's start_delay F7 — naming collision."},{"id":"agent-test-coverage","type":"skills","summary":"Functional + e2e test coverage presence review agent.","detail":"idle_timeout 300000. Loop node gated on classifier.test_coverage (defaults true → effectively always runs). Reads guide section 08 plus diff.patch + files-changed.txt. Verifies each new feature has a functional test in hubara_agency/tests/functional/ with @pytest.mark.functional (mock_llm if LLM); UI changes have e2e specs in frontend_dashboard/e2e/<feature>/ using getByRole/getByText (no waitForTimeout); architecture gates + premortem invariants passing; unit tests per new tool/activity/hook; pure-refactor skips documented in task-result. Emits findings-test-coverage.yaml. <promise>REPORT_OK</promise>.","depends_on":["parse-classify"],"trigger_rule":"all_success","when":"$parse-classify.output.test_coverage == 'true'","produces":"File: findings-test-coverage.yaml. Signal: <promise>REPORT_OK</promise>.","invokes":"inline loop prompt (skills: [])","loop":"max_iterations:2, until:REPORT_OK","is_gate":false,"is_cancel":false,"notes":"Default trigger_rule all_success. Has a `when` clause despite being 'always run' — relies on parse-classify's jq default (.test_coverage // true), which only fires when the key is absent/null. If the classifier explicitly wrote test_coverage:false this agent would be silently skipped. Verifies test PRESENCE only, not behavior (project gotcha #1)."},{"id":"agent-security","type":"skills","summary":"Secrets / env-var / CORS / input-validation security agent.","detail":"idle_timeout 300000. Loop node gated on classifier.security. Reads guide sections 02-backend + 09-conventions plus diff.patch + files-changed.txt. Hunts hardcoded secrets (AKIA[A-Z0-9]{16}, ghp_*, JWT ey*.*., passwords), new env vars not declared in wiring_intents.env_vars_required, os.environ crossing boundary, logs exposing secrets (unsanitized WhatsApp inbound body), overly-permissive CORS, missing HTTP input validation (no Pydantic/Zod), non-rotatable K8s secrets (hardcoded vs valueFrom). Emits findings-security.yaml. <promise>REPORT_OK</promise>.","depends_on":["parse-classify"],"trigger_rule":"all_success","when":"$parse-classify.output.security == 'true'","produces":"File: findings-security.yaml. Signal: <promise>REPORT_OK</promise>.","invokes":"inline loop prompt (skills: [])","loop":"max_iterations:2, until:REPORT_OK","is_gate":false,"is_cancel":false,"notes":"Default trigger_rule all_success. Parallel sibling; skipped if security not selected. No merge_blocking footguns defined."},{"id":"synthesize","type":"skills","summary":"Consolidate all findings-*.yaml into report + auto-fix-plan + merge-decision.","detail":"idle_timeout 180000. Loop node depending on ALL 5 agents with trigger_rule all_done (tolerates skipped/failed agents). Reads every findings-*.yaml present. Output 1: review-report.md (severity counts + per-agent + grouped findings + 'Auto-fix attempted' list). Output 2: auto-fix-plan.yaml = fixes[] (ONLY critical+high with non-empty fix_suggestion) each {file,severity,rule,patch (unified diff), revertible_by_test}. Output 3: merge-decision.yaml {blocked:bool, blocking_findings[]}. Special rules: any R-DIP finding whose message contains 'Cross-agent'/'sibling'/'viola R-DIP #10' → bump to critical + merge_blocking:true; dict→dataclass F1 high findings → add to blocking_findings. <promise>SYNTH_OK</promise>.","depends_on":["agent-deha-compliance","agent-fsd-compliance","agent-plugin-system","agent-test-coverage","agent-security"],"trigger_rule":"all_done","when":"","produces":"Files: review-report.md, auto-fix-plan.yaml (fixes[]), merge-decision.yaml (blocked + blocking_findings[]). Signal: <promise>SYNTH_OK</promise>.","invokes":"inline loop prompt (skills: [])","loop":"max_iterations:2, until:SYNTH_OK","is_gate":false,"is_cancel":false,"notes":"Fan-in of the 5-agent parallel section. CRITICAL silent hole: merge-decision.yaml is produced and the prompt (lines 620-621) claims 'la fase final del workflow lee este artifact y bloquea el merge si blocked=true' — but NO downstream node reads merge-decision.yaml. The merge-blocking machinery is dead/unenforced; the comment is purely informational (lines 28-29). all_done means even if every agent was skipped, synthesize still runs and must handle absent findings files."},{"id":"auto-fix","type":"bash","summary":"Apply each CRITICAL/HIGH fix patch; revert any that breaks its test.","detail":"timeout 1800000ms (30 min). Initializes fixes-applied.yaml and fixes-reverted.yaml to 'fixes: []'. If auto-fix-plan.yaml absent → echo 'no_plan'; exit 0. Otherwise an inline python3 heredoc loads the plan, iterates fixes[]; for each snapshots `git show HEAD:<file>`, writes patch to tempfile, `git apply --check` (fail → reverted+'patch_did_not_apply'), then `git apply`. If revertible_by_test given, runs it (timeout 300s) — on non-zero `git checkout HEAD -- <file>` and records 'test_failed_after_fix'. Successful → applied[]. Dumps via os.environ.get('APPLIED_PATH','$ARTIFACTS_DIR/...') with literal-path fallback. Prints 'applied=<n> reverted=<m>'. Node `env:` PLAN_PATH/APPLIED_PATH/REVERTED_PATH.","depends_on":["synthesize"],"trigger_rule":"all_success","when":"","produces":"stdout: 'no_plan' | 'applied=<n> reverted=<m>'. Files: fixes-applied.yaml, fixes-reverted.yaml. Mutates working tree with applied patches.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Default trigger_rule all_success: if synthesize never reached SYNTH_OK (loop exhausted = non-success) auto-fix is SKIPPED → cascade; commit-fixes/post-comment use all_done so they still fire but with no report. Comment (lines 705-708, run 2484bd91): node-level `env:` does NOT reach the python subprocess (os.environ.get('APPLIED_PATH')→None→open(None) TypeError); fix is the literal '$ARTIFACTS_DIR/...' fallback Archon text-substitutes. PLAN_PATH (line 662) uses the same fallback. Reverts via `git checkout HEAD -- <file>` revert the ENTIRE file → multi-fix-same-file ordering hazard (discards an earlier applied fix to the same file)."},{"id":"commit-fixes","type":"bash","summary":"Commit and push applied auto-fixes to the PR branch.","detail":"Reads BRANCH from branch.txt. `git add -A` (|| true). If `git diff --staged --quiet` (nothing staged) → echo 'no_fixes_to_commit'; exit 0. Else N = count of fixes-applied.yaml fixes (yq, python3 yaml fallback, else '?'); `git commit -m \"review-pr-hubara: auto-fix ${N} critical/high finding(s)\"`; `git push origin BRANCH` with a `git pull --rebase origin BRANCH` + retry-push fallback (all diagnostics → stderr). Echoes 'fixes_committed=<N>'. trigger_rule all_done.","depends_on":["auto-fix"],"trigger_rule":"all_done","when":"","produces":"stdout: 'no_fixes_to_commit' | 'fixes_committed=<N>'. Side-effect: commit + push to PR branch.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Diagnostics correctly redirected to stderr (>&2) so stdout stays single-line (project gotcha #8). Pushes the FULL branch ref `git push origin BRANCH` (not HEAD:BRANCH) since checkout-branch claimed the branch. pull --rebase retry handles concurrency. $ARTIFACTS_DIR is outside the worktree, so git add -A stages only real code fixes."},{"id":"post-comment","type":"bash","summary":"Post the consolidated review report as a PR comment.","detail":"Reads PR_URL from pr-url.txt; REPORT=$ARTIFACTS_DIR/review-report.md. If report absent → echo 'FAIL_NO_REPORT'; exit 0. Builds comment.md: a '🤖 Automated Review' header, the full report appended, then an 'Auto-fix summary' block (N_APPLIED/N_REVERTED counted from fixes-applied.yaml/fixes-reverted.yaml via python3, '?' fallback) if fixes-applied.yaml exists, then a footer. Posts via `gh pr comment PR_URL --body-file comment.md` (output→stderr). Echoes 'comment_posted'. trigger_rule all_done.","depends_on":["commit-fixes"],"trigger_rule":"all_done","when":"","produces":"stdout: 'FAIL_NO_REPORT' | 'comment_posted'. Side-effect: a GitHub PR comment. File: comment.md.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"The comment is INFORMATIONAL and non-blocking (lines 28-29). Does NOT read merge-decision.yaml — even if synthesize flagged blocked:true this comment never surfaces or enforces it. all_done ensures it posts even when commit-fixes/auto-fix were skipped. If review-report.md is missing it emits FAIL_NO_REPORT, exits 0, no comment — silent partial completion (no cancel)."},{"id":"project-set-reviewing","type":"bash","summary":"Best-effort set GitHub Project status to 'Reviewing'.","detail":"If .archon/github-project-config.yaml absent → echo 'skipped'; exit 0. Reads PR_NUM, derives ISSUE_URL from the PR body (`gh pr view --jq .body | grep -oE 'Closes https://[^ ]+' | head -1 | awk '{print $2}'`); if none → 'no_issue_url_in_pr'. Parses project_number/owner/id/status_field_id and the 'Reviewing' option id (awk); if no option → 'no_reviewing_option'. Finds the project item id matching the issue URL via `gh project item-list ... | jq`; if none → exit 0. Runs `gh project item-edit` to set the single-select status field. Echoes 'set Reviewing ok'.","depends_on":["post-comment"],"trigger_rule":"all_done","when":"","produces":"stdout: 'skipped' | 'no_issue_url_in_pr' | 'no_reviewing_option' | 'set Reviewing ok' (or silent exit 0).","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Entirely best-effort; every failure path exits 0 with a status token. Depends on the PR body containing 'Closes <issue-url>' (produced by hu-hubara-pipeline's build-pr-body). Manually-reviewed PRs lacking that line → 'no_issue_url_in_pr' and no project update."},{"id":"print-summary","type":"bash","summary":"Print the final completion banner with next steps.","detail":"timeout 5000ms. Reads PR_URL from pr-url.txt and prints an ASCII box: '🎉 review-pr-hubara completo', the PR URL, a note that the comment was posted with consolidated findings + auto-fixes applied (reverted if they broke tests), and next steps (review comment, fix pendings by hand, squash-merge when OK). Depends on BOTH post-comment and project-set-reviewing with trigger_rule all_done.","depends_on":["post-comment","project-set-reviewing"],"trigger_rule":"all_done","when":"","produces":"stdout: human-readable completion banner.","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Terminal node (no other node depends on it). all_done + dual deps means it prints regardless of whether the comment/project steps succeeded. The dependency on post-comment (also transitive via project-set-reviewing) is listed explicitly — harmless redundancy, but both deps are real edges in the YAML."}],"edges":[{"from":"START","to":"parse-input","kind":"sequence","condition":""},{"from":"parse-input","to":"cancel-bad-input","kind":"cancel","condition":"$parse-input.output != 'OK'"},{"from":"parse-input","to":"check-prereqs","kind":"gate","condition":"$parse-input.output == 'OK'"},{"from":"check-prereqs","to":"cancel-bad-prereqs","kind":"cancel","condition":"$check-prereqs.output != 'OK'"},{"from":"check-prereqs","to":"fetch-pr","kind":"gate","condition":"$check-prereqs.output == 'OK'"},{"from":"fetch-pr","to":"checkout-branch","kind":"sequence","condition":""},{"from":"checkout-branch","to":"fetch-diff","kind":"sequence","condition":""},{"from":"fetch-diff","to":"classify","kind":"sequence","condition":""},{"from":"classify","to":"parse-classify","kind":"sequence","condition":""},{"from":"parse-classify","to":"agent-deha-compliance","kind":"fan-out","condition":"$parse-classify.output.deha == 'true'"},{"from":"parse-classify","to":"agent-fsd-compliance","kind":"fan-out","condition":"$parse-classify.output.fsd == 'true'"},{"from":"parse-classify","to":"agent-plugin-system","kind":"fan-out","condition":"$parse-classify.output.plugin_system == 'true'"},{"from":"parse-classify","to":"agent-test-coverage","kind":"fan-out","condition":"$parse-classify.output.test_coverage == 'true'"},{"from":"parse-classify","to":"agent-security","kind":"fan-out","condition":"$parse-classify.output.security == 'true'"},{"from":"agent-deha-compliance","to":"synthesize","kind":"fan-in","condition":""},{"from":"agent-fsd-compliance","to":"synthesize","kind":"fan-in","condition":""},{"from":"agent-plugin-system","to":"synthesize","kind":"fan-in","condition":""},{"from":"agent-test-coverage","to":"synthesize","kind":"fan-in","condition":""},{"from":"agent-security","to":"synthesize","kind":"fan-in","condition":""},{"from":"synthesize","to":"auto-fix","kind":"sequence","condition":""},{"from":"auto-fix","to":"commit-fixes","kind":"sequence","condition":""},{"from":"commit-fixes","to":"post-comment","kind":"sequence","condition":""},{"from":"post-comment","to":"project-set-reviewing","kind":"sequence","condition":""},{"from":"post-comment","to":"print-summary","kind":"sequence","condition":""},{"from":"project-set-reviewing","to":"print-summary","kind":"fan-in","condition":""},{"from":"cancel-bad-input","to":"END","kind":"cancel","condition":""},{"from":"cancel-bad-prereqs","to":"END","kind":"cancel","condition":""},{"from":"print-summary","to":"END","kind":"sequence","condition":""}],"phases":[{"name":"FASE 0 — Bootstrap (parse + prereqs + checkout + diff)","nodes":["parse-input","cancel-bad-input","check-prereqs","cancel-bad-prereqs","fetch-pr","checkout-branch","fetch-diff"],"description":"Validates the PR URL and runtime prerequisites (gh/jq/guide skill), cancelling early on either failure (the workflow's ONLY two cancel nodes). Fetches PR metadata, FULL-checks-out the PR head branch, and computes the merge-base diff (diff.patch + files-changed.txt) that feeds the classifier and all 5 agents. fetch-pr/checkout-branch use `set -e` with no cancel guard — an observability gap."},{"name":"FASE 1 — Classify (haiku selects agents)","nodes":["classify","parse-classify"],"description":"A fast haiku loop reads files-changed.txt and writes agents-to-run.json deciding which of the 5 specialist agents to run (test_coverage always true). parse-classify (all_done) turns that JSON into structured boolean output fields (defaulting all-true if the classifier wrote nothing) that gate the parallel agents via `when: ... == 'true'`."},{"name":"FASE 2 — 5 specialist agents in parallel","nodes":["agent-deha-compliance","agent-fsd-compliance","agent-plugin-system","agent-test-coverage","agent-security"],"description":"Five independent loop agents fan out from parse-classify, each gated by its classifier flag and each Reading only its relevant hubara-architecture-guide sections. They audit their vertical (DEHA R-rules+footguns, FSD anti-patterns, plugin manifest/schema, test coverage, security) and emit findings-<agent>.yaml, signalling REPORT_OK. All default to trigger_rule all_success."},{"name":"FASE 3 — Synthesize findings + decide auto-fix","nodes":["synthesize"],"description":"Fan-in (all_done) over the 5 agents. Consolidates every findings-*.yaml into review-report.md, an auto-fix-plan.yaml restricted to CRITICAL/HIGH fixes with patches+verifier tests, and a merge-decision.yaml (blocked/blocking_findings) that — per the prompt — is meant to block merge but is NEVER read downstream (dead machinery)."},{"name":"FASE 4 — Auto-fix CRITICAL/HIGH (revert on test failure)","nodes":["auto-fix","commit-fixes"],"description":"auto-fix applies each planned patch, runs its revertible_by_test command, and reverts (git checkout HEAD -- file) any fix whose test fails (recording applied vs reverted). commit-fixes then commits and pushes the surviving fixes to the PR branch with pull --rebase retry, or reports no_fixes_to_commit."},{"name":"FASE 5 — Post comment + project status + summary","nodes":["post-comment","project-set-reviewing","print-summary"],"description":"Posts the consolidated, non-blocking review comment (report + auto-fix summary) to the PR, best-effort sets the GitHub Project card to 'Reviewing', and prints the final completion banner with operator next steps. All three use trigger_rule all_done so the run finishes cleanly even when upstream steps were skipped or partially failed."}],"verification_notes":["INDEPENDENT RECOUNT: read the entire 828-line YAML from scratch and extracted node ids via grep -nE '^  - id:' → exactly 20 nodes. node_count = 20 CONFIRMED, matching the first pass. (First pass said 829 lines; the file is 828 lines — trivial off-by-one in prose, no impact on the model.)","First pass was SUBSTANTIALLY CORRECT. No nodes missed, no nodes invented. All 20 nodes, all depends_on lists, all `when` clauses, all trigger_rule values, all is_cancel flags, and all loop {max_iterations:2, until} signals verified line-by-line against the YAML and match exactly.","CORRECTION 1 (is_gate): First pass marked check-prereqs and parse-classify as is_gate=true but left parse-input as is_gate=false. parse-input is structurally identical to check-prereqs — its output VALUE ('OK' vs 'FAIL_BAD_URL') routes the fork between cancel-bad-input (when != 'OK') and check-prereqs (when == 'OK'). Corrected parse-input.is_gate to true for consistency. (Minor; does not change the DAG.)","CORRECTION 2 (edge kind): First pass tagged the project-set-reviewing→print-summary edge as kind 'sequence'. print-summary depends on BOTH post-comment AND project-set-reviewing (line 826) — it is a join of the two FASE-5 branches, so I re-tagged project-set-reviewing→print-summary as 'fan-in' (post-comment→print-summary stays 'sequence' as the primary chain). Cosmetic edge-kind refinement; both edges exist in either model.","EDGE COUNT: 28 total edges. Independently derived: 24 dependency edges + 1 START→parse-input + 3 END edges (cancel-bad-input→END, cancel-bad-prereqs→END, print-summary→END). First pass listed 29 — the difference is solely that I do not duplicate; on careful recount both models contain the identical edge SET. Verified each dependency produces exactly one edge: parse-input(2 children), check-prereqs(2), fetch-pr(1), checkout-branch(1), fetch-diff(1), classify(1), parse-classify(5 fan-out), 5 agents→synthesize(5 fan-in), synthesize(1), auto-fix(1), commit-fixes(1), post-comment(2: project-set-reviewing+print-summary), project-set-reviewing(1: print-summary) = 24 dependency edges.","trigger_rule audit: only FOUR nodes carry explicit trigger_rule: all_done — parse-classify (line 158), synthesize (line 568), commit-fixes (line 737), post-comment (line 780), project-set-reviewing (line 805), print-summary (line 827). Recount: that is SIX nodes with explicit all_done (parse-classify, synthesize, commit-fixes, post-comment, project-set-reviewing, print-summary). All other nodes (incl. auto-fix line 645-718 and ALL 5 agents) have NO trigger_rule → default all_success. First pass captured all six all_done nodes and the all_success defaults correctly.","CANCEL nodes: exactly TWO — cancel-bad-input (line 56, `cancel:` key) and cancel-bad-prereqs (line 72, `cancel:` key), both pre-diff in FASE 0. Confirmed via grep '^    cancel:'. After bootstrap there is NO cancel/abort path — the review is fully informational. First pass correct.","SILENT HOLE CONFIRMED (high-value): synthesize PRODUCES merge-decision.yaml (lines 612-621) and its prompt asserts 'La fase final del workflow lee este artifact y bloquea el merge si blocked=true' — but I grepped every downstream bash node (auto-fix, commit-fixes, post-comment, project-set-reviewing, print-summary) and NONE reads merge-decision.yaml. The merge_blocking machinery (R-DIP cross-agent, F5, F7, F1) is computed and bumped but never enforced; lines 28-29 confirm 'El comment NO bloquea el PR — es informativo.' First pass flagged this correctly.","auto-fix `env:` block (lines 713-716) verified NOT to reach the python3 heredoc subprocess — the inline comment (lines 705-708, run 2484bd91) and the literal '$ARTIFACTS_DIR/...' fallbacks at lines 662/709/710 confirm this is a known footgun the YAML works around via text substitution. First pass note accurate.","Naming collision CONFIRMED: TWO different 'footgun F7' — agent-deha-compliance F7 (line 337) = 'Workflow con start_delay sin eligibility gate' (HIGH merge_blocking); agent-plugin-system F7 (line 473) = 'Frontend block contract' (HIGH merge_blocking). Same label, different agents/meaning. First pass flagged correctly.","test_coverage gating CONFIRMED: agent-test-coverage has `when: $parse-classify.output.test_coverage == 'true'` (line 495) despite 'corré SIEMPRE' (line 132). Relies on parse-classify's jq default `.test_coverage // true` (line 151), which only fires when the key is absent/null — an explicit test_coverage:false would silently skip the agent. First pass note accurate.","Branch strategy CONFIRMED FULL checkout (git fetch origin BRANCH; git checkout BRANCH; git pull --ff-only — lines 95-97; push origin BRANCH — line 731), NOT detached HEAD. Differs from plugin sub-pipelines (project gotcha #9). First pass correct.","Loop retry rationale (max_iterations:2, until:<SIGNAL>) is uniform across classify (CLASSIFY_OK), all 5 agents (REPORT_OK), and synthesize (SYNTH_OK) — 7 loop nodes total. Verified each `until:` value via grep. First pass correct.","FINAL VERDICT: The first pass model is accurate. The only substantive corrections are (1) parse-input.is_gate false→true (consistency with check-prereqs) and (2) the project-set-reviewing→print-summary edge re-tagged sequence→fan-in (it is the join point of FASE 5's two branches). All node attributes, depends_on, when, trigger_rule, is_cancel, and the edge SET are otherwise confirmed correct. node_count = 20 (independently counted)."]},"idea-a-hu-hubara":{"pipeline_id":"idea-a-hu-hubara","title":"idea-a-hu-hubara — IDEA-to-HU front-end (raw idea → GitHub Issue + Project card → optional pipeline launch)","purpose":"Entry-point del pipeline hubara. Toma una idea de negocio en texto libre (o ruta a un .md corto, o texto pegado de un issue) y, en UNA sola pasada de AI (sin loop de refinamiento), la convierte en una HU narrativa de PRODUCTO bien formada (# Título ≤80 chars + Como/Quiero/Para + ## Acceptance criteria Given/When/Then + ## Out of scope + ## Notas técnicas opcional). Valida la estructura del draft determinísticamente, lo persiste a hubara_agency/.hubara/drafts/idea-<ts>.md, publica un Issue en GitHub con label `hubara-hu`, lo agrega al GitHub Project board con status \\\"Idea refined\\\" (si existe .archon/github-project-config.yaml), y presenta UNA única approval gate explícita: ¿lanzar `hu-hubara-pipeline` ahora en background? APROBAR dispara el super-orquestador via nohup/disown fire-and-forget; RECHAZAR termina dejando el Issue + card listos para lanzar a mano. NO produce el refinement TÉCNICO (14 secciones + §0 plugin classification) — eso lo hace el pipeline real (hu-hubara-pipeline). Este workflow solo produce la HU narrativa de producto. worktree.enabled: false — no crea branch ni HU_ID; el único side-effect en el working tree es save-draft.","trigger":"archon workflow run idea-a-hu-hubara \\\"<idea>\\\"  — donde <idea> ($ARGUMENTS / $USER_MESSAGE) es: (a) texto libre de una idea de feature, (b) ruta a un .md con notas más extensas (detectado con `[ -f \\\"$RAW\\\" ]` y copiado), o (c) texto pegado de un issue existente (tratado como text plain). Sin flags. provider: claude, model: sonnet, interactive: true (tiene approval gate), worktree.enabled: false.","node_count":14,"inputs":["$ARGUMENTS / $USER_MESSAGE — la idea cruda (texto libre, ruta a .md corto, o texto de issue pegado)","$ARTIFACTS_DIR — workspace efímero del run (env var real para bash + substituido como literal text)",".archon/github-project-config.yaml — OPCIONAL; si existe habilita Project sync (project_number, project_owner, project_id, status_field_id, status_options con 'Idea refined'). Leído por check-prereqs (smoke test) y agregar-a-project","gh CLI autenticado con scope project + read:project (validado por check-prereqs via gh project item-list smoke test)","jq disponible en PATH (validado por check-prereqs con command -v jq)","$ARTIFACTS_DIR/idea-original.md — escrito por normalize-input, leído por refinar-hu-producto","$ARTIFACTS_DIR/hu-draft.md — escrito por refinar-hu-producto (Write), leído por validate-hu/save-draft/crear-issue","$ARTIFACTS_DIR/.issue-url — escrito por crear-issue, leído por agregar-a-project/print-issue-info/lanzar-pipeline/print-final-summary (RESUME-SAFE: $create-issue.output se pierde tras la approval)"],"global_logic":"MODE DETECTION: este workflow NO clasifica single vs multi_plugin (eso ocurre downstream en hubara-tech-refiner-archon §0 dentro de hu-hubara-pipeline). Su único \\\"modo\\\" interno es input-type detection en normalize-input: si `[ -f \\\"$RAW\\\" ]` copia el archivo, si no hace echo del texto a idea-original.md. KEY ENV VARS / REFS: $ARGUMENTS (la idea), $ARTIFACTS_DIR (workspace efímero; env var real en bash Y literal substituido), $create-issue.output (la URL del issue — usada en el approval message y como fallback), $<node>.output refs en los 6 when conditions. NO usa HU_ID, BRANCH ni WORKFLOW_ID — worktree.enabled: false; el HU_ID/branch los crea el pipeline downstream. BRANCH STRATEGY: ninguna — no hace checkout/detach/push; el único write al tree es save-draft (hubara_agency/.hubara/drafts/, NO commitea). RUN-WIDE INVARIANTS: (1) RESUME-SAFETY — la URL del issue se persiste a $ARTIFACTS_DIR/.issue-url porque $create-issue.output se PIERDE cuando Archon reanuda tras la approval gate (bug documentado en líneas 289-291: prior_success skipea el nodo pero no preserva stdout). Los 4 nodos post-crear-issue leen el archivo con fallback a $create-issue.output. (2) FAIL-SOFT en Project sync — agregar-a-project y print-issue-info casi nunca tumban el run (mayoría exit 0); ÚNICA excepción exit 1 = FAIL_GH_SCOPE en agregar-a-project. (3) UNA ÚNICA approval explícita (gate-lanzar-pipeline); el resto es auto. (4) 3 cancel nodes usan when negativo (output != valor-OK) y abortan con diagnóstico. (5) idempotencia GitHub — gh label create silenciado con `|| true`, item-add tolera 'already/exists/duplicate', item-list con loop de 3 reintentos para el race con auto-add. (6) PATRÓN DE EXIT CODES MIXTO: los validadores (check-prereqs, normalize-input, validate-hu) usan exit 0 + código-en-stdout → routing por VALOR de output (cancel nodes disparan por when); pero crear-issue (FATAL), agregar-a-project (solo FAIL_GH_SCOPE) y lanzar-pipeline (FAIL_NO_ISSUE_URL/FAIL_INVALID_URL) usan exit 1 → propagan como node_error y, al NO tener cancel node dedicado, cortan la cadena downstream por all_success.","nodes":[{"id":"check-prereqs","type":"bash","summary":"Valida pre-requisitos de runtime: gh auth, jq, y scope project del GitHub Project (smoke test).","detail":"`set -e`. Corre `gh auth status` (redirigido) y si falla emite `FAIL_GH_AUTH` + exit 0. Verifica `command -v jq`, si no emite `FAIL_NO_JQ` + exit 0. Si existe `.archon/github-project-config.yaml`, parsea project_number (PN) y project_owner (PO) con grep+awk y hace un SMOKE TEST real: `gh project item-list $PN --owner $PO --format json --limit 1`; si falla emite `FAIL_GH_NO_PROJECT_SCOPE` + exit 0 (comentario: smoke test > parsear gh auth status que cachea scopes). Si todo pasa emite `OK`. timeout 30000.","depends_on":[],"trigger_rule":"all_success","when":"","produces":"output in {OK, FAIL_GH_AUTH, FAIL_NO_JQ, FAIL_GH_NO_PROJECT_SCOPE}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"GATE de valor: su output (OK vs FAIL_*) rutea normalize-input (continue) vs cancel-bad-prereqs (cancel). Todos los paths de fallo usan exit 0 — routing por VALOR, no por exit code. El smoke-test de project scope SOLO corre si el config existe Y tiene PN y PO no vacíos; si no, el scope project NUNCA se valida (silent hole leve: un repo sin config no chequea scope, pero agregar-a-project skipea limpio si no hay config). README §7 menciona FAIL_MISSING_*/FAIL_DIRTY_PROTECTED_FILES para check-prereqs, pero esos NO existen en este YAML (son de hu-hubara-pipeline; doc-vs-YAML mismatch). Nodo de entrada (depends_on vacío)."},{"id":"cancel-bad-prereqs","type":"manual","summary":"Cancela el run con diagnóstico si check-prereqs no emitió OK.","detail":"Nodo `cancel:` (aborta el run). El mensaje incluye `$check-prereqs.output` y una tabla de diagnóstico: FAIL_GH_AUTH → `gh auth login`; FAIL_NO_JQ → `brew install jq`; FAIL_GH_NO_PROJECT_SCOPE → `gh auth refresh -s project,read:project`. Solo dispara cuando el when es true.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output != 'OK'","produces":"cancela el run (sin output downstream)","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"trigger_rule default all_success — check-prereqs siempre termina con exit 0 (success) en todos sus paths de fallo, así que el cancel dispara correctamente vía el when. Fail-closed: solo aborta si output != 'OK'. Terminal → edge a END."},{"id":"normalize-input","type":"bash","summary":"Normaliza la idea cruda ($ARGUMENTS) a $ARTIFACTS_DIR/idea-original.md con guards de vacío y tamaño mínimo (≥20 chars).","detail":"Lee `RAW=\"$ARGUMENTS\"`. Guard 1: trimea whitespace (`tr -d '[:space:]'`); si queda vacío emite `FAIL_EMPTY_INPUT` + exit 0. Si `[ -f \"$RAW\" ]` copia ese archivo a idea-original.md; si no, hace `echo \"$RAW\" >` idea-original.md. Guard 2: mide `SIZE=$(wc -c < idea-original.md)`; si SIZE < 20 emite `FAIL_TOO_SHORT_${SIZE}` + exit 0. Si todo pasa emite `OK`. timeout 30000.","depends_on":["check-prereqs"],"trigger_rule":"all_success","when":"$check-prereqs.output == 'OK'","produces":"output in {OK, FAIL_EMPTY_INPUT, FAIL_TOO_SHORT_<N>}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"GATE de valor: su output (OK vs FAIL_*) rutea refinar-hu-producto (continue) vs cancel-bad-input (cancel). Gated además por when sobre check-prereqs.output == 'OK' (redundante con dependencia + cancel-bad-prereqs, pero fail-closed). NO tiene `set -e` (a diferencia de otros bash nodes), pero usa exit 0 explícito en cada guard. El umbral de 20 chars aquí es distinto del de >200 bytes que aplica validate-hu al draft."},{"id":"cancel-bad-input","type":"manual","summary":"Cancela el run si la idea normalizada fue inválida (vacía o <20 chars).","detail":"Nodo `cancel:` (aborta). Mensaje: 'Input inválido: $normalize-input.output. Pasame una idea no vacía (mínimo 20 chars): archon workflow run idea-a-hu-hubara ...'. Dispara solo si when true.","depends_on":["normalize-input"],"trigger_rule":"all_success","when":"$normalize-input.output != 'OK'","produces":"cancela el run","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"normalize-input siempre exit 0 → success → all_success satisface → el when discrimina. Fail-closed. Terminal → edge a END."},{"id":"refinar-hu-producto","type":"command","summary":"Nodo AI (prompt LLM) que genera la HU narrativa en UNA sola pasada y la escribe a $ARTIFACTS_DIR/hu-draft.md.","detail":"Nodo de prompt LLM (NO bash, NO skill): tiene `prompt:`, `allowed_tools: [Read, Write]`, `idle_timeout: 600000` (10 min). El prompt instruye actuar como product owner de AgencyHubara: Paso 1 lee idea-original.md (opcionalmente hubara-architecture-guide SKILL.md + sections/01-general.md si la idea menciona plugin/agente/Temporal); Paso 2 escribe hu-draft.md con estructura EXACTA (# Título ≤80 chars sin punto; Como/Quiero/Para; ## Acceptance criteria 3-5 bullets Given/When/Then; ## Out of scope ≥2 items; ## Notas técnicas opcional); Paso 3 reglas de calidad: título accionable, narrativa completa, AC empiezan con Given, out-of-scope ≥2, doc >200 bytes, INFERIR lo que falte (NO preguntar al humano, NO usar placeholders <TBD>). Termina al escribir el archivo — NO emite promise.","depends_on":["normalize-input"],"trigger_rule":"all_success","when":"$normalize-input.output == 'OK'","produces":"$ARTIFACTS_DIR/hu-draft.md (efecto secundario vía Write); NO emite output estructurado ni promise","invokes":"(prompt inline — NO invoca el skill hubara-tech-refiner-archon; ver notes)","loop":"","is_gate":false,"is_cancel":false,"notes":"GOTCHA explícito en el YAML (líneas 132-136): este nodo NO usa el skill hubara-tech-refiner-archon — ese skill produce el refinement TÉCNICO (14 secciones + §0 plugin classification). Este nodo solo produce la HU narrativa de producto. UNA pasada, SIN loop (no hay loop: block, ni max_iterations, ni gate_message — explícito en línea 207 'no necesitás emitir promise'). Diseño deliberado: si el draft queda mal el operador (a) rechaza gate-lanzar-pipeline, (b) edita el issue en GitHub, o (c) borra el issue y re-corre. Tipado 'command' por falta de mejor encaje (es un nodo prompt/AI puro con allowed_tools, NO un .archon/commands/<name>.md ni un .claude/skills). Su output NO se valida por valor — la validación es validate-hu sobre el archivo escrito. RIESGO SILENT-HOLE: si el AI no escribe el archivo / hace timeout / escribe basura, no hay promise ni signal que lo detecte aquí; recae 100% en validate-hu — pero validate-hu depende con all_success, así que si este nodo termina en node_error/timeout, validate-hu NO corre y NO hay cancel con all_done que capture ese caso (run colgado/fallado sin diagnóstico)."},{"id":"validate-hu","type":"bash","summary":"Valida estructuralmente $ARTIFACTS_DIR/hu-draft.md (existe, >200 bytes, título, narrativa, ≥2 AC Given, sección scope).","detail":"Early-exit emitiendo un código por cada fallo: `test -f` → FAIL_NOT_EXISTS; `wc -c > 200` → FAIL_TOO_SHORT; `grep -qE '^#\\s+\\S'` → FAIL_NO_TITLE; `grep -qiE 'como\\s+'` → FAIL_NO_NARRATIVE; cuenta bullets Given (`grep -cE '^[[:space:]-]*\\*?\\*?Given'`, default 0); si <2 → FAIL_FEW_AC_$ac_count; `grep -qiE '^##.*scope'` → FAIL_NO_SCOPE_SECTION. Si todo pasa emite `PASS`. timeout 10000. NO tiene when (corre siempre que refinar-hu-producto haya tenido success).","depends_on":["refinar-hu-producto"],"trigger_rule":"all_success","when":"","produces":"output in {PASS, FAIL_NOT_EXISTS, FAIL_TOO_SHORT, FAIL_NO_TITLE, FAIL_NO_NARRATIVE, FAIL_FEW_AC_<N>, FAIL_NO_SCOPE_SECTION}","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"GATE estructural cuyo VALOR (PASS vs FAIL_*) rutea save-draft (continue) vs cancel-bad-hu (cancel). Todos los fallos hacen exit 0 → siempre success → routing por valor. La validación de narrativa busca literal 'como' case-insensitive en cualquier parte (no ancla a la línea narrativa) — un 'como' en otro contexto la satisface (silent hole leve). El conteo de AC busca Given con prefijo opcional de espacios/guion/asteriscos — alineado con el formato `- **Given**` del prompt. NO tiene when (confirmado líneas 213-227): es la única red de seguridad para un draft AI defectuoso, pero solo corre si refinar-hu-producto terminó success (all_success)."},{"id":"cancel-bad-hu","type":"manual","summary":"Cancela el run si la validación estructural del draft no dio PASS.","detail":"Nodo `cancel:` (aborta). Mensaje: 'HU draft validation FAILED: $validate-hu.output. Re-lanzá: archon workflow run idea-a-hu-hubara ...'. Dispara solo si when true.","depends_on":["validate-hu"],"trigger_rule":"all_success","when":"$validate-hu.output != 'PASS'","produces":"cancela el run","invokes":"","loop":"","is_gate":false,"is_cancel":true,"notes":"validate-hu siempre exit 0 → success → el when discrimina PASS vs FAIL. Fail-closed. Terminal → edge a END."},{"id":"save-draft","type":"bash","summary":"Persiste el draft validado a hubara_agency/.hubara/drafts/idea-<timestamp>.md (siempre, antes de publicar).","detail":"`set -e`. Crea el dir `hubara_agency/.hubara/drafts` con mkdir -p. Construye `DRAFT_PATH=hubara_agency/.hubara/drafts/idea-$(date -u +%Y%m%d-%H%M%S).md` y copia hu-draft.md ahí. Emite la ruta del draft (DRAFT_PATH) a stdout.","depends_on":["validate-hu"],"trigger_rule":"all_success","when":"$validate-hu.output == 'PASS'","produces":"output = ruta del draft persistido (hubara_agency/.hubara/drafts/idea-<ts>.md)","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Escribe en el working tree directamente (worktree.enabled: false). El draft NO se commitea por este workflow (solo lo deja en el tree). when redundante con la dependencia + cancel-bad-hu pero fail-closed. Comparte dependencia (validate-hu) con cancel-bad-hu — son ramas mutuamente excluyentes por when (PASS vs !=PASS). ÚNICO side-effect del workflow en el tree."},{"id":"crear-issue","type":"bash","summary":"Crea el Issue en GitHub con label hubara-hu, parsea la URL y la persiste a $ARTIFACTS_DIR/.issue-url.","detail":"`set -e`. Extrae TITLE de la primera línea `# ` del draft (`grep -m1 '^# ' | sed 's/^# //' | head -c 120`); si vacío → `FATAL: no extrajo título` + exit 1. Crea la label `hubara-hu` (color 0E8A16) de forma idempotente (`gh label create ... || true`, log a .gh-label.log). Corre `gh issue create --title --body-file hu-draft.md --label hubara-hu` (log a .gh-create.log); si RC != 0 → FATAL + tail log + exit 1. Parsea ISSUE_URL del log con `grep -oE 'https://github\\.com/[^ ]+/issues/[0-9]+'`; si vacío → FATAL + exit 1. CRÍTICO: persiste la URL a $ARTIFACTS_DIR/.issue-url (sobrevive al resume tras approval) y la emite a stdout. retry: max_attempts 2, delay_ms 5000, on_error transient. timeout 60000.","depends_on":["save-draft"],"trigger_rule":"all_success","when":"","produces":"output = ISSUE_URL (https://github.com/.../issues/N); side-effects: $ARTIFACTS_DIR/.issue-url, Issue en GitHub con label hubara-hu","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"ÚNICO nodo con retry block (transient, 2 intentos). Los fallos aquí SÍ usan exit 1 (FATAL) — distinto del patrón exit-0-con-código de los validadores; un FATAL propaga como node_error y (sin cancel node propio) cortaría la cadena downstream por all_success dejando el run fallado aquí sin diagnóstico de cancel. GOTCHA documentado (líneas 289-291): $create-issue.output se PIERDE en el resume post-approval (prior_success skipea el nodo pero no preserva stdout) — por eso la URL se persiste a archivo y todos los downstream leen el archivo con fallback a $create-issue.output. La label create silenciada con `|| true`. El --label hubara-hu puede disparar un auto-add workflow del Project (race con agregar-a-project, manejado con el loop de 3 reintentos allí). FAN-OUT: tiene 2 dependientes (agregar-a-project + print-issue-info)."},{"id":"agregar-a-project","type":"bash","summary":"Agrega el issue al GitHub Project board y le setea status 'Idea refined' (fail-soft, skip si no hay config).","detail":"Si NO existe .archon/github-project-config.yaml → `echo skipped no_config` + exit 0. Lee ISSUE_URL de $ARTIFACTS_DIR/.issue-url (fallback a $create-issue.output); si vacío → WARN + exit 0. Parsea PN/PO/PID/FID del config. Step 1: `gh project item-add` (log .add.log); si RC!=0 y log matchea 'missing required scopes|read:project|gh auth refresh' → FAIL_GH_SCOPE + exit 1; si matchea 'already|exists|duplicate' → 'item ya estaba'; else WARN. Step 2: encuentra ITEM_ID con loop de 3 intentos (`gh project item-list ... | jq` filtrando por content.url, sleep attempt*2, race con auto-add); si no lo encuentra → WARN + exit 0. Step 3: extrae OID (option_id de 'Idea refined') del config con awk; si vacío → WARN + exit 0. Step 4: `gh project item-edit --single-select-option-id $OID` (log .edit.log); si RC!=0 → FAIL item-edit + tail + exit 0 (fail-soft). Si OK emite '✅ status Idea refined set'. timeout 90000.","depends_on":["crear-issue"],"trigger_rule":"all_success","when":"","produces":"output in {skipped no_config, WARN ..., item-add OK, item ya estaba en project, FAIL_GH_SCOPE: ..., item_id=..., option_id=..., ✅ status 'Idea refined' set ...} (informativo)","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"FAIL-SOFT por diseño: casi todos los paths hacen exit 0 (no rompen el run). ÚNICA excepción: FAIL_GH_SCOPE hace exit 1 — propagaría como node_error y cortaría la cadena por all_success (no hay cancel node que lo capture; el run quedaría fallado aquí Y print-issue-info NO correría porque su trigger_rule none_failed_min_one_success exige ningún dep fallido). El output se referencia en print-issue-info (PROJECT_STATUS=$agregar-a-project.output) pero NO sobrevive al resume post-approval igual que crear-issue.output (no se persiste a archivo) — seguro solo porque print-issue-info corre PRE-approval. El loop interno de 3 intentos maneja el race con el auto-add del Project disparado por la label."},{"id":"print-issue-info","type":"bash","summary":"Imprime un banner con la URL del issue y el status del Project antes de la approval gate.","detail":"Lee ISSUE_URL de $ARTIFACTS_DIR/.issue-url (fallback a $create-issue.output). Toma PROJECT_STATUS=$agregar-a-project.output. Imprime un banner ASCII (heredoc) con '✅ Issue creado', la URL, el Project status, y la lista de fases que recorrerá hu-hubara-pipeline (Refining → Refined → Planning → Planned → Implementing → Reviewing → Done). timeout 5000.","depends_on":["crear-issue","agregar-a-project"],"trigger_rule":"none_failed_min_one_success","when":"","produces":"banner informativo a stdout (no estructurado)","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"ÚNICO nodo con trigger_rule explícito y NO estándar: `none_failed_min_one_success` (línea 404 — NO pertenece al set base all_success/all_done/one_success del modelo; variante Archon: ningún dep falló Y al menos uno tuvo éxito). Le permite correr aunque agregar-a-project haya hecho exit 0 con WARN/skip, pero NO si crear-issue o agregar-a-project fallaron (exit!=0). FAN-IN de 2 deps (crear-issue + agregar-a-project). PROJECT_STATUS puede venir vacío en resume post-approval (no persistido) — pero este nodo corre PRE-approval así que normalmente está poblado. Si agregar-a-project hizo FAIL_GH_SCOPE (exit 1), none_failed es false → print-issue-info NO corre → cadena cortada antes del gate."},{"id":"gate-lanzar-pipeline","type":"manual","summary":"ÚNICA approval gate del workflow: ¿lanzar hu-hubara-pipeline ahora en background?","detail":"Nodo `approval:` con `message:` largo (líneas 413-437). Muestra: Issue publicado ($create-issue.output), Card en 'Idea refined', y la decisión. APROBAR → arranca hu-hubara-pipeline en background (~$0.50-1.00, 20-40 min) describiendo los 7 pasos (refinar técnico → plan plugin-level → single inline / multi fan-out → implementar con gates determinísticos → validación final + PR único → review 5 agentes → Project sync). RECHAZAR → termina; el comando manual queda impreso: `archon workflow run hu-hubara-pipeline \"$create-issue.output\"`.","depends_on":["print-issue-info"],"trigger_rule":"all_success","when":"","produces":"decisión humana approve/reject que rutea lanzar-pipeline (approve → corre; reject → run termina sin banner final)","invokes":"","loop":"","is_gate":true,"is_cancel":false,"notes":"is_gate=true (nodo approval). El message referencia $create-issue.output (la URL) — se preserva en el message porque se renderiza ANTES del resume; pero el RESUME del nodo lanzar-pipeline ya NO puede confiar en $create-issue.output (de ahí la lectura desde .issue-url). Esta es la única approval; todo lo anterior es auto. Si el operador rechaza, lanzar-pipeline (su único downstream) NO corre por all_success y el run termina. Gate de routing humano: el VALOR (approve/reject) decide continue vs stop. No tiene when."},{"id":"lanzar-pipeline","type":"bash","summary":"Dispara hu-hubara-pipeline en background vía nohup/disown con la URL del issue (resume-safe, fire-and-forget).","detail":"Lee ISSUE_URL de $ARTIFACTS_DIR/.issue-url (fallback a $create-issue.output, comentario: sobrevive al resume tras approval). Si vacío → FAIL_NO_ISSUE_URL + exit 1. Valida la URL con `grep -qE '^https://github\\.com/.+/issues/[0-9]+'`; si no matchea → FAIL_INVALID_URL + exit 1. Crea LOG_FILE=$HOME/.archon/logs/hubara-pipeline-<epoch>.log (mkdir -p). Lanza en background: `(env -u CLAUDECODE ARCHON_SUPPRESS_NESTED_CLAUDE_WARNING=1 nohup archon workflow run hu-hubara-pipeline \"$ISSUE_URL\" > LOG_FILE 2>&1 & disown) 2>/dev/null` y emite `pipeline_triggered_log=$LOG_FILE` (&&) o `pipeline_trigger_failed_run_manually` (||). timeout 10000.","depends_on":["gate-lanzar-pipeline"],"trigger_rule":"all_success","when":"","produces":"output in {pipeline_triggered_log=<path>, pipeline_trigger_failed_run_manually, FAIL_NO_ISSUE_URL ..., FAIL_INVALID_URL: ...}","invokes":"archon workflow run hu-hubara-pipeline (sub-workflow fire-and-forget en background via nohup/disown — NO es una sub-pipeline invocation gestionada por Archon; es un proceso detached que NO se espera ni se mergea)","loop":"","is_gate":false,"is_cancel":false,"notes":"`env -u CLAUDECODE` + ARCHON_SUPPRESS_NESTED_CLAUDE_WARNING=1 evita el 'may hang silently' del nested archon-in-claude-code. Fire-and-forget: NO espera ni mergea resultado del pipeline lanzado (a diferencia de la rama-A inline del pipeline real). Los FAIL_* aquí usan exit 1 (FATAL) → si la URL falta/es inválida, propaga node_error y print-final-summary (all_success) NO corre. El éxito del background nunca es exit!=0 por el `&& ... || echo ...` (captura el resultado del subshell). Smart-resume del propio idea-a-hu-hubara: si se re-corre, este nodo ya estaría en prior_success y se skipearía (de ahí la dependencia en .issue-url y no en $create-issue.output). No tiene when."},{"id":"print-final-summary","type":"bash","summary":"Imprime el banner final del workflow con la URL del issue y el estado del lanzamiento del pipeline.","detail":"Lee ISSUE_URL de $ARTIFACTS_DIR/.issue-url (fallback a $create-issue.output). Toma PIPELINE=$lanzar-pipeline.output. Imprime banner ASCII (heredoc) '🎉 idea-a-hu-hubara completo' con Issue, Pipeline status, y próximos pasos (mirar Project board, esperar status auto, ver PR+comment del review, y el comando manual de fallback si fue pipeline_trigger_failed_run_manually). timeout 5000.","depends_on":["lanzar-pipeline"],"trigger_rule":"all_success","when":"","produces":"banner final informativo a stdout","invokes":"","loop":"","is_gate":false,"is_cancel":false,"notes":"Nodo TERMINAL (ningún nodo depende de él) → edge a END. Solo corre si lanzar-pipeline tuvo éxito (all_success); si lanzar-pipeline emitió FAIL_* con exit 1, este nodo NO corre. Si el operador rechazó la approval, este nodo NO corre (lanzar-pipeline no corrió). Banner cosmético sin efectos. No tiene when."}],"edges":[{"from":"START","to":"check-prereqs","kind":"sequence","condition":""},{"from":"check-prereqs","to":"cancel-bad-prereqs","kind":"cancel","condition":"$check-prereqs.output != 'OK'"},{"from":"check-prereqs","to":"normalize-input","kind":"gate","condition":"$check-prereqs.output == 'OK'"},{"from":"normalize-input","to":"cancel-bad-input","kind":"cancel","condition":"$normalize-input.output != 'OK'"},{"from":"normalize-input","to":"refinar-hu-producto","kind":"gate","condition":"$normalize-input.output == 'OK'"},{"from":"refinar-hu-producto","to":"validate-hu","kind":"sequence","condition":""},{"from":"validate-hu","to":"cancel-bad-hu","kind":"cancel","condition":"$validate-hu.output != 'PASS'"},{"from":"validate-hu","to":"save-draft","kind":"gate","condition":"$validate-hu.output == 'PASS'"},{"from":"save-draft","to":"crear-issue","kind":"sequence","condition":""},{"from":"crear-issue","to":"agregar-a-project","kind":"fan-out","condition":""},{"from":"crear-issue","to":"print-issue-info","kind":"fan-out","condition":""},{"from":"agregar-a-project","to":"print-issue-info","kind":"fan-in","condition":""},{"from":"print-issue-info","to":"gate-lanzar-pipeline","kind":"sequence","condition":""},{"from":"gate-lanzar-pipeline","to":"lanzar-pipeline","kind":"gate","condition":""},{"from":"lanzar-pipeline","to":"print-final-summary","kind":"sequence","condition":""},{"from":"print-final-summary","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-prereqs","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-input","to":"END","kind":"sequence","condition":""},{"from":"cancel-bad-hu","to":"END","kind":"sequence","condition":""}],"phases":[{"name":"prereqs","nodes":["check-prereqs","cancel-bad-prereqs"],"description":"FASE 0 del YAML — Pre-requisitos básicos. check-prereqs valida gh auth, jq, y (si hay config) el scope project del GitHub Project mediante un smoke test real contra el API. Es un gate de valor: cancel-bad-prereqs aborta con diagnóstico si output != OK."},{"name":"normalize-input","nodes":["normalize-input","cancel-bad-input"],"description":"FASE 1 del YAML — Normalizar input. normalize-input convierte la idea cruda ($ARGUMENTS, texto o ruta a .md) en $ARTIFACTS_DIR/idea-original.md con guards de vacío y tamaño mínimo (≥20 chars). Gate de valor: cancel-bad-input aborta si output != OK."},{"name":"refine","nodes":["refinar-hu-producto"],"description":"FASE 2 del YAML — Refinar la idea a HU narrativa en UNA sola pasada de AI (prompt LLM con allowed_tools [Read, Write], sin loop, sin gate_message). Produce $ARTIFACTS_DIR/hu-draft.md. Explícitamente NO usa el skill hubara-tech-refiner-archon (no es el refinement técnico de 14 secciones)."},{"name":"validate-draft","nodes":["validate-hu","cancel-bad-hu"],"description":"FASE 3 del YAML — Validar estructura del HU draft. validate-hu es un gate determinístico (existencia, >200 bytes, título, narrativa 'como', ≥2 AC Given, sección scope). cancel-bad-hu aborta si output != PASS (la única red de seguridad para un draft AI defectuoso, pero solo si refinar-hu-producto terminó success)."},{"name":"persist-draft","nodes":["save-draft"],"description":"FASE 4 del YAML — Persistir el draft validado a hubara_agency/.hubara/drafts/idea-<ts>.md (siempre, antes de publicar). Único side-effect del workflow en el working tree; no commitea."},{"name":"publish-issue","nodes":["crear-issue","agregar-a-project","print-issue-info"],"description":"FASE 5 del YAML — Publicar Issue + agregar al Project (auto, sin gate). crear-issue publica el Issue con label hubara-hu y persiste la URL a .issue-url (resume-safe, con retry); fan-out a agregar-a-project (lo agrega al board con status 'Idea refined', fail-soft) y print-issue-info (fan-in de ambos con trigger_rule none_failed_min_one_success, muestra el banner pre-gate)."},{"name":"launch-gate","nodes":["gate-lanzar-pipeline","lanzar-pipeline","print-final-summary"],"description":"FASE 6 del YAML — Gate ÚNICO + lanzamiento. gate-lanzar-pipeline es la única approval explícita; si se aprueba, lanzar-pipeline dispara hu-hubara-pipeline en background (nohup/disown, env -u CLAUDECODE, fire-and-forget) leyendo la URL desde .issue-url; print-final-summary imprime el banner final. Si se rechaza, el run termina dejando Issue + card listos."}],"purpose_verification_placeholder":"unused","verification_notes":["AUTHORITATIVE COUNT (independiente, desde cero): leí el archivo COMPLETO (496 líneas, single Read) + dos greps de cross-check de keys estructurales. El YAML define EXACTAMENTE 14 nodos (un `- id:` cada uno): check-prereqs(53), cancel-bad-prereqs(76), normalize-input(92), cancel-bad-input(118), refinar-hu-producto(138), validate-hu(213), cancel-bad-hu(229), save-draft(238), crear-issue(252), agregar-a-project(301), print-issue-info(381), gate-lanzar-pipeline(411), lanzar-pipeline(439), print-final-summary(469). node_count=14, nodes[]=14. CONFIRMADO: el first pass acertó node_count=14, NO inventó nodos, NO faltó ninguno.","Tipos de nodo (independiente): 9 bash (check-prereqs, normalize-input, validate-hu, save-draft, crear-issue, agregar-a-project, print-issue-info, lanzar-pipeline, print-final-summary), 3 cancel (cancel-bad-prereqs/input/hu → mapeados a type 'manual' is_cancel=true), 1 approval (gate-lanzar-pipeline → 'manual' is_gate=true), 1 prompt/AI (refinar-hu-producto, con `prompt:` + `allowed_tools:[Read,Write]` + `idle_timeout:600000` → mapeado a 'command' por ser nodo prompt puro, NO un .archon/commands/ ni un skill). 9+3+1+1=14. CORRIJO una incoherencia interna de la verification del first pass: dijo '8 bash' en una nota y '9 bash' en otra — el conteo CORRECTO es 9 bash.","DISCREPANCIA CORREGIDA #1 (is_gate): el first pass marcó SOLO validate-hu como is_gate=true. Pero check-prereqs y normalize-input son ESTRUCTURALMENTE el mismo gate de valor — su output (OK vs FAIL_*) rutea continue (downstream) vs cancel (cancel-bad-prereqs / cancel-bad-input), exactamente la definición del modelo ('GATE node: its output VALUE routes the downstream chain continue vs cancel'). CORREGIDO: marqué los TRES (check-prereqs, normalize-input, validate-hu) como is_gate=true para coherencia. El first pass era inconsistente al flaggear solo uno de los tres gates idénticos.","DISCREPANCIA CORREGIDA #2 (edge kinds en el fan-out/fan-in de crear-issue→print-issue-info): el first pass marcó crear-issue→agregar-a-project como 'sequence' y crear-issue→print-issue-info como 'sequence', con solo agregar-a-project→print-issue-info como 'fan-in'. CORREGIDO: crear-issue tiene 2 dependientes (fan-out), así que crear-issue→agregar-a-project y crear-issue→print-issue-info son ambos kind 'fan-out'; print-issue-info tiene 2 deps (fan-in), así que crear-issue→print-issue-info y agregar-a-project→print-issue-info convergen — marqué agregar-a-project→print-issue-info como 'fan-in'. El conteo total de edges NO cambia.","when conditions VERBATIM verificados línea por línea (vía grep): SOLO 6 nodos tienen when — cancel-bad-prereqs(85) '$check-prereqs.output != \\'OK\\''; normalize-input(115) '$check-prereqs.output == \\'OK\\''; cancel-bad-input(120) '$normalize-input.output != \\'OK\\''; refinar-hu-producto(140) '$normalize-input.output == \\'OK\\''; cancel-bad-hu(231) '$validate-hu.output != \\'PASS\\''; save-draft(245) '$validate-hu.output == \\'PASS\\''. Los otros 8 nodos (check-prereqs, validate-hu, crear-issue, agregar-a-project, print-issue-info, gate-lanzar-pipeline, lanzar-pipeline, print-final-summary) NO tienen when. CONFIRMADO: el first pass acertó TODOS los when, incluyendo el sutil — validate-hu NO tiene when (correctamente vacío).","trigger_rule verificado (vía grep): SOLO print-issue-info tiene trigger_rule explícito = 'none_failed_min_one_success' (línea 404, variante Archon fuera del set base all_success/all_done/one_success). Los otros 13 nodos lo omiten → default all_success. CONFIRMADO: el first pass acertó esto exactamente, reportando el valor verbatim del YAML en vez de forzarlo al enum base.","depends_on verificado (vía grep, línea por línea): check-prereqs []; cancel-bad-prereqs/normalize-input/refinar-hu-producto [check-prereqs|normalize-input]; cancel-bad-input [normalize-input]; validate-hu [refinar-hu-producto]; cancel-bad-hu/save-draft [validate-hu]; crear-issue [save-draft]; agregar-a-project [crear-issue]; print-issue-info [crear-issue, agregar-a-project]; gate-lanzar-pipeline [print-issue-info]; lanzar-pipeline [gate-lanzar-pipeline]; print-final-summary [lanzar-pipeline]. CONFIRMADO: TODOS los depends_on del first pass son correctos, incluyendo el único multi-dep (print-issue-info con 2).","is_cancel verificado: 3 nodos con `cancel:` key = cancel-bad-prereqs(77), cancel-bad-input(119), cancel-bad-hu(230). CONFIRMADO: first pass correcto. is_gate: gate-lanzar-pipeline (approval, 413) correcto; + las 3 correcciones de gate de valor arriba (#1).","Edges: 19 totales (14 dependency edges + 1 START→check-prereqs + 4 →END: print-final-summary terminal + los 3 cancel nodes terminales). CONFIRMADO: el first pass tenía 19 edges con el mismo grafo exacto. Las ÚNICAS diferencias son las 2 re-etiquetas de kind del fan-out de crear-issue (discrepancia #2); ningún edge faltante ni inventado, ningún from/to incorrecto, ninguna condition incorrecta.","Branch/HU_ID: CONFIRMADO worktree.enabled:false (línea 46) — el workflow NO usa HU_ID/BRANCH/WORKFLOW_ID, NO hace checkout/detach/push. Único write al tree: save-draft → hubara_agency/.hubara/drafts/. El HU_ID y branch hu/<HU_ID> los crea el pipeline downstream. First pass correcto.","Loops: CONFIRMADO ningún nodo tiene `loop:` block. refinar-hu-producto es one-shot (línea 207: 'no necesitás emitir promise'; sin max_iterations/until/gate_message). First pass correcto.","GOTCHAS confirmados del YAML (relevantes para entender el grafo, ambos capturados correctamente por el first pass): (a) resume-safety líneas 289-291 — $create-issue.output se pierde tras la approval, por eso .issue-url se persiste a archivo y 4 downstream leen con fallback; (b) líneas 132-136 — refinar-hu-producto explícitamente NO es el skill hubara-tech-refiner-archon (solo HU narrativa, no refinement técnico).","RIESGOS SILENT-HOLE confirmados (ambos ya notados por el first pass, los mantengo): (1) si refinar-hu-producto (AI) termina en node_error/timeout, validate-hu (all_success) NO corre y NO hay cancel con all_done → run colgado sin diagnóstico. (2) FAIL_GH_SCOPE en agregar-a-project (exit 1) propaga como node_error sin cancel dedicado → corta la cadena (y none_failed_min_one_success de print-issue-info pasa a false). (3) check-prereqs solo valida project scope si el config existe con PN+PO no vacíos.","VEREDICTO: el first pass estaba SUSTANCIALMENTE CORRECTO en la espina dorsal (14 nodos, todos los depends_on, todos los when verbatim incl. el sutil validate-hu sin when, el trigger_rule no estándar, is_cancel, los 19 edges con el grafo correcto). Las ÚNICAS correcciones aplicadas: (#1) marqué check-prereqs y normalize-input también como is_gate=true para coherencia con validate-hu (los tres son gates de valor idénticos); (#2) re-etiqueté los 2 edges salientes de crear-issue como fan-out (tiene 2 dependientes). Ninguna corrección afecta node_count (14), el conteo de edges (19), ni la topología."]}};
