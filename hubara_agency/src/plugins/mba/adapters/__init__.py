"""Adapters HTTP del plugin ``mba`` contra la Meta Business Agent Cloud API
(host único en ``meta_api.MBA_API_BASE_URL``). Vendor aislado detrás de ports (R-DIP): los
use cases reciben el port; ``Fake*`` para tests y composición sin red."""
