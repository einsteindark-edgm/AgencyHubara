# PLACEHOLDERS — sobrescribir con valores reales out-of-band.
# Si el clúster usa SealedSecrets / external-secrets-operator / SOPS,
# reemplazar este manifest por el equivalente sellado.
apiVersion: v1
kind: Secret
metadata:
  name: hubara-medusa-secret
  namespace: default
type: Opaque
stringData:
  MEDUSA_BASE_URL: "https://medusa.hubara.example.com"
  MEDUSA_ADMIN_TOKEN: "sk_REPLACE_WITH_REAL_SECRET_FROM_MEDUSA_ADMIN_PANEL"
