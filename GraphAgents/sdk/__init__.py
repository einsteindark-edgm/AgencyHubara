"""SDK de GraphAgents — superficie propia (manifest model + loader + testkit +
connectorkit + cli). NO importa el monorepo AgencyHubara: son arquitecturas
separadas que se unen recién en la fase B por un puente HTTP/execution-id.
"""
