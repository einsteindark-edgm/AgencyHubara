"""Capabilities — StateGraphs de LangGraph (task graphs deterministas). Cada
módulo expone `build() -> CompiledStateGraph`. El esqueleto es PURO; el LLM/IO va
en nodos marcados (G-DET). El rojo de cada capability es un golden-replay.
"""
