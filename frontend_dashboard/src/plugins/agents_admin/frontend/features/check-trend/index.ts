// La gráfica vive en `@/shared/ui` (la reusa el laboratorio): el contrato de
// `check-stats` ya tiene la forma de vista genérica, así que se re-exporta tal cual.
export { CheckTrend } from "@/shared/ui";
